# Delivery Report Template

| Field | Value |
|-------|--------|
| Build Order ID | BO-FE-U02 (AXIOM-V2-BO-FE-U02-DRAFT-001 `b5f60dc6…`, ADOPTED AS DRAFTED `07ecde63…`) |
| Wave / Unit | FE-U02 — Global Chrome |
| Version | v1.0.0 |
| Date | 2026-09-11 |
| Author | Development Authority |

*(Template: `docs/templates/DELIVERY_REPORT_TEMPLATE.md` md5 `5c8e9de9…` per U01 precedent; CF-1 re-stamp offer stands.)*

## Executive Summary

The chrome now tells the truth. The asserted heritage cluster (`Gate CLOSED` / `Research-only` / `Presentation shell` / `live:simulated` and both SAL-2 recitals — **nine expelled sites, ACC-1-R1 census fully reconciled**) is replaced by two backend-sourced chips: the **locked-config mode badge** `RESEARCH · NON-ACTUATING` with an honest verification mark (turns *verified* only after a real authed `/api/v1/v2/mode` read — witnessed live in the backend log), and the **platform health chip** (REACHABLE/UNREACHABLE/CHECKING on a generation-guarded poll, stale ⇒ CHECKING). The rail gained an honestly persisted collapse (declared election `axiom_chrome_rail_collapsed`, survives reload — browser-witnessed). The palette answers gibberish with "No matching workspace or action" — never faux results. Fail-first honored: **15 coupons, 10 genuine reds** before implementation. Full suite **190 files / 1,032 / 0**; the 189/1,017 floor grew by exactly the chrome family. All ACC duties + rider ACC-1-R1 discharged in-pack.

## Objectives Completed

BO §2.1 health strip ✅ (DEGRADED N/A-with-reason; FE0-BF-5) · §2.2 mode badge ✅ (no capability cue; needled) · §2.3 operator chip ✅ (session as-is) · §2.4 palette C4-a ✅ (real registry; honest empty) · §2.5 role-aware rail + collapse election ✅ · §2.6 route announcements ✅ (as-built conforms; witnessed) · §2.7 branding covenant ✅ (logo sole asset) · §2.8 tokens baseline ✅ (codified by citation; zero additions — ACC-2 rows) · §2.9 heritage expulsion ✅ (9 sites; out-of-boundary protected) · §2.10 registry preservation ✅ (16 routes + alias through the rail).

## Architecture Summary

One new client seam: `fetchV2Mode()` — added after the shell-hosts-only coupon caught the DA's first raw-fetch draft (self-catch #1, disclosed). Rail gained optional `collapsed/onToggleCollapsed` props; shell owns the persisted election. No other architecture moved. Self-catch #2: `disposition` carried the UI-006 `position` needle — reworded to `stance` (4 sites).

## Files Created / Modified

**Bounded diff: 11 files** (E-7 full hashes; before-plane = HEAD `9c78afa` bytes):
5 chrome-family (Shell.tsx/.css, Rail.tsx/.css, CommandPalette.tsx) · 1 NEW `ChromeContract.test.tsx` (15 coupons) · `api/client.ts` (seam addition only) · 4 heritage coupons superseded-by-citation.

**ACC-3 coupon-disposition table (by name):**
| Coupon | Disposition |
|---|---|
| uiconv `SAL-2 … shell presentation surfaces` (shell arm) | superseded-by-citation → **negative pin** (`not.toContain`) |
| WorkflowNavigationCompletion `…preserves_gate_closed_and_research_only_status` | superseded → chrome-cluster-scoped negative pin + truthful-badge pin; `Research-only` count 2→1 (surviving hit = out-of-boundary echo) |
| MarketWorkspaceCompletion `…accessibility_responsive_and_registry_integration_hold` | superseded → cluster-scoped negative pin + badge pin |
| ArtifactExplorerCompletion `…no_actuation_recompute…` + `…integration_hold` | superseded → cluster-scoped pins (its needle corpus also caught self-catch #2) |
| **Byte-still heritage (named):** `terminalShell.test.tsx`, `terminalWholeSurface.test.tsx`, all `components/terminal/*` coupons, `f00_design_system`, `uiconv_p01_shell`, U01's `LoginPage.test.tsx` (22) | UNTOUCHED — verified green in E-6 |

## Testing Summary

Fail-first: 15 coupons red-run vs as-built shell — **10 RED / 5 green** (E-3 red transcript). Post-implementation: 15/15; neighbors green; **full suite 190 files / 1,032 tests / 0 failures** (E-6). tsc clean. ACC-5 twin laws couponed separately (health stale⇒CHECKING; badge locked-config+mark).

## Validation Evidence

Pack `FEPACK-FEU02-001` — 12 renders + E-2/E-3 (up 30 lines · down 4) + E-4 scans (11 needles zero in-boundary) + **E4_ACC1_R1_EXPULSION_CENSUS.md** (9 sites/8 lines, tallies reconcile line-by-line; the census correction self-reported) + E-5 + E-6 + E-7 (with ACC-2 rows: all four conditional files BYTES NOT MOVED, md5-proven) + capture script + manifest. Real-conditions law absolute: real login, real authed mode reads (backend log), genuinely stopped backend for down-phase, real reload for persistence, ACC-4 controls each producing witnessed real effects (theme class flip, settings open/close via its real control, search dialog, sign-out landing at the door).

## Risks

Settings overlay ignores Escape (close is its explicit control) — out-of-boundary heritage observation, recorded not fixed. Narrow (<768px) hides the header status cluster per heritage responsive CSS — truthful values remain in DOM; a future unit may elect a narrow-form status presentation (observation, not defect).

## Technical Debt

FE0-BF-5 opened (health granularity). F-4/F-5 untouched. No new debt; two dead chip CSS classes removed with their chips.

## Documentation Updates

State → v112.0.0 · campaign line FE-U02-DELIVERED · this DR · pack manifest.

## Known Limitations

jsdom coupons assert DOM truth; visual truth rides the 12 real-browser renders. The narrow status-cluster disclosure above.

## Recommendations

Operator visual review of the truthful cluster renders; the B-battery runbook (`OPERATOR_B_BATTERY_RUNBOOK_U02.md`) whenever ITRGA wants the second-party plane.

## Readiness Statement

> Implementation completed and submitted for independent ITRGA review. Not self-approved.

**Approval-bearing pack: `FEPACK-FEU02-001`** — awaits the Operator Decision naming it, then ITRGA determination. Only then does FE-U03 become draftable.
