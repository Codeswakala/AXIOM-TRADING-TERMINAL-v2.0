# VM-002 — IMAGE-PLANE + RUNBOOK ARRIVALS (second interim record)

| Field | Value |
|---|---|
| Document ID | AXIOM-V2-VM-FE-U01-SPINE-002 |
| Date | 2026-09-10 |
| Parent line | `AXIOM-V2-VM-FE-U01-SPINE-001` → determination still SUSPENDED, converging |

## 1. NEWLY-MEASURED IDENTITIES — 6/6 MATCH against verified -004 manifest claims

- `render_desktop_error_transport_unreachable.png` = `bde7e38e…` (779,776 B)
- `render_narrow_error_401.png` = `bb0e6eba…` (122,683 B)
- `render_narrow_error_transport_unreachable.png` = `e4b37a01…` (130,158 B)
- `render_narrow_ref003_initial.png` = `5565b2a8…` (119,725 B)
- `REF-001_Login_Signup_Screens.jpg` = `24aff50a…` (88,108 B) — **first reference ever in corpus**
- `REF-003_Hero_Holographic_Terminal.png` = `a7ef8fe7…` (463,116 B)

**Render custody: 5/7 of -004's renders now verified; references 2/3.**

## 2. EYEBALL CONTENT CHECKS (mantra: captions ≠ claims — seen directly)

- Narrow renders: pattern-(b) chip + reachability tri-state visible (green REACHABLE / amber UNREACHABLE); transport banner amber with the explicit *"transport failure, not a credential rejection"* wording — the C3 distinction rendered, not asserted.
- 401 narrow/desktop: red banner *"Invalid username or password"* with admin pre-filled — verbatim-detail posture consistent.
- REF-001 visual confirms exactly the AAE-030…036 subject (social-auth row, register, recovery — the excluded family pre-election); REF-003 carries the exact fiction-readout blocks inherited by REF-003-E1 (ESMS/NQMS/… + DATA FEED/LIVE + frozen `2025-05-23` timestamp) — the fiction provenance is visual, not asserted.

## 3. OPEN REMAINDER

1. `render_desktop_error_401.png` (claim `ab66d0be…`)
2. `render_desktop_reduced_motion_static.png` (claim `a6af3d9c…`)
3. `REF-002_Login_Page_Template.png` (claim `343be6a0…`) — **completes §9 HOLD retirement**
4. **B-battery paste** — the runbook is pre-run by the DA with expectations tabled; the second-party plane is your terminal's raw output of that same block.

## 4. STANDING

No mismatches to date on any byte measured in-corpus (spine 43/43 · images 7/7 so far). Suspension continues until the four remainders land; the determination frame and grade are pre-declared, nothing is presumed.
