# ITRGA REVIEW — V2·BE-12E: RECONCILIATION + INCIDENT WORKFLOWS (base)
`16-section house template · 2026-09-09 · Reviewer: ITRGA`
`Finding family: V2-BE12E-DEL-NNN · Verdict at §8: CORRECTION REQUIRED`

## 1. SCOPE OF PROVENANCE VERIFIED
- Delivery: `AXIOM-V2-BE-12E-DR-001` v1.0.0 against **BO-V2-BE12E-001**
  (DR-filed BO md5 `cad894b5877277da23ca103c741e1d11` — consistent with
  the order as issued 2026-09-09).
- Evidence custody (measured at intake):
  DR `md5 1f73674e…` · FAILFIRST `4a241164…` · SOURCE `2f3949c4…` ·
  TESTRUN `1aa64112…`. All four artifacts read in full; every quoted
  line below walked against the delivered bytes, not the DR's summary.
- Suite tail from E-02: **`1310 passed, 2 warnings`** — 1,290 + 20,
  inside BO §2's pinned window 1,308–1,318; the 2 warnings are the
  carried pair (Starlette deprecation + inherited asyncio-mark), no new
  noise. Count arithmetic verified against the transcript's own
  collection (all 20 new coupon names enumerated: 14 engines + 6
  migration/borders, every one PASSED; both supersession coupons
  PASSED).

## 2. INPUT PROVENANCE WALK
1. **OBS-E witness is genuine.** Stash target = THE LOCUS itself
   (`reconcile/money.py`): collection genuinely interrupted (import
   fan, "1 error during collection"), restoring byte-identical sha
   `25a53f33…` == manifest entry #2. The failed fan confronts exactly
   the locus's own consumer set (the engines coupon file). Method
   lawful; restore proven, not asserted.
2. **Zero-12D-moves is PROOF, not claim.** Manifest recites the
   thirteen 12D-set files byte-unmoved AND pins `13-file recompute NOW
   == d25c4857…` == the fielded lxe-1.2.0. The migration coupon then
   re-computes lxe-1.3.0 from disk over the 18-file set and asserts
   equality to the registry row — the disk hash is the registry hash
   by construction. This is the discipline the carry-item required.
3. Byte-still recitals stand: boundary file `9ba9fd82…` (BE-11 pin
   unmoved); migration 0056 == the operator's fielded pin.
4. Delivery report: full 16 sections, known-findings table honest
   (two authoring notes, both re-verified — see §6).

## 3. BO CLAUSE WALK (§1.a–§1.g)
| Clause | Walk | Result |
|---|---|---|
| §1.a.1 evidence-only / no self-activity | positive-pin scan (timer/scheduler/cron/signal/`threading.`/`background` tokens, corpus ≥5 asserted) + cross-package no-writer BOTH directions (`V2LiveExecIncident` ∉ reconcile; vice versa) | PASS |
| §1.a.2 verb law + named exception | run = SAL-4 no-step-up with rationale recited in code + docstring; open/close confirm-rank via sealed `require_step_up` (border: both verbs refuse `step_up_reference_absent` — the standing sealed chain, never a new shape) | PASS |
| §1.a.3 lane non-commutation, final form | `PRACTICE_LOCK_ORDER == 5` recited; practice pass + practice refusal carry practice vocabulary only; refusal token ∉ INCIDENT_REFUSALS | PASS |
| §1.a.4 LOW-1 annotation closure | closure coupon asserts `mode="RESEARCH"` literal at the killswitch arm-insert AND asserts the valve body (post-docstring code region) NEVER names mode; **zero 12D byte moves** (§2.2); forward law = request world threaded (border: RESEARCH via API, PAPER engine-level) | PASS (LOW-1 CLOSED) |
| §1.a.5 LOW-2 annotation | singleton-writer posture recited in the two NEW module docstrings (prior-compver files frozen); no concurrency coupon built — per order | PASS (LOW-2 CLOSED) |
| §1.b DR-F2 locus | one locus (scan: `to_canonical_money` in money.py+engine.py only); one direction; interior Decimal only; float → typed `money_float_banned`; **ROUND_HALF_EVEN DELEGATED** via `quantize_2dp` import from the fielded bridge (pbr-1.0.0) with ties-to-even witnessed (0.125→0.12, 0.135→0.14) and an AST coupon proving NO second quantize anywhere in the locus; canonical-form pins exact (fixed 2dp, no exponent/`+`, single leading zero, `-0.00`→`0.00`); optional enum: BO's fallback honored with verbatim-clause disclosure (§6.1) | PASS (world-class clause) |
| §1.c reconciliation | read-only boundary; closed 3-dim scope; C-2 **both arms over the real chain** (empty ⇒ exactly 1 row clean/NULL-drift; seeded orphan ⇒ exactly one MORE row parity_break with `fill_unprojected`+`ledger_orphan` serialized); digest recomputed at Level I; NO VALVE — direct UPDATE/DELETE die on guards; zero-row GET shape typed `no_runs_recorded` | PASS |
| §1.d incident | DR-1 shape exact (severity CHECK SEV-1..4; instruments non-empty typed `incident_instruments_absent`; recovery non-empty at edge; NULL-iff-open pair); lifecycle every cell typed, non-firing; close via valve dance with `WHERE status='open'` predicate, guard recreated + verify-or-die RuntimeError; digest on open AND close (close digest ≠ open, recomputed from the WRITER's representation — authoring-note-2 law honored); cookie: no DELETE route | PASS |
| §1.e honesty pair + closures | register line byte-equal to BO §e.1 (≠ rendered via `\u2260`); supersession-by-citation of the 12D ordered pin (grep-able pointer + floor re-recited); counterfeit-witness scan POSITIVE-PINNED corpus (A–D DRs + witnesses + template + line, every path asserted), banned-token set, self-exclusion, honest phraseology allow-listed and asserted present; **§e.3 template-literal closure shipped** (full `142f8772…` hash pinned) | PASS |
| §1.f.1 closed vocabularies + F1 final | engine tuples closed + disjoint; F1 final `6+5+9+2+3−1 == 24` with only declared overlap `killswitch_armed` — **BUT see DEL-001: the wire speaks one validation word the closure does not know** | **PARTIAL — FAILS as claimed (`FULL active set`)** |
| §1.f.2 DEL-001-class borders | all ORDERED counter-arms present over the real 0057 chain: step-up-absent on both confirm verbs, instruments-empty typed, close-of-unknown typed, cycle open→close→re-close, guard inversion, count-2 restore, digests; reconcile arm likewise complete per order. Gaps relative to the WIRE (not the order): severity-invalid arm + 404 arms absent — folded into DEL-001/DEL-002 | PASS-as-ordered (wire gap: DEL-001/002) |
| §1.g migration 0057 | one-line law; 90→94/83→88/16→17; five perms exact with SAL mapping in permissions.py (enum+role+SAL); lxe-1.3.0 insert-only version-filtered, `evidence_ref=BO-V2-BE12E-001` — registry's last row names the last build (**carry item "compver rows with last build" CLOSED**); prior three rows stand and DISK-RE-PROVE; downgrade exact restores 90/83/16 with guard dance silent, lxe-1.3.0 removals verified-or-die; rendered CHECK names pinned from DB DDL; drift itemized with band tokens banned (`reconciliation`/`lxrecon`/`lxinc`/`incident` all absent); ZERO seed rows; both models registered; the two created_at indexes are the disclosed-only indexes | PASS |

## 4. THE CORRIDOR WALK (both borders, real 0057 chain)
Both borders ride the real alembic chain (upgrade 0056→0057 in-tmp),
login→permissioned client, Level-I (DB) verification after every API
act, env restore discipline unchanged from the 12D chassis. The
incident stamp on close updates actor/step_up/operator carriage and
the digest moves — recomputed from the sqlite-naive datetime through
the writer's tz-aware representation (harness law, third sighting,
correctly handled and disclosed). Template fresh-recompute stands in
the engine corpus; the kill-switch's 12D law is pinned untouched by
the mode-stamp closure coupon; nothing in the 12D corpus lost a coupon
(the 12D ordered-line pin was superseded by citation with the pointer
left grep-able per mechanism).

## 5. FINDINGS

## `V2-BE12E-DEL-001` — MEDIUM — the wire speaks one validation word the closed vocabulary does not know
`api.py` route `POST /incident/open` constructs
`_refusal(request, "incident_severity_invalid", …, status_code=422)`
inline at the edge — a NEW refusal id emitted on every envelope that
answers an out-of-vocabulary severity. It appears in **no** closed
tuple (`INCIDENT_REFUSALS` holds the ordered 3), in **no** F1 recital
(the engine coupon's claimed `FULL active set` arithmetic
6+5+9+2+3−1 == 24 is thereby false *as a statement about the wire*),
in **no** coupon (the incident border never asks an invalid severity),
and in **no** DR disclosure (the DR §3 API row enumerates routes only).
Envelopes are parsed by operators; a refusal reason the register cannot
recover is exactly what the closed-vocabulary line of house law exists
to prevent. The behavior itself is *better than the alternative* —
typed 422 beats a brute schema IntegrityError — so this is MEDIUM, not
HIGH: a closure/disclosure defect, correctable surgically.
**Required correction (CR-1 scope):** enumerate the id into the
closed law — add `incident_severity_invalid` to the engine's
`INCIDENT_REFUSALS` tuple (edge cites the engine constant; literal in
api.py retires), re-pin `test_12e_vocabularies_closed` to the
4-member form, re-state the F1 final recital as
`6+5+9+2+4−1 == 25` (disjointness re-witnessed), add ONE border
counter-arm (invalid severity ⇒ 422, `reason` read from the engine
constant), and disclose the amended closure in the DR re-issue. The
BO's closed-set text is thereby satisfied *on the wire*, not only in
the engine's tuples.

## `V2-BE12E-DEL-002` — LOW — the 12E lookup idiom is undisclosed and uncouponed (rides CR-1 free)
`GET /reconcile/{id}` answers `_refusal(request,
"reconciliation_not_found", …, 404)`; wire-visible, tuple-less,
uncouponed, undisclosed. Lawful under the standing 12B lookup-idiom
precedent (`submission_not_found`), and the reuse of the engine-pinned
`incident_not_open` at `GET /incident/{id}` 404 is noted as acceptable
(pinned-id lookup reuse). But this band's NEW lookup id deserves the
same roll-call the CR-1 gives its sibling: **rider on the CR** — add a
`GET /reconcile/{unknown}` 404 arm to the reconcile border and, with
CR-1's amended api.py, land a small wire-literal enrollment coupon over
`api.py` (`_refusal(request, "<literal>"` literals pinned == exactly
{`submission_not_found` (12B lineage, recited), `reconciliation_not_
found`}) with all other `_refusal` reasons pass-through-from-engine —
so the *wire's* literal surface is coupon-enrolled once, for the whole
ledger. LOW; rides CR-1.

## `V2-BE12E-DEL-003` — LOW — missing fill price silently canonicalizes to "0.00"
In the fill-parity loop, `payload.get("price", "0")` silently substitutes
zero for an absent price; a nil-priced projection (0.00) would then
falsely read parity, and absence is not surfaced as a drift fact of any
kind (closed kinds stay 4, correctly un-extended at runtime). Practical
risk is low (real fills carry prices; the 12B payload law holds), the
behavior is disclosed in the literal body and deterministic — evidence-
fidelity family, same weight as 12D's DEL-001. **Register-logged**;
remediation posture (NOT ordered at runtime): `payload["price"]` with
KeyError → drift fact under the existing `fill_price_mismatch`-with-
note mechanism (the late-binding path the uncanonicalizable arm already
uses); may ride CR-1 by DA election or stand for the closeout-era
register refresh.

## `V2-BE12E-DEL-004` — LOW — needle disclosure: the locus's second refuse reason under-disclosed in the DR
DR §3's locus row names `money_float_banned` only; the locus also
refuses `money_not_decimal` (malformed inputs), which IS coupon-
witnessed (`test_drf2_float_banned_typed`) but DR-invisible. Needle-
discipline family; LOW; one DR line in the re-issue closes it.

(No other findings. The disclosed authoring-notes were each re-verified
against the delivered bodies — see §6.1.)

## 6. VERIFICATION NOTES (observations, not findings)
1. **Authoring-note-1 RATIFIED as house-correct.** The 12C absence-
   with-pin coupon lawfully expired when this BO built the pinned-absent
   workflow — surfaced by a *genuine* suite failure at 1,309 (the pin
   doing its duty to the day its absence became lawful), superseded BY
   CITATION with the surviving arm kept (modify/ still never speaks
   workflow tokens `open_incident(`/`recovery_path`/`incident_workflow`).
   The expiry-and-survival structure is exactly the pin's designed
   lifecycle. **RATIFIED.** (One cosmetic chain note, §6.3.)
2. **Authoring-note-2 ratified.** Incident digest on close recomputed
   from the writer's representation (tz-aware str), driver-naive text
   corrected — the 12B/12C harness-law family, third sighting, now
   standing harness law. Correct posture.
3. **12C pointer prose carries a stale middle-link name.** The 12D-era
   exact-form pin `test_register_line_12d_form_on_envelopes` was renamed
   at 12E to `test_register_line_superseded_pointer_12d`; the 12C
   pointer's prose still names the old identifier (chain remains
   followable terminal-to-terminal: 12C pointer → grep → only the prose
   remains → new name discoverable adjacent). Cosmetic; a one-line prose
   refresh may ride CR-1.
4. **Incident valve DDL body is whitespace-divergent from the
   migration trigger text** (split-line `RAISE(ABORT,` + message vs the
   migration's single-line form) — the refusal MESSAGE is byte-identical
   and trigger name/event/table identical; the 12D kill-switch form was
   full-text byte-equal. Zero operational consequence (drift gate does
   not compare trigger DDL; border counts by name). Observation;
   aligning the engine constant to the migration's single-line form may
   ride CR-1 if the file already moves for DEL-001.
5. **Delegation disclosure is the BO fallback, exercised honestly.** The
   BE-9 projection carries string-decimal facts with NO named rounding
   convention (provider seam `str(info.balance)`; reconcile exact-
   compare); the nearest standing NAMED convention is the bridge's
   `quantize_2dp` (pbr-1.0.0, D-B10-2DP mirror), recited verbatim, and
   the AST coupon proves the locus never re-implements. The BO's
   "projection body's OWN convention" primary with disclosed-fallback
   design held as intended.
6. **Reconciliation digest fact-set excludes id/created_at/
   correlation_id** (deterministic by design, disclosed in code,
   Level-I recomputed). Consistent with the harness-law family; noted.
7. **Counterfeit-scan corpus excludes the 12E DR** (could not exist at
   coupon-write). At the DR-5 closeout the 12E DR joins the band
   family; the corpus constant will need its one-line add then.
   Register note for the closeout verdict.
8. `position_money_uncanonical`/`fill_price_mismatch` note strings can
   carry the locus reasons as evidence text — kinds remain closed at 4;
   note text is the standing free-text family (LiveExecRefused notes
   precedent). Noted.
9. Position-parity honest closing (no LX-side position ledger exists to
   differ against; canonicalizability + enumeration it is) — disclosed,
   sane, PASS as scoped by BO ("enumerated at design commit").
10. Compver delete-guard recreated literal family stands (`…versions…`
    family) — the standing register advisory, unchanged.

## 7. COMPVER / FIELD-SHAPE CATENATION (for the post-CR-1 0057 card)
- **The lxe-1.3.0 expectation cannot be quoted from this delivery.**
  DEL-001's correction moves `incident/engine.py` — an lxe-1.3.0-scoped
  file — so the 18-file disk hash moves with CR-1. The migration's
  rolling hash is computed at apply-time, so the registry row will
  record the CR-1-era hash self-consistently; the card takes the C-2
  disclosure of the **CR-1 delivery's final bytes**, never this base
  number. Current base-era expectation (for the CR-1 diff's benefit):
  `30117f6475c09d13afd8788b2ca92199f760479e5c13fe7de97db4315ab86e9f`.
- 0057 card pins (drafted with every erratum from birth, as at 0056):
  tattoo 90/83/16 → **94/88/17**; rendered CHECK names
  `ck_v2_live_exec_reconciliation_ck_v2_lxrecon_{outcome,data_class}` +
  `ck_v2_live_exec_incident_ck_v2_lxinc_{severity,status,data_class}`;
  all FOUR compver rows exact (1.0.0/1.1.0/1.2.0 stand and **disk-
  re-prove** — the 13-file gate == `d25c4857…` rides again); indexes
  exactly the two disclosed created_at indexes; DDL print from birth;
  itemized drift + band tokens; migration pin re-measured post-CR-1.
- **FIELD LAW framing (important difference vs 12D):** the 12E tables
  are EVIDENCE LEDGERS — rows are lawful evidence once operator verbs
  run (unlike the activation/kill tables, for which zero-row IS the
  doctrine). The field posture nonetheless stays **zero-row by
  election through the card**: pins at every witness point, refuse/
  rollback probes only, **no reconcile run, no incident open/close on
  the fielded file** — the card will say so as election-with-reason
  ("the fielded lineage holds no runtime evidence rows of any band"),
  distinguishing the doctrine-law (12D) from the posture (12E).
- Suite pins move with CR-1: `INCIDENT_REFUSALS` 3→4, F1 24→25,
  coupon count 1,310 → 1,311..1,312 (one enrollment coupon + border
  arm; corpus arithmetic re-enumerated pre-commit per pointer lane).

## 8. DETERMINATION
### **V2-BE-12E — CORRECTION REQUIRED**
Gating: `V2-BE12E-DEL-001` (MEDIUM) — the wire's refusal surface is
not closed under the closed-vocabulary law; the F1 "FULL active set"
claim is false on the wire by exactly one word. Register-logged:
DEL-002 (lookup idiom), DEL-003 (missing-price zero-default),
DEL-004 (locus reason under-disclosed) — all LOW, all with named
remediation, none requiring architecture change. Everything else in
the BO is satisfied with unusually high fidelity (§3 table). **Suite
floor NOT re-pinned** (stays `AKX-12D-SUITE-FLOOR: 1,290 / 0`); CR-1
review re-pins at acceptance.

## 9. NEXT AUTHORIZED ACTIONS
```text
1. DA CR-1 against DEL-001 (REQUIRED) + riders DEL-002 (404 arm +
   wire-literal enrollment coupon), DEL-004 (DR line), §6.3 prose
   refresh, §6.4 alignment (by election); DEL-003 by DA election or
   closeout-era register refresh. Source amendments land as one
   CR delivery with the amended turbo: engine tuple→4, api constants,
   vocab pin re-pin, F1 recital 25, ONE new border arm, ONE
   enrollment coupon, DR re-issue, OBS-E-class witness for any moved
   writer file.
2. ITRGA INT-CR review (ITRGA_REV_V2_BE12E_CR_001) on CR-1 receipt.
3. On CR-1 APPROVED: 0057 field-apply card + pack (pins per §7,
   re-measured post-CR-1); FIELD LAW language per §7.
4. On FIELDED: the DR-5 closeout verdict (the ladder's last act) —
   with the L1–L6 fielded-witness enumeration gate pinned in BO §4.
Ordering law: no 0057 card until 12E ACCEPTED; no closeout until 12E
FIELDED.
```

## 10. SCOPE OVER-RUN CHECK
NO force-ward motion; NO gateway/actuation addition; NO new provider;
the two tables carry guard-pair + zero-UPDATE with one valve on the
incident only — exactly as ordered. The extra edge behavior (typed
severity at the edge) is the kind of over-delivery houses forgive
when it is *disclosed*; the finding is the missing disclosure, not
the behavior. Over-run: NONE beyond DEL-001/002's surface.

## 11. RISK
Single residual risk retained by design and annotation: singleton-
writer posture (LOW-2 law, applies to the incident valve window);
the remaining risks (float at seam, fake canonicalization, evidence
self-activity) are coupon-armed shut by this delivery.

## 12. TRACEABILITY & DISCIPLINE FIT
REM-001 full; C-2 disclosures complete incl. the verbatim rounding-
convention clause per BO; OBS-E genuine; both errata executed from
birth as drafting law (this is now the band's habit); suite window
honored; honest-expiry machinery of the register-line/pin world ran
itself correctly through two supersessions.

## 13. N-O11 / SELF-ACTIVITY COMPLIANCE
No timer/scheduler/signal token in the 12E surface (positive-pin
scans); every verb operator-initiated; evidence plane inert by
construction. COMPLIANT.

## 14. HONESTY-CLAUSE INTEGRITY
The conviction of this band was keeping it honest in BOTH directions:
the honesty pair shipped correctly (clause on envelopes; scan on
claims; template literal). The single vocabulary miss is the kind of
thing the honesty machine itself flags in a register read — the
review records it and moves it to closure. Nothing about the
delivery's candor discouraged: the expired-pin disclosure,
authoring-note-2, and the delegation-fallback disclosure are the
delivery arguing against itself where it could have stayed silent —
and that is the behavior this house reviews *for*.

## 15. REGISTER POSITION
Carry-list after this base delivery: DEL-001/002/003/004 (this band)
+ the closeout-era notes (fake-witness corpus add for 12E DR; DEL-003
posture if unridden). The DR-5/DR-3(i) closeout gate stands pinned
and untouched by this verdict's outcome class.

## 16. CLOSING
The ladder's final band shipped as the best delivery of the ladder —
and still earned its correction cycle where the wire broke the
vocabulary's own closure by one word. CR-1 is surgical; acceptance
then re-opens the shortest road left in the whole campaign:
CR-1 → 0057 card → FIELDED → `BE-12 CAPABILITY-COMPLETE`.

**We don't guess. We prove.** — ITRGA, 2026-09-09
