# ADOPTION RECORD — BO-FE-U02 (Global Chrome)

| Field | Value |
|---|---|
| Document ID | AXIOM-V2-BO-FE-U02-ADOPT-001 |
| Date | 2026-09-11 |
| Operator decision | **ADOPT** (verbatim, 2026-09-11) — **APPROVED AS DRAFTED** |
| Instrument adopted | `AXIOM-V2-BO-FE-U02-DRAFT-001`, without amendment |
| Pinned contract | PC-FEU02-1 — **to be filed** per D04 format (md5 `4521605b…`); re-keys to its filed identity after §6 docs-only turn executes |
| Prior unit | FE-U01 **CLOSED** (`AXIOM-V2-DET-FE-U01-002`, TRANSMISSION-ADAPTED); census floor re-based 189 files / 1,017 tests / 0 failures |
| Effect | **FE-U02 implementation is authorized to the DA** within the stated boundary — the second implementation-bearing instrument of the V2 frontend program, first under the ratified **FE custody law** |
| Executing authority | **DA only. ITRGA implements nothing** (role boundary §1/§37; carried into this record) |

## 1. AUTHORIZED BOUNDARY (verbatim from the adoption)

The chrome family itself: app shell/layout primitive · header strip (identity block, command/search affordance, operator identity chip, time, mode badge, global health) · left nav rail (route set, role-aware visibility, keyboard, collapse) · route announcer · tokens/primitives baseline. The `*Shell`/`*Header`/`*NavRail`/`tokens` family with identities declared in PC-FEU02-1 and inventoried at E-7. No surface rendered *under* the chrome; no gated-capability cue (U14/U15/U16); no backend change; no `/login` interior touch.

## 2. REQUIRED DELIVERABLES (≤ the BO's §2, verbatim classes)

Truthful global-health strip (real `GET /health`; stale ⇒ "checking") · mode badge: RESEARCH · NON-ACTUATING, never PAPER/BROKER/LIVE · operator chip asserts nothing pre-auth (pattern (b) at chrome level) · command/search — **one** elected truthful form in the contract, faux results prohibited · role-aware nav keyed to real entitlement · honestly persisted collapse · truthful route announcements · branding covenant (`axiom-logo.png` only; new asset class = disclosure-and-election first) · tokens codified + visual-regression baseline · heritage literals expelled at the chrome boundary (F-B3-SCOPE ownership consequence) · 16 protected routes + `/chart` alias intact through the new rail.

## 3. BINDING ORDER OF WORK

**§6.2 docs-first, then fail-first, then implementation bytes:** (1) DA files PC-FEU02-1 under the D04 format — doc-class transmission, md5-pinned; the contract rides ITRGA review (adjudicable; acceptance-with-correction posture); (2) tests red against the as-built heritage shell for the declared state matrix and needle family — red evidence captured into the pack; (3) implementation; (4) `FEPACK-FEU02-001` custody-law packaged. No completion claim without state-matrix, needle-zero, and regression evidence.

## 4. SCOPE PROTECTIONS (verbatim standing)

Surfaces under the chrome · gated-capability cues (paper/broker/live vocabulary and affordances) · F-4/F-5 backend-coupled mechanics · FE-U17 console · any backend change · `/login` interior (U01, closed) · unrelated refactoring · FE-U03+ — **all unauthorized**. Out-of-boundary change = scope defect, escalated, never silently absorbed; discovered conflicts surfaced before widening.

## 5. EVIDENCE AND APPROVAL SEQUENCE (ordered, unskippable)

`FEPACK-FEU02-001` (E-1…E-9; real-condition captures at 1440×900 / 390×844; census re-measured against 189 / 1,017 floor, never assumed; **same-turn custody archive + extraction-rehearsed operator battery runbook with pre-declared expectations; code never over the upload channel — channel law**) → **Operator visual approval, a separate state from implementation completion**, decision explicitly naming `FEPACK-FEU02-001` → ITRGA determination on the same pack → FE-U02 closure → FE-U03 (home terminal) becomes draftable.

## 6. DOCS OBLIGATION (precondition §6.2)

The DA's contract turn is docs-only and precedes all evidence: file `PC-FEU02-1` at the repo design path per D04's format, transmit doc-class with md5, publish filing identity; E-1's pin re-keys to the filed identity thereafter.

## 7. GOVERNANCE BOUNDARY (verbatim standing)

Authorizes FE-U02 implementation only. No backend governance change; no broker/paper/live/AI capability; no authorization of FE-U03+. Standing pendings unchanged: U3 formal adoption word (recorded, non-blocking); FE-U17 uncommissioned (independently schedulable); FE-U14/15/16 gate-blocked (live unit future-gated F01); the §G.3 heritage revert-disposition word still pending (outside this unit's boundary).

## 8. LOOP STATE AT THIS RECORD

FE-U01: **CLOSED** (accepted surface `FEPACK-FEU01-004`) · FE-U02: **IN BUILD (DA-authorized, docs-first stage)** — the corpus awaits `PC-FEU02-1` · FE-U03: not draftable until U02 closes · the live campaign remains parked F01.
