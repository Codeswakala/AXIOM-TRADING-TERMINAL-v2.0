# TECHNICAL_DEBT_REGISTER.md

| Version | 3.0.12 |
| Status | **Canonical consolidated register** (Tier 7) |
| Updated | 2026-07-29 (Amendment 4 provenance submission form) |

> Supersedes scattered `docs/technical-debt.md` narrative as the single status table.

| ID | Description | Severity | Status | Target |
|----|-------------|----------|--------|--------|
| TD-001 | No persistence (original) | — | **Closed** U02 | — |
| TD-001b | Postgres verification | Low | **Closed** Operator/U03 | — |
| TD-002 | ML package stub only | Medium | **Reduced** W2-U01 dataset subpackage created; feature/model work deferred | Wave 2 |
| TD-003 | Legacy `/ws/status` unauthenticated | Low | **Closed** W1-U01 | — |
| TD-004 | No auth (original) | — | **Closed** U04 | — |
| TD-005 | Handcrafted CSS / no design tokens | Low | **Closed (CONV-P01/P02)** | Single token system via var(--ix-*); 0 ad-hoc hex outside tokens.css; duplicate pages retired |
| TD-006 | No CI/containers | Medium | **Closed** U08; CI gate matured W1-U04 | — |
| TD-007 | Filename alias drift | Low | Reduced U08 F-4 | Housekeeping |
| TD-008 | No Playwright E2E | Medium | Open | Future UI/E2E unit |
| TD-009 | Dual schema create_all | Medium | **Closed** U08 | — |
| TD-010 | Minimal persistence API | Low | **Closed** W1-U01 | — |
| TD-011 | JSON feature payloads | Low | Open | W2-U03 feature store |
| TD-012 | npm audit critical/high | High | **Closed** W1-U04; npm audit 0 vulnerabilities | — |
| TD-013 | Row-by-row upsert | Medium | Open | Future ingestion scaling |
| TD-014 | Naive UTC timestamps | Medium | **Closed** W1-U01/W2-U01 | — |
| TD-015 | Many public endpoints | Medium | **Closed** W1-U01 | — |
| TD-016 | No gap detection | Low | Open | W2-U02/W2-U04 |
| TD-017 | No refresh reuse detection | Medium | **Closed** U08 | — |
| TD-018 | Bootstrap admin defaults | Medium | **Closed** U08 | — |
| TD-019 | JWT HS256 only | Low–Med | Open | IdP later |
| TD-020 | bcrypt pin | Low | Open | Later |
| TD-021 | Simulated live only | Medium | Open | Future market data/broker unit |
| TD-022 | WS JWT in query string | Medium | **Closed** U08 | — |
| TD-023 | No tick/order book | Low | **Closed WONTFIX (C-1 Permanence)** | Order book, depth ladder, and tick DOM permanently excluded per constitutional boundary C-1 |
| TD-024 | Live persist every interval | Medium | Open | Later |
| TD-025 | Live quotes hook-local | Low | Open | If multi-page |
| TD-026 | Chart indicators deferred | Low | Open | Later chart units |
| TD-027 | Multi-adapter timers | Low | Open | Later |
| TD-028 | Chart seed synthetic | Low | **Enforced by W2-U01 guard for ML datasets** | — |
| TD-029 | Multi-TF UI vs M1 sim | Medium | **Closed** DATA-P02 (`ITRGA_DETERMINATION_DATA-P02.md`, 2026-08-18): "The TD-029 notice is now true… the disclosure and the behaviour finally agree." Server-side wall-clock aggregation + typed native/aggregated/unavailable discriminant; the resampled notice renders only for genuinely aggregated series | — |
| TD-030 | Drawing tools deferred | Low | Open | Later |
| TD-031 | SQLite concurrency under live | Low | Accept Postgres prod; W1-U01 harness stabilized | Ops |
| TD-032 | Audit `session.refresh` 500 on inserts | High | **Closed** U08.1 | — |
| TD-033 | No first-class observability/metrics | Medium | **Closed** W1-U02 | — |
| TD-034 | CI missing Ruff/audit/local equivalent gate | Low–Med | **Closed in code** W1-U04; real run evidence carried | Operator/CI evidence |
| TD-035 | Real MT5 adapter / broker connection absent | High | **Deferred by governance** | Future broker unit / Wave 6 as authorized |
| TD-036 | Broker live quotes/account info absent | Medium | **Deferred by governance** | Future broker data unit |
| TD-037 | Execution simulator / paper trading / order dispatch absent | Critical | **Deferred by roadmap** | Wave 6 only |
| TD-038 | Feature computation not implemented | Medium | **Closed foundationally** W2-U03 (feature framework/store v1); advanced features deferred | Later feature units |
| TD-039 | Dataset split engine not implemented | High | **Closed** W2-U04 (temporal split engine + manifest) | — |
| TD-040 | Experiment registry not implemented | High | **Closed** W2-U05 (registry + pre-registration workflow) | — |
| TD-041 | Model training/evaluation not implemented | High | **Reduced** W2-U06 baseline harness implemented; statistical/calibration/economic validation deferred | W2-U07+ |
| TD-042 | Live Deriv/provider connection absent | Medium | Explicitly deferred; W2-U02 provider adapter skeleton only, no credentials/I/O | Future gated provider unit |
| TD-043 | Market metadata not yet used by feature/evaluation reports | Low-Med | **Reduced** W2-U03 proves metadata evaluation-readable but feature-excluded; full evaluation consumption deferred | W2-U10 |
| TD-044 | Advanced ML dependencies not adopted | Medium | Pure-Python baseline used to avoid Py3.14 wheel risk | Future model unit compatibility spike |
| TD-045 | Statistical validation suite not implemented | High | **Reduced** W2-U07 statistical validation framework implemented; calibration/economic validation deferred | W2-U08/W2-U09 |
| TD-046 | Calibration/economic validation not implemented | High | **Reduced** W2-U08 calibration implemented; economic validation deferred | W2-U09 |
| TD-047 | Advanced statistical dependencies not adopted | Medium | Pure-Python bootstrap/CI used; compiled dependency spike deferred until needed | Future validation/model unit |
| TD-049 | Market-impact modeling not implemented | Medium | Explicitly out of W2-U09; future research | Future economic/execution research |
| TD-050 | Live drift monitoring loop not implemented | Medium | W2-U10 design records only; live monitoring Wave 3+ | Wave 3+ |
| TD-051 | Auto-retraining not implemented | Medium | Deliberately prohibited; future retraining requires governance evidence | Future governed retraining |
| TD-052 | Operator-facing signal contract/persistence absent | High | **Closed foundationally** W3-U02 advisory signal table/service/history API implemented; UI remains deferred | — |
| TD-053 | Signal dashboard/alerts absent | Medium | Explicitly deferred; W3-U02 is backend/API only | W3-U05/W3-U06 |
| TD-054 | Emit-time domain/calibration/economic guardrail maturation incomplete | Medium-High | **Closed foundationally** W3-U03 emit-time domain/calibration/economic/staleness guardrails implemented | — |
| TD-055 | Live market inference adapter absent | Medium | **Closed foundationally** W3-U04 live market inference adapter implemented using existing W1 seam | — |
| TD-056 | Automated background signal expiry worker absent | Low-Med | W3-U03 domain service and `current_only` API enforce expiry/current filtering; scheduler deferred until UI/ops need it | Future ops unit |
| TD-057 | Operator advisory dashboard absent | Medium | **Closed foundationally** W3-U05 protected advisory dashboard/signal workspace implemented | — |
| TD-058 | Live signal alert/push stream absent | Medium | **Reduced** W3-U06 persisted/read-only alert records and API implemented; live push/external notifications still deferred | Future notification unit |
| TD-061 | Alert UI indicator absent | Low-Med | **Closed** SURF-P02: left-rail unread badge, terminal ALERTS right-dock tab, read-state-only acknowledge, severity styling, alert detail record | — |
| TD-062 | External alert notification providers absent | Medium | Explicitly out of W3-U06; future gated provider work | Future notification unit |
| TD-063 | Persisted analytics snapshot artifact absent | Low | W3-U07 intentionally read-only aggregation over existing signal records; persisted artifact not required by Build Order | Future analytics archive if needed |
| TD-064 | Advanced outcome/return attribution absent | Medium | W3-U07 avoids guaranteed-return analytics; deeper validated outcome analytics requires future governed outcome data contract | Future analytics unit |
| TD-067 | Correlation report UI absent | Low-Med | **Reduced** W4-U07 Institutional Intelligence dashboard displays correlation report summaries; richer detail UI deferred | Future UI detail unit |
| TD-068 | Regime report UI absent | Low-Med | **Reduced** W4-U07 dashboard displays market context report summaries; richer detail UI deferred | Future UI detail unit |
| TD-069 | Learned regime model absent | Low-Med | W4-U03 intentionally uses explainable rules over normalized features; learned/clustering model would require experiment governance and dependency spike | Future governed model unit if needed |
| TD-070 | Scenario report UI absent | Low-Med | **Reduced** W4-U07 dashboard displays hypothetical research summaries; richer detail UI deferred | Future UI detail unit |
| TD-071 | Advanced scenario methodology absent | Medium | W4-U04 implements simple hypothetical shock research artifact; richer scenario methods require future Build Order | Future scenario analytics unit |
| TD-072 | Portfolio/risk report UI absent | Low-Med | **Reduced** W4-U07 dashboard displays market-series risk summaries; richer detail UI deferred | Future UI detail unit |
| TD-073 | Real portfolio/account risk integration absent | High | Explicitly forbidden in W4-U05; would require future governance and execution/broker/account authorization | Future governed unit |
| TD-074 | Governed forward outcome data absent for signal validation | Medium | W4-U06 reports outcome_data_status not_available and avoids fabricated realized performance | Future outcome-data contract |
| TD-075 | Signal validation UI absent | Low-Med | **Reduced** W4-U07 dashboard displays advisory quality review summaries; richer detail UI deferred | Future UI detail unit |
| TD-076 | Institutional Intelligence detailed report drill-down absent | Low-Med | W4-U07/W4-U08 dashboard provides list/summary cards only; detailed per-report UX deferred | Future UI detail unit |
| TD-077 | Institutional Intelligence dashboard lacks cross-report navigation/drilldown | Low | W4-U08 closeout does not expand UI beyond OBS-1 fix; richer navigation deferred | Future UI detail unit |
| TD-078 | External LLM provider absent | Medium | W5-U01 intentionally uses deterministic local assistant; external LLM requires future gated Build Order | Future assistant provider unit |
| TD-079 | Collaboration artifact persistence absent | Medium | **Reduced** W5-U02 persists assistant responses/refusals, W5-U03 persists chart annotations, W5-U06 persists trade plan notes, and W5-U07 persists journal entries; W5-U08 closeout evidence index prepared | Future collaboration artifacts only as authorized |
| TD-080 | Assistant UI absent | Low-Med | W5-U03 adds chart annotation UI and W5-U04 adds signal investigation UI, not an assistant chat/panel; assistant surface deferred until authorized | Future W5 UI unit |
| TD-081 | Chart research annotation automation/E2E absent | Low-Med | W5-U03 adds manual browser evidence checklist and component tests; automated browser E2E remains deferred | Future UI/E2E unit |
| TD-082 | Signal investigation automated browser E2E absent | Low-Med | W5-U04 adds component tests and mandatory manual browser evidence checklist; automated browser E2E remains deferred | Future UI/E2E unit |
| TD-083 | Scenario comparison automated browser E2E absent | Low-Med | W5-U05 adds component tests and mandatory manual browser evidence checklist; automated browser E2E remains deferred | Future UI/E2E unit |
| TD-084 | Trade planning automated browser E2E absent | Low-Med | W5-U06 adds component tests and mandatory manual browser evidence checklist; automated browser E2E remains deferred | Future UI/E2E unit |
| TD-085 | Manual journal automated browser E2E absent | Low-Med | W5-U07 adds component tests and mandatory manual browser evidence checklist; automated browser E2E remains deferred | Future UI/E2E unit |
| TD-086 | Wave-5 automated browser E2E suite absent | Low-Med | W5-U08 closeout requires manual served-browser evidence across collaboration surfaces; automated E2E remains deferred | Future UI/E2E unit |
| TD-087 | Execution Research artifacts not yet implemented | Medium | **Closed foundationally** W6-U02…W6-U06 implement simulated runs/fills, ledger, risk reports, replay/experiments, and analytics reports | — |
| TD-088 | Execution Research UI absent | Medium | **Closed foundationally** W6-U07 adds display-only execution research workspace UI; richer UI/E2E automation remains deferred | Future UI/E2E hardening |
| TD-089 | Execution Research automated browser E2E absent | Low-Med | W6-U07 adds component tests and mandatory manual browser evidence checklist; W6-U08 requires served-browser E2E; automated browser E2E remains deferred | Future UI/E2E unit |
| TD-090 | TD-W6-CI-AUDIT — networked npm audit in Git-Bash CI intermittently blocked by registry connectivity | Low-Med | Carried environmental tracker; W6-U08 and W7-U06 recorded exit 0, W7-U07 closed by operator waiver when registry DNS failed | Operator/CI environment |
| TD-091 | Institutional Platform feature surfaces absent | Medium | **Reduced** W7-U02 implements workspace preferences; research management, plugins, reporting, portfolio dashboard, scalability remain deferred to later W7 Build Orders | W7-U03+ as authorized |
| TD-092 | Workspace customization automated browser E2E absent | Low-Med | W7-U02 adds component tests and mandatory manual browser evidence checklist; automated browser E2E remains deferred | Future UI/E2E hardening |
| TD-093 | Research management automated browser E2E absent | Low-Med | W7-U03 adds component tests and mandatory manual browser evidence checklist; automated browser E2E remains deferred | Future UI/E2E hardening |
| TD-094 | API catalogue persisted contract registry absent | Low-Med | W7-U04 intentionally uses generated route metadata and no table; persisted catalogue deferred unless future need is authorized | Future API governance unit |
| TD-095 | API rate/abuse guard absent | Medium | W7-U04 declares abuse guard deferred and adds no dependency/storage; future rate limiting requires dependency/spike and Build Order | Future API hardening unit |
| TD-W7-U07-RATE-GUARD | Enterprise API abuse/rate guard deferred | Medium | W7-U07 formally defers rate/abuse guard to avoid unspiked dependency/storage; future implementation requires dependency/storage spike and Build Order | Future enterprise hardening |
| TD-096 | Dynamic plugin runtime absent | High | Deliberately prohibited by R7-1; W7-U05 adds contracts/refusal only. Dynamic execution requires separate hard-gated Build Order and sandbox threat model | Future hard-gated plugin runtime only if authorized |
| TD-097 | Persisted plugin contract registry absent | Low-Med | W7-U05 uses code-defined constants for safety foundation; persisted registry deferred unless future need is authorized | Future plugin governance unit |
| TD-098 | Persisted advanced research report/export archive absent | Low-Med | W7-U06 generates reports on request with deterministic hash and no table; persisted archive deferred unless future need is authorized | Future reporting/archive unit |
| TD-099 | Portfolio research automated browser E2E absent | Low-Med | W7-U06 adds component tests and mandatory manual browser evidence checklist; automated browser E2E remains deferred | Future UI/E2E hardening |
| TD-065 | Compiled advanced ML wheel compatibility spike remains future work | Medium | **Discharged for numpy 2.5.1 / pandas 3.0.3 / scipy 1.18.0** by W4-U01 ITRGA approval; any other compiled dep still needs spike | Future compiled dependency if proposed |
| TD-066 | SQLite StaticPool live-market test flake in local_ci | Medium | **Closed** W3-U08.1 ITRGA approved; CI exit 0 and five-repeat stability proven | — |
| TD-060 | Advisory dashboard browser automation not in repository | Low-Med | W3-U05 evidence pack requires manual real-browser screenshots; automated browser E2E deferred | Future UI/E2E unit |
| TD-059 | External market data provider integration absent | Medium | W3-U04 explicitly reuses simulated W1 seam; external provider requires future gated Build Order | Future gated provider unit |
| TD-048 | Economic validation not implemented | High | **Closed foundationally** W2-U09 (cost model/provenance/sensitivity/report) | — |
| TD-UI005-COMPLETION-TIMEOUT | UI-005 completion test `test_ui005_completion_all_surfaces_are_existing_artifact_presentation_or_existing_research_notes` uses a fixed 10-second timeout and can expire under CI resource contention. | Low | Open; environment-bound observation accepted at P04 final | Raise test timeout or reduce harness render cost in a separately authorized hardening unit |
| TD-AXIOM-GIT-PROVENANCE | Repository provenance remediated through the first anchored baseline. Historical pre-v0.62 per-phase commit gap remains honestly disclosed. | High | **Closed by ITRGA** - baseline `07dd78e7f31f69e338316874f79d0cbc5815834a`, tag `AXIOM_v0.62.0_BASELINE`; no longer a Doc 11 pre-certification blocker. | Closed by ITRGA_REVIEW_TD-AXIOM-GIT-PROVENANCE_FINAL.md |
| TD-GOV-UI007-P04-PROVENANCE | P04 worktree provenance follows in-flight resolution of 56 committed merge-conflict blocks and the TerminalLayout deletion. ITRGA accepted the single-commit substitute method: marker scan, status, governance-document enumeration, and explicit R16 disposition. | High | **Closed for UI-007-P04** by ITRGA baseline-reference ruling | — |
| TD-GOV-UI007-ARCHIVE | UI-007-P03 Build Order and UI-007-P02 ITRGA review are cited by current records but absent from the repository snapshot; the user-supplied P03 determination and P04 Build Order establish the current chain, but the missing predecessor artifacts require archival recovery. | Medium | Open; disclosed in P04 delivery | Operator / ITRGA source recovery |
| TD-UI-POSTCSS-HIGH | High-severity transitive PostCSS advisory `postcss <=8.5.17` / GHSA-r28c-9q8g-f849 | High | **Closed** UI-007 remediation; ITRGA approved vite-8 toolchain remediation with `postcss@8.5.23` and `npm audit --audit-level=high` exit 0 | — |
| TD-UI-REACTROUTER-MODERATE | Moderate react-router / react-router-dom advisories disclosed by npm audit after PostCSS remediation | Moderate | Open, non-blocking; below high audit gate and not a production-readiness blocker unless elevated by future ITRGA certification review | Future dependency hygiene |
| TD-AXIOM-DEV-CREDENTIAL-LITERALS | Development credential literals remain in tracked source, tests, configuration, and governance records under ITRGA D-2 disposition. | Medium | Open, non-blocking for provenance remediation; **pre-certification for Doc 11 §2**. Amendment 2 records a two-entry hash manifest and separate counters; no plaintext is preserved in governance. | Dedicated security-remediation Build Order: environment/fixture injection with no literal defaults |
| TD-UI-CONV-P02-ORPHANED-PAGE-DEVIATION | `pages/ChartWorkspacePage.tsx` was a referenced legacy surface hosting shared chart components; the deviation was **superseded by relocation**: the module moved to `components/chart/ChartWorkspaceSurface.tsx` (all export names/contracts preserved, six dependent test files repointed) under OBS-CONV2-4 full discharge in the R1 transport package. ITRGA Patch Verification Record §3 confirms the disposition (97% similarity rename — relocation, not retirement). | Low | **Closed / superseded by relocation** (UI-CONV-P02 R1 transport package) | — |
| TD-UI-CONV-P02-SIG004-EVIDENCE-FIXTURE | Synthetic evidence signal `sig-004` (`signal_state=withheld`, `freshness_status=expired`) was inserted into the LOCAL dev database to power Level-I capture 06 (OBS-CONV2-3 mixed-state proof). Disclosed in DELIVERY_REPORT_UI-CONV-P02.md §R1-3 with seed:synthetic provenance. The database file is local-only and gitignored; no seed script or production schema changed. | Low | **Tracked — OBS-CONV2-5 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-CONV-P03-SCEN002-EVIDENCE-FIXTURE | Synthetic second scenario row `scen-002` ("Dovish Easing Path Shock") inserted into the LOCAL dev database to enable a genuine two-way side-by-side comparison in UI-CONV-P03 capture 03 (the P05 seed script creates a single scenario). Disclosed in DELIVERY_REPORT_UI-CONV-P03_CYCLE1.md §9.2 with seed:synthetic provenance. Database file is local-only and gitignored; no seed script or production schema changed. | Low | **Tracked — UI-CONV-P03 cycle-1 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-SURF-P02-ALERTS-EVIDENCE-FIXTURE | Three monitoring alerts (one per severity Literal: critical / warning / info, one acknowledged) inserted into the LOCAL dev database via the existing MonitoringAlertService.create_alert seam to power SURF-P02 Level-I captures 01-03 (genuine unread badge count of 2 and a real acknowledge write with audit row). Seed script: scripts/seed_surf_p02_alerts.py, surf-p02-seed actor and surf_p02_evidence_fixture lineage. Disclosed in DELIVERY_REPORT_SURF-P02.md. The database file is local-only and gitignored; no schema, endpoint or service change. | Low | **Tracked — SURF-P02 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-SURF-P03-UNPRIVILEGED-EVIDENCE-FIXTURE | One active operator with role `unprivileged` (a role absent from INSTITUTIONAL_ROLE_PERMISSIONS) inserted into the LOCAL dev database to power SURF-P03 Level-I capture of the scope-records access-denied notice (M3) and the per-source denial rows — produced by the REAL backend default-deny 403, not by interception. Seed script: scripts/seed_surf_p03_unprivileged.py, password operator-pass-123. Disclosed in DELIVERY_REPORT_SURF-P03.md. The database file is local-only and gitignored; no schema, endpoint or service change. | Low | **Tracked — SURF-P03 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-DATA-P01-EVIDENCE-ENVIRONMENT-FIXTURE | DATA-P01 correction cycle 3 (CA-DATA1-3, evidence-only) capture environment: the live feed was **stopped at capture time** (the platform's own start/stop control, POST /market/live/stop) and the watchlist quotes rendered from the **retained server snapshot** (`latest_by_symbol`, source live:simulated) — the frontend's designed quote-seeding behaviour; additionally, `live:simulated` candle rows were deleted from the LOCAL dev database before capture so the persisted series is seed-only and the sparkline provenance tag renders the short form `SEED:SYNTHETIC`. Every label shown in the captures is true of the captured state. Both facts are machine-recorded in `DATA-P01_CAPTURE_VERIFICATION.json` (Rev D) and disclosed in DELIVERY_REPORT_DATA-P01_CORRECTION3.md. The database file is local-only and gitignored; no schema, endpoint or service change. | Low | **Tracked — DATA-P01 cycle-3 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-DATA-P02-EVIDENCE-FIXTURE | DATA-P02 capture environment: six days of M1 seed history (8,640 bars × 11 symbols = 95,040 rows) inserted into the LOCAL dev database via the existing `chart_seed_service.seed_chart_history` seam (DA tooling `scripts/seed_data_p02_capture_history.py` — the same service the authenticated seed-history endpoint uses, invoked with a larger bars argument; no generator/seed/provenance logic modified, R1). Capture 04 removed USDCHF's M1 rows so the REAL backend aggregation path returned `kind=unavailable` (rows restored afterwards and verified at 8,640). The live feed was stopped at capture time; watchlist quotes render from the retained snapshot. Disclosed in DELIVERY_REPORT_DATA-P02.md. Database file local-only and gitignored; no schema change. | Low | **Tracked — DATA-P02 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-CHART-P01-EVIDENCE-FIXTURE | CHART-P01 capture environment REUSES the DATA-P02 fixture unchanged (same six-day seed, 95,040 seed-only M1 rows; same feed-stopped snapshot cycle; `live:simulated` rows deleted after the snapshot was taken). Capture 05 additionally relies on the D1 timeframe holding only 5 complete aggregated bars — a property of the fixture, disclosed in DELIVERY_REPORT_CHART-P01.md. Database file local-only and gitignored; no schema change; no generator change. | Low | **Tracked — CHART-P01 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-CHART-P02-EVIDENCE-FIXTURE | CHART-P02 capture environment REUSES the CHART-P01/DATA-P02 fixture unchanged (six-day seed, 95,040 seed-only M1 rows; feed-stopped snapshot cycle; `live:simulated` rows deleted after the snapshot). The snapshot is in-memory and was lost on a backend restart mid-phase; the feed start/stop cycle was re-run and the row deletion repeated (both disclosed in DELIVERY_REPORT_CHART-P02.md). Capture 08 relies on D1 holding 5 complete bars. Database file local-only and gitignored; no schema change; no generator change. | Low | **Tracked — CHART-P02 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-CHART-P03-EVIDENCE-FIXTURE | CHART-P03 capture environment REUSES the CHART-P02 fixture unchanged (six-day seed; feed-stopped snapshot). Two LEGACY annotation rows were inserted via the existing audited annotation API to power capture 05 (one with `content.visual.x_percent/y_percent`, one with `content.price_level` only) — real pre-anchoring shapes, exactly the M4 case. Fifty perf-fixture drawings were created and then deleted (audited) to measure S4 render cost (500 SVG nodes, ~33 ms re-render) — deletion leaves audit rows only. All disclosed in DELIVERY_REPORT_CHART-P03.md. Database local-only and gitignored; no schema change. | Low | **Tracked — CHART-P03 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-CHART-P03-ANNOTATION-VERBS | CHART-P03 extends the EXISTING annotation contract's verb surface: `PATCH` and `DELETE /collaboration/chart-annotations/{id}` were added (same CurrentOperatorDep auth, same factory validation, same audit trail with new `.updated`/`.deleted` audit actions) to support drawing reposition and deletion (S1) — an extension of the one contract, NOT a parallel persistence path. RBAC unchanged (no role/registry change; the endpoint-auth breadth test extended). Stated in DELIVERY_REPORT_CHART-P03.md. | Low | **Tracked — CHART-P03 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-POLISH-P01-EVIDENCE-FIXTURE | POLISH-P01 capture environment: the six-day seed was regenerated with the NEW zero-mean walk (the old fixture carried the +31% drift the phase removes); the feed was stopped/restarted and `live:simulated` rows deleted before capture 02 so the watchlist rendered the MIXED provenance state (long tags) — the state the clip fix exists for. The production build (`vite preview` on 4173) served captures 06/07, which is what makes the chunk-level network traces meaningful. All disclosed in DELIVERY_REPORT_POLISH-P01.md. Database local-only and gitignored; no schema change. | Low | **Tracked — POLISH-P01 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-POLISH-P01-LAZY-ROUTES | POLISH-P01 M2: route-level code splitting — four workspace pages (LiveMarket, InstitutionalIntelligence, TradePlanning, ManualJournal) and two stage views (ResearchHubView, ExecutionResearchView) became lazy chunks. The RBAC gate now enforces the EXISTING `allowedRoles` metadata at route render time (before the chunk fetch): an unauthorized operator sees ACCESS DENIED and the chunk is never requested (unit-proven + network-trace capture). Consequence disclosed: a role not in allowedRoles (e.g. the unprivileged fixture role) now receives an explicit denial page instead of a page whose APIs 403 — enforcement of existing metadata, not a new policy. The vite preview server gained a proxy block mirroring the dev proxy (bundler configuration, per M2). Stated in DELIVERY_REPORT_POLISH-P01.md. | Low | **Tracked — POLISH-P01 deviation register entry; no origin impact** | None required; carried as disclosure |
| TD-UI-POSTCLOSURE-HOTFIX | **Post-programme-closure defect found during the Operator-requested local-run verification (2026-08-19).** Symptom: on a fresh machine with the live feed never started, `1m → 1h`/`1d` timeframe switching crashed the chart stage ("Cannot update oldest data"). Root cause: the incremental update path detected only series-LENGTH changes; switching to a longer timeframe always REGRESSES the last-bar time (DATA-P02 M6 excludes the still-forming bucket), and the approved captures masked it because the merged live bar changed the length. Fix (defect class): the reset condition now also fires on last-bar time regression — `frontend/src/components/chart/PriceChart.tsx` only, plus a fail-first source-pin test. Artifact: `postclosure_hotfix.patch` sha256 `002eee83ae75fd94d922bede27c4917a660551e140507b6540b99d078a4015a2` (61 lines / 2 files; applies clean as the 17th chain element in a pristine clone). Suites re-run green: 476 BE / 860 FE, tsc 0. Full browser smoke test (login → terminal → 1h/1d/1m → indicators → confluence → lazy routes → stage views): zero page errors. Not ITRGA-reviewed; routed at the Operator's discretion. | Low | **Tracked — post-closure hotfix; disclosed in LOCAL_RUN_GUIDE.md §5** | Operator decision on ITRGA routing |
| TD-UI-POSTCLOSURE-BRAND | **Post-programme-closure Operator directive (2026-08-19): the Operator supplied the project logo as an image asset and instructed the DA to use it.** The mark now renders from `frontend/public/branding/axiom-logo.png` (410x408, opaque square, dark background with light artwork — used EXACTLY as provided, no cropping; measured, since the DA cannot view the artwork). The GA-173 compass+epsilon SVG remains only as a load-failure fallback, so the mark never renders blank; both surface testids (`login-brand-logo-mark`, `shell-brand-logo-mark`) preserved. Verified in a real browser: the image loads on the login page and the shell header (naturalWidth 410) with zero page errors. Artifact: `postclosure_brand.patch` sha256 `f4c08e41d9be28cd7ad829cb71d0a6b25319d6ea18e600be39b2a9c46e2a185c` (binary patch including the PNG; applies clean as the 18th chain element in a pristine clone; the PNG reproduces byte-identical to the upload, hash `0a0c716b…`). Operator-owned branding — supersedes GA-173 per the Operator's own later instruction. Not ITRGA-reviewed; routed at the Operator's discretion. | Low | **Tracked — post-closure brand directive; disclosed in LOCAL_RUN_GUIDE.md** | Operator decision on ITRGA routing |

| TD-UI-CAPASSESS-GENERATION-WIRING | **ITRGA post-closure capability assessment (CAPABILITY_ASSESSMENT.md `979f815f…`, 2026-08-19), FIND-1..4 consolidated.** Confirmed mechanically by the DA: (FIND-1) `LiveMarketInferenceAdapter` is exported and never instantiated — signals inference is unwired; (FIND-2) the five intelligence `create_report` services (correlation/regime/scenario/portfolio-risk/signal-validation) have zero runtime callers — the only backend `create_report` callers are the two execution-research endpoints (simulated, operator-triggered, which the assessment treats separately); (FIND-3) the collaboration router exposes GET-only assistant-response seams and the frontend suggestion chips set state only — no ask/submit path exists anywhere; (FIND-4) `backend/app/ml/__init__.py` declares `ML_PLATFORM_STATUS = "stub"` per W0-U01 and no `/ml` router or model artifacts exist. **All four are by design** (R3 honest-empty discipline, T-4/T-5 zero external LLMs, no training-data authorization in any approved phase, W0-U01 architectural boundary), and each surface says so honestly in-product. No code change without a Build Order. DA R1 checklist measured code-presence; the reconciled R2 record (CAPABILITY_CHECKLIST.md addendum) adopts ITRGA's runtime lens. | Medium | **Tracked — by-design absences now formally assessed; no action without authorization** | New Build Order if generation is ever authorized |
| TD-UI-CAPASSESS-SIMCLOCK | **ITRGA post-closure capability assessment FIND-5 (carried as OBS-3, but the register's OBS-3 row closed only the naive-timestamp class).** Confirmed mechanically: `backend/app/market/adapters/simulated.py` line 157 constructed `open_time = start_time + timedelta(minutes=seq)` with `interval_seconds=1.0` default — the simulated clock advanced one M1 bar per ~1 wall second (~60× real-time; the assessment estimated ~30× — same defect class). Violated the closure standard "a displayed value means what it claims". **RESOLVED by BO-B-00 (Operator Build Order, 2026-08-19), option (a) wall-clock bind:** the adapter now emits bar N only when the wall clock reaches `start_time + N minutes` (accelerated catch-up preserved while behind); fail-first probe pinned the defect (FUTURE-DATED BARS DETECTED … emitted within 0.40s, dated 16:54/16:55 vs bound 16:53:05); 4 pinned invariant tests added (`tests/test_b00_simulated_clock.py`, fake-clock deterministic + chronology-guard invocation with negative control + real-clock smoke); Level-I soak: wall-minute soak at production defaults emitted bar 16:57:00 at 16:57:00.001, FUTURE_DATED_COUNT=0 across both soaks; full suite 480 passed (476 + 4). Patch: `b00.patch.txt` sha256 `5662ec3d1dc7dcc0087a27a45e7f1993333413b6a1686fc20d3abffe616f55bd`, chain position 19, apply-check exit 0 in pristine clone. **Awaiting ITRGA determination.** | Medium | **Implemented by BO-B-00 — awaiting ITRGA determination** | ITRGA determination closes this row |
| TD-UI-CAPASSESS-NANOID-HIGH | **ITRGA post-closure capability assessment FIND-6, the new component.** Fresh `npm audit --json` (2026-08-19, clean `npm ci`): nanoid@3.3.16 HIGH (GHSA-2v37-7h3g-55p8, "custom generators can loop indefinitely when size is zero"), chain `vite@8.1.4 → postcss@8.5.23 → nanoid@3.3.16`, fix ≥3.3.18 via `npm audit fix`. This breaches the project's own remediation gate (the closed TD-UI-POSTCSS-HIGH row established `npm audit --audit-level=high` exit 0). The assessment's attribution of HIGH to react-router-dom/postcss differs from the current audit: react-router/react-router-dom are MODERATE and already registered (TD-UI-REACTROUTER-MODERATE); the postcss advisory itself is closed (postcss@8.5.23). **RESOLVED by BO-F-00.3 (2026-08-21):** `overrides: {"nanoid": "^3.3.18"}` in package.json + regenerated lockfile → `npm ls nanoid` = 3.3.18 (overridden); **`npm audit --audit-level=high` exit 0** (remediation gate restored); full frontend suite + tsc + production build green. The two react-router MODERATE advisories remain and are accepted per the B-00.2 exception policy (fix requires a react-router 7.18+ major migration — deferred to a dedicated future order; see OBS-F00-2). Patch: `f00.patch.txt` (chain position 30). | Medium | **CLOSED — resolved by BO-F-00.3; audit gate green** | — |
| TD-B00-ECDSA-EXCEPTION | **BO-B-00 §B-00.2 formally accepted exception (2026-08-19).** pip-audit 2.10.1: ecdsa 0.19.2 (transitive of python-jose[cryptography]) carries PYSEC-2026-1325 (Minerva timing attack on P-256; `sign_digest()`/keygen/ECDH), CVSS **7.4 HIGH** (AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N), **no fix published** (0.19.2 is current). Exposure analysis: AXIOM's JWT flow is HS256 (`settings.jwt_algorithm` default "HS256"; `app/auth/security.py` encodes/decodes with the configured secret — no ES* path), and ecdsa is never imported by application code: the vulnerable signing path is unreachable in this application. Exception accepted under the B-00.2 model (classified: transitive · no-fix · unreachable path; documented, not silently waived). No pin change applicable (no fix exists). Revisit when upstream publishes a fix. Evidence: `docs/evidence/uiconv/pipaudit_b00_r1.log`. | High (accepted exception) | **Open — formally accepted exception; revisit on upstream fix** | Future dependency-hygiene unit when a fix is published |
| TD-B00-PYTEST-EXCEPTION | **BO-B-00 §B-00.2 formally accepted exception (2026-08-19).** pip-audit 2.10.1: pytest 8.4.2 carries PYSEC-2026-1845 (`/tmp/pytest-of-{user}` tmpdir pattern; local DoS/privilege), CVSS **6.5 MEDIUM** (AV:L/AC:L/PR:N/UI:N/S:C/C:L/I:L/A:L), fixed in 9.0.3 — **outside the pin range** `pytest>=8.3.0,<9.0.0`. Classification: dev-only (test tooling) · local-attacker vector · fix outside pin range. BO §7 requires respecting the pin-range policy unless an advisory forces a targeted bump; a medium, local-only, dev-tool advisory does not force a major-version range change mid-unit. Exception accepted under the B-00.2 model. Adopting pytest 9.x is a future dependency-hygiene unit candidate (requires the full suite re-run). Evidence: `docs/evidence/uiconv/pipaudit_b00_r1.log`. | Medium | **Open — formally accepted exception; future hygiene-unit candidate** | Future dependency-hygiene unit |
| TD-B00-UNIT | **BO-B-00 delivered (2026-08-19).** Clock model (a) wall-clock bind in `backend/app/market/adapters/simulated.py`; 4 pinned invariant tests (`backend/tests/test_b00_simulated_clock.py`); Provenance & Landing Protocol binding (`backend/docs/PROVENANCE_LANDING_PROTOCOL.md`). Executed backend baseline: 480 passed / 0 failed / 0 skipped (71 files), reconciled against PROJECT_STATE.md (415/414 inventory — stale, discrepancy reported in DELIVERY_REPORT_B-00.md §6). Patch `b00.patch.txt` sha256 `5662ec3d1dc7dcc0087a27a45e7f1993333413b6a1686fc20d3abffe616f55bd`, chain position 19, apply-check exit 0 in pristine clone, clone-side tests 4/4. **ITRGA final determination 2026-08-19: APPROVED WITH OBSERVATIONS** (OBS-B00-1 delivery process · OBS-B00-2 evidence-model wording · OBS-B00-3 PROJECT_STATE staleness — all non-blocking). **B-00 CLOSED.** | — | **CLOSED — APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-00_FINAL.md)** | — |
| TD-B01-UNIT | **BO-B-01 delivered (2026-08-19).** Data Foundation executed: labeled `synthetic` corpus (5 series × 2,000 H1 bars) ingested through the governed API seam with per-series `market_series_metadata` (source_authority=synthetic); validation-tier separation formalized as binding (`backend/docs/VALIDATION_TIER_SEPARATION.md`) and enforced in code (tier-aware snapshot freeze + feature computation, shared `authority_from_source` classifier with additive `synthetic`/`historical:real` labels); 5 pipeline-validation snapshots frozen with content hashes; 5 temporal split manifests persisted (new `app/ml/dataset/split_store.py`); 3 builtin feature definitions registered; 10,000 feature records + 5 quality reports (identity-fields-excluded). Live negative control: research-tier freeze over the synthetic corpus quarantined (200 rows, SYNTHETIC_SOURCE_NOT_AUTHORITATIVE). Full backend suite: **489 passed** (480 + 9 new). Patch `b01.patch.txt` sha256 `a404953193b257b2dee90dfe327131e55130d0f012ea38263e0bc3e6e20e3d45`, chain position 20, apply-check exit 0 in pristine clone, clone-side tests 9/9. Deviations D1–D5 disclosed. **ITRGA determination 2026-08-19: APPROVED WITH OBSERVATIONS** (OBS-B01-1 transmission completeness — standing corrective CA-B01-1; OBS-B01-2 fixture-label convention; OBS-B01-3 v1 features only — all non-blocking). **B-01 CLOSED.** | — | **CLOSED — APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-01.md)** | — |
| TD-B02-UNIT | **BO-B-02 delivered (2026-08-19).** Governed ML lifecycle executed at pipeline_validation tier over the B-01 synthetic corpus: experiment `exp-b02-eurusd-baseline` (pre-registered + approved), majority-class baseline trained with candle-derived no-look-ahead labels (97 walk-forward folds; accuracy 0.5134, effect +0.0134, p=0.239), calibration over labeled degenerate priors (ECE 0.0, Brier 0.2499), economic validation under the full six-class cost model (economically_unusable, net −11,524 bps), generalization hold-out (crypto vs forex, aggregate 0.5), and an **honest NOT-eligible evaluation** — `STATISTICAL_THRESHOLD_NOT_MET`, `CALIBRATION_THRESHOLD_NOT_MET`, `ECONOMIC_NOT_USABLE`; promotion refused with the same reasons. Substantive-threshold framework installed as binding (`backend/docs/SUBSTANTIVE_THRESHOLD_FRAMEWORK.md`) and enforced in `GovernedModelEligibilityGate` (4 new rejection reasons, fail-closed on malformed values). Full backend suite: **500 passed** (489 + 11 new). Patch `b02.patch.txt` sha256 `d5e4a3c876c0bff826cc05f3e9dd58a3f9382607df3bc5a93acbf253a035b1fe`, chain position 21, apply-check exit 0 in pristine clone, clone-side tests 30/30. Deviations D1–D6 disclosed (spec-driven churn of the W3 gate fixture + 3 signal-guardrail tests to the superseding threshold semantics; warm-up row exclusion; SQLite-naive coercion; untracked runner; no migration). Standing dependency (§8): the substantive path requires real historical data — **Operator decision R1/R2 pending; B-03 signal production with an eligible model is blocked until then.** **ITRGA determination 2026-08-19: APPROVED WITH OBSERVATIONS** (OBS-B02-1 signal-layer semantic tightening record · OBS-B02-2 PROJECT_STATE staleness · OBS-B02-3 fixture labels — all non-blocking). **B-02 CLOSED.** | — | **CLOSED — APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-02.md)** | — |
| TD-BDATA-UNIT | **BO-B-DATA (Operator R1 decision) delivered (2026-08-20).** **Part A (prerequisite, landed first):** `authority_from_source` grants AUTHORITATIVE only to `historical:real`; `sample:*`/`csv:*`/`test` → UNKNOWN (no new bypass class); sample-ingestion default pinned non-authoritative; 3 new tests (incl. the unlabeled-ingest quarantine) + 3 W2-U01 churn sites disclosed; fail-first probe showed the exact hazard (unlabeled snapshot FROZE under the legacy convention — now quarantined with UNKNOWN_SOURCE_AUTHORITY). **Part B (payload):** real historical corpus retrieved 2026-08-20 from two keyless public endpoints — OKX history-candles (6 instruments × 17,520 H1 bars, complete 2024-08-01→2026-07-31 window) + Kraken public OHLC daily (6 instruments × 701 D1 bars) = **109,326 real bars**, pinned via per-file SHA-256 manifest (`MANIFEST.json` sha256 `2a2c0921…`); Binance probed and **rejected** (terms-eligibility refusal treated as dispositive per BO §3); license declaration written (`backend/docs/B-DATA_SOURCE_LICENSE_DECLARATION.md`) with bounded internal-research permitted-use statements and the Operator-legal-responsibility boundary restated. Ingested via the governed API seam with explicit `historical:real` labels: 12/12 metadata rows AUTHORITATIVE, 0 invalid rows. **ChronologyGuard at authoritative tier: research_validation snapshot over 17,520 real BTCUSDT bars FROZEN with 0 quarantine rows; synthetic negative control still quarantined.** Full backend suite: **503 passed** (500 + 3 new). Patch `bdata.patch.txt` sha256 `72ca93eef292ccaa083099f189c9f22ab86b06716f4d409681d1ab8f386ff139`, chain position 22, apply-check exit 0 in pristine clone, clone-side Part-A set 34/34. Deviations D1–D6 disclosed (corpus transmitted-not-patched; source substitution OKX/Kraken; Kraken H1 depth limit; untracked tooling; no migration; churn). **The R1 unblock is real: the substantive ML unit (real model + governed promotion) and B-03 signals are now unblocked.** **ITRGA determination 2026-08-20: APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-DATA_FINAL.md)** — prior CORRECTION REQUIRED reversed after the Operator disclosed the upload constraint and ITRGA independently reconstructed all 12 corpus files byte-identical from the live keyless endpoints and re-ran B5/B6 (real→frozen/0-q; synthetic→quarantined). OBS-BDATA-1 (relay-accurate manifest rule, standing) · OBS-BDATA-2 (crypto-only asset breadth, info). **B-DATA CLOSED.** | — | **CLOSED — APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-DATA_FINAL.md)** | — |
| TD-BML-UNIT | **BO-B-ML delivered (2026-08-20) — the substantive ML unit, HONEST NEGATIVE.** Real model implemented: pure-Python logistic regression (seeded SGD, zero new dependencies, deterministic/explainable/real probabilities — `backend/app/ml/models/logistic_regression.py` + ADR `B-ML_ALGORITHM_JUSTIFICATION.md`); validation service extended with an optional fold-model parameter (default unchanged); **split-engine reproducibility fix (D7): deterministic cross-series tie-break via optional `sort_key` — the uuid tie-break made split membership drift between runs. REPRODUCTION PROVEN THRICE: the research runner executed three times from fresh databases; every substantive metric line byte-identical across runs (only uuid-embedding fingerprints differ, per D7). A stale pre-fix economic figure in the first report version was self-detected via the third-run diff and corrected to the deterministic value everywhere.** Research-tier lifecycle executed over the REAL OKX H1 corpus: experiment `exp-bml-logistic-core` approved; train BTC+ETH+SOL (52,560 bars), hold-out XRP/ADA/DOGE; **347 walk-forward folds: accuracy 0.5028, effect +0.0028, p=0.322 → STATISTICAL_THRESHOLD_NOT_MET; ECE 0.0767 pass-leg, Brier 0.2558 → CALIBRATION_THRESHOLD_NOT_MET; net −163,559.5 bps → ECONOMIC_NOT_USABLE; hold-out aggregate 0.4938 < 0.50 floor → GENERALIZATION_THRESHOLD_NOT_MET. NOT PROMOTED — promotion attempted and refused with all four threshold reasons; artifact remains research_only.** Full backend suite: **509 passed** (503 + 6 new). Patch `bml.patch.txt` sha256 `7a9d532454ebcd04aefd303e519b10959a04c59fcef698584791353c2dadd173`, chain position 23, apply-check exit 0 in pristine clone, clone-side 27/27. Deviations D1–D7 disclosed (incl. the disclosed fingerprint limitation: content/metrics deterministic; snapshot/split/artifact hashes embed uuid ids and are run-instance-specific). **B-03 (signals) remains gated (no promoted model). Next step is the Operator's BO §12 decision (decision memo: `DECISION_MEMO_B-ML_S12.md`).** **Awaiting ITRGA determination.** | — | **Delivered (honest negative, thrice-reproduced) — awaiting ITRGA determination** | ITRGA determination (BO-B-ML §12 completion condition) |

### Carried observations

| ID | Status |
|----|--------|
| OBS-1 simulated chronology | **Transitioned to enforced contract** W2-U01: `seed:synthetic` and `live:simulated` excluded/quarantined from authoritative training datasets |
| OBS-2 / W0-CI-C4b green CI run | **Code gate implemented**; remote run evidence still desired |
| OBS-3 UTC output | **Closed in code/tests**; W2-U01 guard rejects naive trusted timestamps |
| OBS-P05-2 Vite chunk size | Carried, non-failing build advisory; future refinement/code-splitting candidate only |
| OBS-P05-5 evidence URL punctuation | **Closed** by final P06 review; URLs isolated in harness prompts and no recurrence accepted by ITRGA |
| OBS-P06-2 audit window | **MEDIUM / carried workstream residual.** The complete `*_REFUSED` population is three rows at audit ranks **374, 509, and 510 of 640**, all beyond the unchanged newest-50 Explorer window; every constitutional refusal is currently unreachable in the UI. Future server-side reason-code filtering, pagination, or date-range selection requires its own Build Order. |
| OBS-P06-3 visibility-runner count | **Open / evidence-harness observation.** The original diagnostic printed `SECURITY_ROW_COUNT: 1` although ITRGA parsed 200 raw records. Treat as an R7 hazard; a future runner correction requires target re-proof before closure. It is not a configuration or audit-integrity finding. |
| OBS-P06-4 evidence encoding | **Open / evidence-harness observation.** Reviewer-facing evidence artifacts were emitted as UTF-16 with BOM and required decoding. Prefer UTF-8 without BOM under a future authorized evidence-harness hardening scope. |
| OBS-SURF3-1-STATUS | **Workspace-state update for the sole open programme finding (2026-08-19).** The local closure-evidence PNG for OBS-SURF3-1 (`/home/user/data_p01_upload/SURF-P03_03_OPERATOR_ROLE_REAL_403_PARTIAL_DENIAL.png`, declared hash `f08ab63d…`) is **no longer present in the DA workspace**. Verified during the Operator-requested workspace cleanup: a full filesystem search found no copy; none of the cleanup commands executed this turn referenced that path or any matching glob (command-by-command audit available in the turn record). The workspace snapshot dropped the folder at an earlier turn boundary — it was not deleted by this turn's cleanup. OBS-SURF3-1 remains **OPEN and Operator/ITRGA-owned** (the finding itself: the PNG was declared but never transmitted; ITRGA records its hash). Historical delivery reports that stated the PNG's presence were true at their time of writing and are not rewritten. Nothing can be regenerated by the DA — the capture is Operator-side evidence. |
| OBS-CAPASSESS-H1 | **CLOSED by BO-F-00.3 (2026-08-21)** — ITRGA post-closure capability assessment (2026-08-19), matrix row 32: accessibility "one gap: no `<h1>` on `/`". The landing operations surface (`TradingTerminalWorkspace`, route `/`) now carries a visually-hidden `<h1>` ("AXIOM Institutional Trading Terminal — Operations", `.ix-sr-only`, testid `operations-landing-h1`) so the document outline is complete without changing terminal density. Test-pinned (`f00_design_system.test.tsx`) + Level-I capture (h1 present on `/`). |
| TD-B06-PLAN | **BO-B-06 design plan prepared by the DA (2026-08-20)** — `DESIGN_PLAN_B-06.md` (sha256 `43217311f09c744e0933127406f834038006db064c28c4426a21a94b9e00505a`): the governed assistant ask path design, grounded in verified code state (engine exists, tested, unexposed — FIND-3). Bounded POST `/collaboration/assistant-respond`, read-only grounding assembly over seven artifact families, single-row mutation boundary, per-class fail-first tests, no-external-calls proof, full acceptance/evidence/transmission structure. **Document class: DA design planning (the lifecycle's designated DA stage) — NOT a Build Order, NOT self-authorization; implementation begins only on an issued Build Order.** B-06 is not model-dependent (parallel-start per the roadmap), so it is the next-unblocked unit. | — | **Plan ready — awaiting Build Order** | BO-B-06 issuance |
| TD-BML-S12-MEMO | **BO-B-ML §12 decision memo prepared by the DA (2026-08-20)** — `DECISION_MEMO_B-ML_S12.md` (sha256 `4a45d0dc9c64f32181a8ead42dcec68c44f616bcda21d9ae6a7d8bb2fc9e98e2`; hash updated after the economic-figure correction to the deterministic value −163,559.5 bps): factual basis for the Operator's RETRY/ACCEPT decision over the honest negative. Core reading: the model is uniformly uninformed (in-domain 0.5028 vs hold-out 0.4938, both coin-flip-level, no overfit). RETRY ranking: feature breadth → asset classes (R1 machinery now mature) → D1 horizon; ACCEPT consequences: B-03 stays gated, B-06/B-07/F-units proceed. The decision is the Operator's. | — | **Memo delivered — awaiting Operator decision** | Operator §12 decision (RETRY or ACCEPT) |
| TD-BML2-UNIT | **BO-B-ML2 delivered (2026-08-20) — predictive iteration under multiple-comparison discipline, SECOND HONEST NEGATIVE.** v2 feature set implemented (8 justified causal market-agnostic features: atr_norm_14, roc_12, ema_dist_20, bband_pos_20, vol_change_1, hour_sin/cos, structure_bos_rec — 11 total in the primary set ≤ 12 bound); harness extended with per-hypothesis `label_overrides`/`feature_filter` (additive, defaults unchanged). **Three hypotheses pre-registered BEFORE training (plan hashes + timestamps in the log; 1 primary + 2 labeled exploratory secondaries; primary evaluated alone at α=0.05, secondaries uncorrected + labeled — disclosed per §1.4). ALL THREE REFUSED by the unchanged fail-closed gate: PRIMARY effect +0.00052, p=0.853, Brier 0.3099, net −163,579.5 bps, hold-out 0.4913 — the expanded set finds no directional structure; S1 ablation likewise negative; S2 (magnitude target, labeled secondary) showed a nominally significant p (4.55e-05) — volatility clustering is real — but effect +0.0331 below the +0.05 floor, ECE 0.444, Brier 0.4685, net −171,139.5 bps: the multiple-comparison trap the BO anticipated, refused by the substantive bars. ZERO promotions.** Full backend suite: **514 passed** (509 + 5 new). Patch `bml2.patch.txt` sha256 `1649e26f66bda3107c3e2f0d1e4687b9103b09f7835e46b7eff14cdf678cadb3`, chain position 24, apply-check exit 0 in pristine clone, clone-side 26/26. Deviations D1–D5 disclosed. **B-03 remains gated. Next step per BO §12: Operator decision (further reformulation / broader real asset classes via a new R1-style order / accept the predictive track's negative status).** **ITRGA determination 2026-08-20: APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-ML2_FINAL.md)** — pre-registration ordering verified (3 hashes precede training), multiple-comparison discipline genuinely exercised, 4/4 SHAs, 5/5 new + 514/514. OBS-BML2-1 (probe log transmission) **closed by the Operator Decision Record**. **B-ML2 CLOSED.** Per the Operator Decision Record (`OPERATOR_DECISION_RECORD_PREDICTIVE_DEFERRAL.md`): the predictive track's negative status is **accepted and DEFERRED** — resumption only after the project is operational with a full working UI. B-03 stays gated; deterministic market-structure analysis is the operational terminal's primary intelligence. | — | **CLOSED — APPROVED WITH OBSERVATIONS; predictive track deferred (Operator decision)** | — |
| TD-B04-UNIT | **BO-B-04 delivered (2026-08-20) — intelligence generation wired (the FIND-2 gap closed).** Five operator-authenticated POST endpoints added to the intelligence router over the five existing deterministic services — no service-math change, no new dependency, no schema migration; structured insufficient-data 422s; honest data-class labels; audit lineage; non-actuation pinned. **Level-I probe over the REAL corpus:** correlation r=0.7321 over 17,520 aligned bars; regime "calm" (0.605); scenario/portfolio-risk/signal-validation generated with `data-class: historical:real` labels. **Full suite 522 passed.** Patch `b04.patch.txt` sha256 `2704a8a2f264747f4f3ad539ce469f1404dc0f6362d5f7a854f2481ba98cbdc3`, chain position 25, clone-side 48/48. **ITRGA determination 2026-08-20: APPROVED WITH OBSERVATIONS** (OBS-B04-1 on-request-only generation; OBS-B04-2 signal-validation family awaits a promoted model — both non-blocking). **CA-TRANSMIT-1 HONORED — first delivery where the transmission discipline held.** **B-04 CLOSED.** | — | **CLOSED — APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-04.md)** | — |
| TD-B05-UNIT | **BO-B-05 delivered (2026-08-20) — alert emission wired (the B-05 "written but unwired" gap closed).** New `app/trading_intelligence/monitoring/emission.py` calls the four previously-uninvoked `create_*` alert methods from real, as-of-bounded conditions (`POST /alerts/check` governed bounded path; `POST /alerts/inference-health` explicit input). Dedup = one alert per (alert_type, service-`subject_id`) within an 86400s cooldown. **Level-I probe:** check #1 emitted 3/3 (11 symbols checked), check #2 all-zero, inference-health 200→409→422, ack read-state-only. **Full suite 530 passed.** Patch `b05.patch.txt` sha256 `33abf5e610de48e18ed7bcbe406b7b4f6ee04de0d6c4ba3f69c5788a16787e6d`, chain position 26, clone-side 22/22. **ITRGA determination 2026-08-20: APPROVED WITH OBSERVATIONS** (OBS-B05-1 on-request emission; D2 narrow guard exemption accepted — no guard weakened). **CA-TRANSMIT-1 honored (second consecutive). B-05 CLOSED.** | — | **CLOSED — APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-05.md)** | — |
| TD-B06-UNIT | **BO-B-06 delivered (2026-08-20) — the governed assistant ask path (FIND-3 closed: the responder is reachable).** `POST /collaboration/assistant-respond` (authenticated) resolves operator-selected artifact ids across 9 persisted families (5 B-04 intelligence reports + dataset snapshots + annotations/journal/trade-plans) into a real `GroundingBundle` and delegates ENTIRELY to the existing `RuleBasedGroundedAssistant` — no new reasoning logic, no responder/policy/registry change, no schema migration, no dependency. Unknown ids excluded; empty grounding → the responder's own GROUNDING_REQUIRED refusal. **Level-I probe over the real corpus:** grounded answer over a real correlation report (`r=0.7321 n=17520`) with source ids + full disclaimer + audit correlation; all four constitutional refusal classes persisted; two grounding refusals; read-back through the unchanged GET surface; unauth 401. **Full backend suite: 537 passed** (530 + 7 new). Patch `b06.patch.txt` sha256 `ce0fedff1c19e360e932b1f5d261da18a24b936cc822c2d3928a2656adeea7a7`, chain position 27, apply-check exit 0 in pristine clone, clone-side 31/31. Deviations D1–D4 disclosed (9 grounding families; BO's operator-selected contract implemented verbatim over the DA's earlier auto-context sketch; persisted record returned, not the transient object). **F-01 (assistant input surface) is now unblocked.** **ITRGA determination 2026-08-20: APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-06.md)** — CA-TRANSMIT-1 honored (third consecutive); FIND-3 closed; the consolidated 27-element chain transcript noted as an institutional-integrity milestone; OBS-B06-1 (rule-based responder; richer summarization/external-LLM are future separately-governed work). **B-06 CLOSED.** | — | **CLOSED — APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_B-06.md)** | — |
| TD-B07-PLAN | **Superseded by the issued Build Order.** The DA prepared an unprompted design plan (`DESIGN_PLAN_B-07.md`) when the Operator had not requested one — the Operator corrected the DA ("i didn't request a design plan"), and BO-B-07 was then issued. The plan file is retained as a historical DA artifact; the BO is the governing scope, and its differences (e.g. no RBAC/index items — those were the plan's candidates, not the BO's) are immaterial to the delivery. **Discipline note: the DA produces design artifacts only when the governed lifecycle or an explicit instruction requests them.** | — | **Superseded by BO-B-07 (issued)** | — |
| TD-B07-UNIT | **BO-B-07 CLOSED (2026-08-20) — the final backend unit: cross-cutting hardening.** Rate limiting implemented (in-memory sliding window, operator-keyed via the token username claim, per-route + global ceilings, admin allowlist, fail-safe deny on misconfiguration, both API mounts guarded — closes TD-W7-U07-RATE-GUARD; disposition → `implemented`); security headers (nosniff/frame-DENY/referrer/HSTS-configurable/restrictive CSP, wrapping the 429 path too); observability deepened (per-pipeline counters + correlation-id log lines for the B-04/B-05/B-06 pipelines; `/metrics` gains `pipelines` + `resources`); Doc 11 evidence inventory (8 categories, honest gaps, no certification claim). **Level-I probe:** headers present; ceiling=4 → 422×4 then 429×3; reads unlimited; pipeline counters populate; rusage resources present. **Full backend suite: 546 passed** (537 + 9 new). Patch `b07.patch.txt` sha256 `cfe3b9278f4ae4f352a3463b4db1c57dbc8cccb3dc270dde7e409139d695feba`, chain position 28, apply-check exit 0 in pristine clone, clone-side 33/33. Deviations D1–D6 disclosed (one spec-driven disposition churn; middleware shadowing fix; dual-mount guarding; probe-time taxonomy correction; non-HTTP disposition; no migration/dependency). **Residual deferral: per-process rate-limit state (multi-instance needs a shared store — future unit).** **ITRGA DETERMINATION (2026-08-20): APPROVED WITH OBSERVATIONS** — all acceptance criteria met; CA-TRANSMIT-1 honored (4th consecutive); 8/8 post-apply SHAs; 9/9 new + 546/546 suite; fail-safe limiter, headers, observability, honest non-certifying Doc 11 all verified. Observation carried: residual per-process rate-limit deferral (recorded, non-blocking). **CLOSED — the backend operationalization roadmap (B-00 → B-07) is COMPLETE.** | — | **CLOSED — APPROVED WITH OBSERVATIONS** | Residual per-process rate-limit deferral (OBS-B07-1, non-blocking, future unit) |
| TD-X01-UNIT | **BO-X-01 delivered (2026-08-21) — End-to-End Platform Verification, Backend Tier.** A verification order — **no code changes introduced** (the only repo-file delta is the three governance/documentary files in this patch). The eight-hop governed operator workflow was executed over the real corpus (109,326 bars, all 12 files sha256-verified against the B-DATA MANIFEST) with per-hop Level-I/II evidence: (1) market data — 12 API ingestions, 12 AUTHORITATIVE `market_series_metadata` rows, runs/stats non-zero; (2) chart/structural — SWINGS55/STRUCT55/BOS55/CHOCH55/FVG3/SMA20 computed server-side over BTCUSDT H1, byte-identical across two runs, unknown ids 422, zero ML imports in the computation modules; (3) research artifacts — frozen dataset snapshot (tier `research_validation`, authoritative-only, content_hash `c379f0e9…`), temporal split manifest (63,072/21,024/21,024, split_hash `30355f4f…`), 105,120 feature records; (4) intelligence — all five B-04 families POST 201 + GET read-back with uncertainty/notes-data-class/economic_usefulness fields; honest 422 for an absent series; (5) alerts — stale+drift+withheld emission, cooldown dedup (second check all-zero), inference-health degraded 200 → repeat 409 → status=ok 422, ack mutates ONLY {acknowledged, acknowledged_at, acknowledged_by}; (6) assistant — grounded ask over the real-corpus correlation report; ORDER/GATE_OPEN/SECRET_EXFILTRATION/UNBOUNDED_TOOL refusals; empty-grounding and unknown-id GROUNDING_REQUIRED; `external_llm_used: False` on every persisted row; unauthenticated → 401; (7) governance — audit events with correlation ids (18 of 38), `/api/v1/metrics` pipeline counters + rusage, route-inventory `actuation_surface_present: false`, RBAC `forbidden_capabilities_present: false`; (8) non-actuation — table-delta census over the hop window: 17 of 43 tables changed, all inert/research (audit, reports, features, snapshots, alerts, responses); every actuation-adjacent table zero delta; static scan: zero outbound network-client imports in the exercised modules (and in `app/` entirely). Full backend suite re-executed: **546 passed, exit 0**. Evidence: `docs/evidence/x01/*.log`; runner `scripts/x01_execute.py` + `scripts/x01_supplement.py`. Two probe-execution defects (wrong `/metrics` path in the runner; auth header accidentally attached to the "unauthenticated" ask) were corrected in `x01_supplement.log` — platform behavior was correct in both cases. **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-X-01 §11 completion condition) |
| TD-X01-CHAIN-PROOF | **Chain proof r3 (2026-08-21):** pristine clone at `34f4c62` → all **28 prior patch elements applied in order** (exit 0) → **`x01.patch.txt` applied** (docs-only: register, DOC11, DOC12 — zero code files) → `git apply --check` exit 0 → post-apply `cmp` per file. Transcript: `x01_applycheck_transcript.txt` (sha256 declared in the X-01 delivery report). | — | **Chain proven green from zero (29 elements)** | — |
| TD-F00-UNIT | **BO-F-00 delivered (2026-08-21) — the first frontend unit: design-system foundation, six-theme system, animated wave login, hygiene.** F-00.1: six themes (Midnight default / Light / Slate / Teal / Amber / High-Contrast) as Tier-5 token overrides in `tokens.css`, typed registry in `design/theme.ts` (RGB-triple form — the no-hex-literals rule holds mechanically), header cycle switcher + command action, per-operator persistence via the existing workspace-preferences path (theme_config.mode normalized; legacy "dark" → midnight), light-theme focus-ring contrast fix (spec-driven). F-00.2: wave login — two perspective candle rows (16 candles), 12-particle drift field, glow pulse, CSS-only transform/opacity keyframes, `prefers-reduced-motion` freeze covering every animated layer, governance chips preserved, zero market-data strings (scene textContent empty, test-pinned). F-00.3: favicon wired to the shipped brand asset (`/branding/axiom-logo.png`, 200, test-pinned); landing `<h1>` added (OBS-CAPASSESS-H1 closed); router future-flags **Option A** (both flags opted-in in `main.tsx`); supply chain — nanoid override 3.3.18 (HIGH cleared, audit-gate exit 0; TD-UI-CAPASSESS-NANOID-HIGH closed), react-router 2× MODERATE accepted exceptions (OBS-F00-2). **Tests: 29 new (`f00_design_system.test.tsx`) — registry completeness, CSS↔registry hex pinning, WCAG AA/AAA contrast per theme, preference round-trip + legacy normalization, switcher cycle, wave composition/no-data/freeze/transform-only pins, favicon/router/h1 pins; 2 spec-driven test churns ("dark"→"midnight", BO-F-00 supersession comments). Full frontend suite: 889 passed / 176 files (860 floor + 29); tsc -b clean; production build green. Level-I captures:** motion frames differ (wave animates), reduced-motion frames byte-identical (frozen), six theme screenshots with computed `--ix-bg-root` sampled (#0b0e14/#f8fafc/#1f2733/#0c1520/#14120c/#000000 — matches the registry), persistence probe: slate survives reload AND full sign-out/sign-in (server-side `PUT /workspace-preferences` observed in the backend log). Patch `f00.patch.txt` (chain position 30, frontend-only, zero backend files). **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-F-00 §12 completion condition) |
| OBS-F00-1 | **Open — backend observation surfaced during the F-00 capture probe (out of F-00 scope; carried for Operator/ITRGA routing).** Under the terminal's concurrent dev-load (many parallel requests on mount), two `audit append failed category=SECURITY action=auth.ws_ticket_issued err=(sqlite3.OperationalError) no such savepoint: sa_savepoint_1` lines appeared in the backend log while the endpoints still returned 200. Class: audit-append failure under concurrent sqlite sessions — the audit row is lost but the request succeeds (non-fatal, visibility gap). Pre-existing; not touched by F-00 (frontend-only unit); recorded here per R-discipline ("report as a finding, not a silent fix") — candidate for a future backend hardening order. | Low | **Open — backend observation; carried for routing** | Operator/ITRGA routing (future backend order) |
| OBS-F00-2 | **Open — accepted supply-chain exceptions (B-00.2 severity/exception policy).** `npm audit` after F-00.3: nanoid HIGH resolved (3.3.18 override); remaining: react-router + react-router-dom MODERATE (GHSA-wrjc-x8rr-h8h6 CVE-2025-68470 bypass, fixed only in ≥7.18.0; GHSA-jjmj-jmhj-qwj2, range 6.30.2–6.30.4). Exception rationale: a react-router 6→7 major migration is a framework-level activity with its own risk surface — not an F-00 hygiene activity; exposure is nil in-product (no user-controlled navigation targets; internal registry paths only). The `--audit-level=high` gate is green (moderates allowed by policy). Deferred to a dedicated future order. | Low | **Open — accepted exceptions (policy-compliant)** | Dedicated future order for the RR7 migration |
| OBS-F00-3 | **Open — custody event recorded during F-00 verification (2026-08-21).** The remote repository history changed: the remote now serves a single commit `7e4d993` ("AXIOM TRADING TERMINAL") whose tree is the Operator-published snapshot of the delivered state — all platform code files byte-identical to the DA's chain-29 state; the remote's `TECHNICAL_DEBT_REGISTER.md` is an OLDER version missing rows the DA delivered (e.g. TD-BML-UNIT, TD-BML2-UNIT, the SURF/CHART evidence-fixture rows); the programme delivery reports are tracked files in the snapshot. The declared baseline object `34f4c62` no longer exists on the remote. **The DA baseline declaration is UNCHANGED** (34f4c62 + the 30-element chain); F-00 verification was performed against the local object database, which retains the baseline byte-identical. No commits/pushes by the DA (standing Operator instruction). Operator may wish to reconcile the remote's register copy with the delivered register. | Info | **Open — remote history rewritten (Operator custody action); DA base unchanged** | Operator decision on remote reconciliation |
| TD-BSERIES-FULLSTACK | **Full-stack test record (2026-08-20):** after all ten B-series units (B-00→B-06 + B-DATA + B-ML + B-ML2, backend-only changes), the FRONTEND suite was re-run fresh: **860 passed / 175 files, 168.90s** — the floor maintained. Combined record: **537 backend + 860 frontend = 1,397 tests green**. Log: `docs/evidence/uiconv/vitest_b06_fullstack.log` (sha256 `62f8a97778e81af2734f0c6563b7a60d1cd836f3e6f839b2c2ecba1503fd71ec`, also in `b06_transmission/`). | — | **Full-stack green (1,397 tests)** | — |
| TD-BSERIES-CHAIN-PROOF | **Consolidated full-chain verification r2 (2026-08-20):** pristine clone at `34f4c62` → all **27 patch elements applied in order with per-element sha256** (exit 0) → **full backend suite executed INSIDE the applied clone: 537 passed, 157.53s, exit 0**. The entire artifact chain is green from zero at the post-B-06 state. Transcript: `chain_verification_r2_transcript.txt` (sha256 `44c73c58ee8047f42d45dd2d7570f6ec78bc2d1945c9b219f20d7217dbddc111`, also in `b06_transmission/`). Status sheet r2: `PROGRAMME_STATUS_B_SERIES.md` (sha256 `a6f90fa5e0d406d01e288d0afc1b441dd09cfa2472f9fe56c3703766644893a1`). | — | **Chain proven green from zero (27 elements, 537 tests)** | — |
| OBS-BDATA-2 | **Open (Info) — asset-class breadth.** The real corpus is crypto-only (OKX/Kraken). The roadmap's multi-market scope (forex, indices, commodities) remains open; future R1-style orders may extend asset classes. Relevant to the Operator's BO-B-ML §12 retry decision. |
| OBS-B00-TRANSMISSION | **Open — fourth recurrence of the declared-not-transmitted delivery class** (after OBS-CONV3-9, OBS-SURF3-1, CA-CHART2-1), per ITRGA_DETERMINATION_B-00.md (2026-08-19): the DA's closing message declared the B-00 artifacts but they did not reach the review channel — ITRGA's custody held zero B-00 artifacts (its clone HEAD `60045b7`, 2 commits, is a state the DA has never seen; the DA's baseline is `34f4c62`). Determination: NOT PROVEN (completeness gap). Root cause class: the DA treated its closing declaration as transmission; the actual channel is the Operator, who must relay the artifacts. **Resolved by process, not blame:** the DA now (1) writes every transmission artifact into a dedicated workspace folder (`/home/user/b00_transmission/`, `/home/user/b01_transmission/`) as byte-identical `.txt` copies, cmp-verified; (2) lists every file path + hash in the closing message; (3) states which are newly transmitted vs already received. Operator instruction "no repo involved" confirms on the record that the detached-artifact evidence model governs (determination §5.2). |
| OBS-B00-1 | **ITRGA B-00 FINAL observation (Low, delivery process):** `pipaudit_b00_r1.log` was declared in the B-00 report but was not among the relayed files. **Disposition (B-01 delivery):** corrective applied — the pip-audit log is re-transmitted as part of the B-01 transmission set (`b01_transmission/pipaudit_b00_r1.log.txt`, sha `110e9deb…`), and every declared hash in the B-01 report was mechanically verified against transmitted files before submission (R3 compliance). |
| OBS-B00-2 | **ITRGA B-00 FINAL observation (Low, governance wording):** BO §9 "commit ref + diff" vs protocol R5 detached-artifact custody model were in tension. **Disposition:** RESOLVED — BO-B-01 §9 formally ratifies the detached-artifact model ("replaces BO-B-00 §9's 'commit ref + diff' wording"), so the DA is no longer in a contradictory position. |
| OBS-B00-3 | **ITRGA B-00 FINAL observation (Info, documentation debt):** PROJECT_STATE.md test inventory stale (415/414 vs executed 476→480→489→500). **Carried** — outside BO-B-01/B-02 allowed-file scopes; flagged for a future documentation-touching unit. |
| OBS-B01-1 | **ITRGA B-01 observation (Low, delivery process):** `b01_execute_r1.log` declared but not relayed; R3 violated twice in a row; CA-B01-1 binding corrective issued. **Disposition (B-02 delivery):** compliance — the Delivery Report carries a full transmission manifest (§12, CA-B01-1); every declared hash was mechanically verified against transmitted files (sha256sum -c exit 0) before submission; the previously-missing B-01 execute log is re-transmitted with the B-02 set (`b02_transmission/b01_execute_r1.log.txt`, sha `cd078b88…`). |
| OBS-B01-2 | **ITRGA B-01 observation (Info):** fixture-convention labels (`sample:`/`csv:`/`test` → AUTHORITATIVE) are a latent honesty risk. **Carried** — future unit should tighten; the B-02 corpus uses the explicit `synthetic` label only. |
| OBS-B01-3 | **ITRGA B-01 observation (Info):** features are the v1 builtin set only. **Carried** — feature breadth remains B-03+/future scope. |
| OBS-DATA-P01-WATCHLIST-CLIP | **Open — carried forward by ITRGA, non-blocking, recommended for POLISH-P01 triage** (`ITRGA_DETERMINATION_DATA-P01_FINAL.md`, 2026-08-18: "DATA-P01 APPROVED WITH OBSERVATIONS"; observation: "the margin is under one pixel, and a wider value would clip"). Measured product observation, deferred per ITRGA CA-DATA1-3 instruction ("Do not modify layout code in response to this finding"). The watchlist slot is a fixed 240px grid column (`--ix-terminal-watchlist-width`) and `.watchlist-rows-container` clips at 239px (`overflow-x: hidden`); the metrics block is right-aligned, so when the sparkline window mixes seed and live sources the long provenance tag (`SEED:SYNTHETIC + LIVE:SIMULATED`, 286px) widens the metrics block and the price column renders truncated mid-number (Rev C: USD/JPY rendered as "15" of "150.010", BTC as "41,9" — measured, matches ITRGA's reading exactly). With a single-source window (seed-only or live-only, 129px tag) the price and % change columns are fully inside the panel (Rev D instrument: ink rects x 119.6→197.6 vs clip x 287). No layout change without an authorizing Build Order. |
| OBS-DATA2-1 | **Open — simulator realism observation, raised by ITRGA in the DATA-P02 determination; explicitly NOT a DATA-P02 defect; Owner: Operator to route** (candidate for a future DATA phase or POLISH triage). The DATA-P01 forex walk uses `gauss(0.00004, 0.00015)` — a positive mean; drift = 0.00004n grows linearly while noise grows as 0.00015√n (drift-to-noise 10.1× at one day, 22.6× at five days; six days of seeding moves EUR/USD +0.3456 / +31%). Invisible in ≤100-bar windows; first exposed by DATA-P02's long-window renders. Nothing is mislabelled and provenance is honest — the aggregation is faithfully showing what the generator produced. ITRGA instruction carried as-is: **do not open the generators now** (R1 fenced them for good reason); consider a mean-reverting or zero-mean walk when they are next legitimately open. |
| OBS-CHART-1 | **Open — non-blocking, POLISH triage** (`ITRGA_DETERMINATION_CHART-P01.md`, 2026-08-18). Orphaned `frontend/src/pages/ChartPlaceholderPage.tsx` — reads "Reserved for TradingView Lightweight Charts integration (Wave 1+)" / "TradingView Workspace — Not Initialized"; that integration has since been built; the file is referenced nowhere (not by the registry, not by any component, not by a test). Dead code describing the absence of a feature that now exists. OBS-CONV2-4 established the precedent of eliminating orphaned unrouted page files. Recommended for removal in whichever phase touches the chart tree; no removal without an authorizing Build Order. |
| CA-CHART2-1 | **Resolved by re-transmission pending ITRGA confirmation** (`ITRGA_DETERMINATION_CHART-P02.md`, 2026-08-18 — RETURN FOR RE-SUBMISSION, transmission only): `chart_p02.patch.txt` (`084b03c3…`), `CHART-P02_CAPTURE_VERIFICATION.json.txt` (`7da21fad…`) and capture 08 (`9622a0a5…`) were declared in the report but not transmitted; captures 01–07 + the report arrived with all hashes matching. Third recurrence of the declared-not-transmitted pattern (after OBS-CONV3-9, OBS-SURF3-1) — a delivery-process defect, not a work defect. All three artifacts verified byte-for-byte in the workspace against the declared hashes; nothing regenerated per the determination's instruction. |
| OBS-DELIVERY-PROCESS | **Open — standing process rule adopted by the DA (2026-08-18), per ITRGA's recommendation in CA-CHART2-1:** before upload, verify mechanically that EVERY hash declared in a delivery report resolves to an attached file. Applied from the next delivery onward: the DA's closing summary will list every artifact with its hash AND its workspace path, and the Operator transmission checklist is the DA's declared list. Also: the DA will state in each closing summary exactly which artifacts were newly transmitted versus already received, so partial transmissions cannot recur silently. |
| W1 broker gate | **Closed/controlled**; W2-U01 does not use External Integration or open gate |

---

**End**

---

## BO-B-AUDIT appended rows (2026-08-21)

*Register convention change (OBS-F00-3 corrective, binding from this unit): the register ships inside every patch as a PURE-ADDITION hunk — appended rows only, zero context, zero edits to historical rows. The register is now append-oriented (the same discipline as the audit trail itself), so a patch's register hunk applies on ANY drifted register state. Superseding status is recorded as new rows below; historical rows are never rewritten.*

| TD-REGISTER-APPEND-ONLY-CONVENTION | **Standing rule (BO-B-AUDIT §9, 2026-08-21):** every patch that produces register rows ships them as pure-additive hunks appended at the end of the table; historical rows are immutable (append-oriented register). This makes the register hunk apply on any drifted recipient state — the corrective ITRGA demanded in OBS-F00-3 (register drift / patch-apply failure). Verified mechanically: the B-AUDIT register hunk applies cleanly onto both the chain-30 register AND the 34f4c62 baseline register. | Info | **Standing — append-only register discipline** | — |
| TD-B-AUDIT-UNIT | **BO-B-AUDIT delivered (2026-08-21) — audit-integrity fix: silent audit loss under concurrency eliminated.** **Repro (B-AUDIT.1, evidence `b_audit_prefix.log` + `scripts/b_audit_repro_investigation.py`):** the exact production error reproduced deterministically — two sessions on the shared StaticPool connection; the concurrent session's rollback lands between the audit INSERT and the savepoint RELEASE → `sqlite3.OperationalError: no such savepoint: sa_savepoint_1 [SQL: RELEASE SAVEPOINT sa_savepoint_1]`, append returns None, row genuinely lost (`audit_rows=0`). **Root cause (B-AUDIT.2, evidenced):** all SQLite configs use StaticPool (one shared DBAPI connection) + pysqlite legacy mode suppresses a second session's BEGIN → concurrent sessions' transactions MERGE; the W3-U08.1 serialization lock covers only `:memory:`; the audit append rode `begin_nested()` on the business session, so a concurrent COMMIT/ROLLBACK orphaned its savepoint; append swallowed the exception. **Fix (B-AUDIT.3, mechanism chosen + justified):** the audit writer is DECOUPLED — short-lived dedicated sessions (`add→flush→commit→same-connection read-back verification`), one retry, and a durable `AuditWriteFailureRecord` marker table (`audit_write_failure_records`, queryable, verified itself) for persistent failure; the marker path is additionally guarded so it too can never 500. The read-back verification also detects the silent merged-transaction loss mode where the commit "succeeds" as a no-op. **Consequence disclosed:** audit rows now commit independently of the business transaction (an audit row may persist for a business op that later rolls back — acceptable per §5.7 accountability; the pre-fix inverse loss, business rollback discarding audit rows, is also eliminated). Residual disclosed: the shared-connection transaction merging for BUSINESS writes remains (serialization for file-sqlite + the ws.py/live_service direct-factory sessions is a future unit — outside this BO's allowed files). **Tests: 6 new (`tests/test_b_audit_concurrency.py`) — deterministic concurrent-rollback repro (row-or-marker contract), retry-then-land, durable-marker fields/queryability, marker schema pin, endpoint no-500 under hard audit-storage failure (incl. raising marker path), decoupled-writer-survives-business-rollback. Fail-first: 6/6 failed pre-fix (`b_audit_prefix.log`); 6/6 pass post-fix. Full backend suite: 552 passed (546 + 6); clone-side (gold standard, pristine clone → 31 elements): 552 passed.** Patch `b_audit.patch` (chain position 31; register hunk = pure-addition). Deviations D1–D3 disclosed (model file + __init__ registration outside the §6 list — the §11 schema mechanism requires them; marker table schema addition; no session.py change). **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-B-AUDIT §12 completion condition) |
| TD-F00-UNIT-STATUS | **Status supersession (2026-08-21): BO-F-00 CLOSED — ITRGA APPROVED WITH OBSERVATIONS** (ITRGA_DETERMINATION_F-00.md). All acceptance criteria met; 29/29 new tests + full suite green in ITRGA custody (913/182 there vs 889/176 here — a test-file-count environment difference, not a work discrepancy); patch `50d0e655…` verified; six themes + wave login genuinely implemented; tsc clean; audit-high=0. Observations: OBS-F00-1 (backend audit loss — now fixed by BO-B-AUDIT), OBS-F00-2 (carried RR moderates), OBS-F00-3 (register drift + history rewrite — Operator-level; corrective in progress), OBS-F00-4 (hash transcription + test-count note — record correction below). | — | **CLOSED — APPROVED WITH OBSERVATIONS** | OBS-F00-1→B-AUDIT · OBS-F00-2 carried · OBS-F00-3 Operator-level · OBS-F00-4 record correction |
| OBS-F00-1-STATUS | **Status supersession (2026-08-21): the backend audit-loss finding is FIXED by BO-B-AUDIT** (see TD-B-AUDIT-UNIT). ITRGA's F-00 determination upgraded the finding to Medium and routed it into the hardening order; the fix is delivered and awaiting ITRGA determination. | — | **Fix delivered by BO-B-AUDIT — awaiting determination** | ITRGA determination |
| OBS-F00-3-STATUS | **Status supersession (2026-08-21): corrective in progress.** ITRGA's F-00 determination escalated this to the programme's top open item: the register's intermediate rows were described in delivery reports but never shipped in patches (the drift ITRGA designates OBS-X01-2), compounding into an F-00 register-hunk apply failure in ITRGA's custody; the remote history rewrite to `7e4d993` made the chain reproducible only from the DA's local object DB. **Corrective delivered with BO-B-AUDIT:** the register now ships inside every patch as a pure-addition hunk that applies on ANY drifted state (see TD-REGISTER-APPEND-ONLY-CONVENTION). Remaining at Operator level: restore an append-only, verifiable remote chain (custody decision). The DA baseline declaration (34f4c62 + chain) is unchanged; no commits/pushes by the DA (standing instruction). | — | **Corrective delivered (register-in-patch) — remote custody remains Operator-level** | Operator custody decision |
| OBS-F00-4 | **Open (Info, evidence-precision) — record correction per ITRGA_DETERMINATION_F-00.md §4.** The F-00 Delivery Report §9 applycheck-transcript hash and the relayed copies ITRGA received differed from the DA workspace's final MANIFEST-verified set (ITRGA's report copy showed `661d32b9…`, its MANIFEST `35834404…`; the DA's final set carries `d62bc0c9…` for the transcript, sha256sum-c verified against `MANIFEST.txt`). Same report-vs-manifest transcription class as OBS-BML-1; no evidence-integrity impact (ITRGA verified the file matches its MANIFEST entry). Likely cause: the Operator's relay delivered a mixed/intermediate folder state. **Corrective:** the transmission MANIFEST remains the single hash authority, mechanically verified at both ends; future reports generate the §9 table directly from the verified MANIFEST (no hand transcription). | Low | **Open — record correction; single-source rule re-pinned** | None required beyond this record |

**End**

---

## BO-F-01 appended rows (2026-08-21)

*Append-only convention honored (TD-REGISTER-APPEND-ONLY-CONVENTION): pure additions, historical rows untouched.*

| TD-B-AUDIT-UNIT-STATUS | **Status supersession (2026-08-21): BO-B-AUDIT CLOSED — ITRGA APPROVED WITH OBSERVATIONS** (ITRGA_DETERMINATION_B-AUDIT.md). CA-TRANSMIT-1 honored (7/7 hashes); patch `6ade497e…` verified; **the pure-addition register hunk applied cleanly onto ITRGA's OWN drifted register state — the OBS-F00-3 corrective mechanism verified in custody**; repro byte-identical; 6/6 new + 552/552 suite; endpoint-safety held. Residual (recorded, non-blocking): file-sqlite BUSINESS-write transaction merging + the ws.py/live_service direct-factory sessions remain (a future hardening unit should serialize file-sqlite business writes the way the :memory: harness already does). Carried at Operator level: the two RR-moderate exceptions (OBS-F00-2) and the remote-custody decision (OBS-F00-3 second half — recorded by the Operator; the BO-F-01 header cites the new-baseline custody decision as made). | — | **CLOSED — APPROVED WITH OBSERVATIONS** | Residual file-sqlite business-write serialization (future unit) |
| OBS-B-AUDIT-1 | **Open — residual named by ITRGA (BO-B-AUDIT determination §5, non-blocking):** serialize file-based SQLite BUSINESS writes the way the `:memory:` test harness already does, and bring the `ws.py`/`live_service.py` direct-factory sessions under the same discipline. The audit path no longer depends on the merged-transaction class (B-AUDIT), but business writes still do. Candidate for a future backend hardening order. | Medium | **Open — future hardening unit** | Operator/ITRGA routing |
| TD-F01-UNIT | **BO-F-01 delivered (2026-08-21) — the assistant ask surface (FIND-3's frontend half closed).** F-01.1: `askAssistant(prompt, groundingSourceIds)` → POST assistant-respond with Bearer JWT + the exact B-06 body contract; typed `ApiError` (401 flagged); GET functions untouched; `useAskAssistant` hook (submitting/error/401/response/reset). F-01.2: `AssistantAskComposer` in the ContextualAssistantPanel — accessible labelled input (placeholder, maxLength 1000, in-form Enter submission, submit disabled on empty), grounding picker over the 8 B-06 families with a client-side GET (correlation/regime/scenario/portfolio-risk/signal-validation reports + chart annotations + journal + trade plans; snapshots excluded — no client GET exists, disclosed), ≤10 cap with disabled overflow + removable selected chips; empty selection submits honestly (backend GROUNDING_REQUIRED); suggestion chips now pre-fill the input (existing onSelectPrompt contract preserved). F-01.3: grounded render (summary, source ids, response text, disclaimer, muted audit-correlation) vs classed refusal render (distinct border/badge); honest submitting skeleton / error banner / 401 notice — never fabricated. F-01.4: RESEARCH-ONLY framing + deterministic-local disclosure preserved; zero actuation vocabulary (test-pinned + capture scan). **Tests: 17 new (`f01_assistant_ask.test.tsx`) — client POST contract/auth/401/422, composer a11y, empty-prompt no-send, trimmed-prompt submit, grounded render fields, refusal distinct, 401, error, cap-10 selector, empty-grounding submit, no-actuation scan, chip pre-fill, panel disclaimer, hook, family registry. Full frontend suite: 906 passed (889 floor + 17); tsc -b clean; production build green. Level-I capture (real browser, real backend): grounded ask over the live correlation report (r=0.7321, sources + disclaimer + audit-correlation `8f0600f8…` rendered), ORDER_INSTRUCTION_REFUSED rendered distinctly, empty state + grounding picker (2 real artifacts) screenshotted; Enter-key submission proven in-browser; backend logged two POST assistant-respond 201s with `assistant:ask_responded` pipeline events — and zero audit-savepoint errors under the concurrent page load (B-AUDIT fix holding). Patch `f01.patch` (chain position 32, frontend-only, register rows in patch per the standing convention). Deviations D1–D4 disclosed (component stylesheet file; Enter-key unit test drives form submit because jsdom lacks implicit submission — keyboard proven in capture; 8-family picker without snapshots; new test file under `src/test/**` as allowed). **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-F-01 §12 completion condition) |

**End**

---

## BO-F-02 appended rows (2026-08-21)

*Append-only convention honored (TD-REGISTER-APPEND-ONLY-CONVENTION): pure additions, historical rows untouched.*

| TD-F01-UNIT-STATUS | **Status supersession (2026-08-21): BO-F-01 CLOSED — ITRGA APPROVED WITH OBSERVATIONS** (ITRGA_DETERMINATION_F-01.md). CA-TRANSMIT-1 honored (10/10 hashes); patch `a9623927…` verified; register-in-patch applied onto ITRGA's drifted register; 17/17 new tests + full suite green in custody (930/183 there vs 906/176 here — environment test-file-count difference, no work discrepancy); the ask path verified end-to-end (grounded r=0.7321 + classed refusal + Enter-key + zero-violation no-actuation scan). Observations: OBS-F01-1 (Info, test-count environment difference), OBS-F01-2 (Info, carried: dataset snapshots absent from the grounding picker — no client GET exists; a future client GET would complete family coverage). Carried: OBS-B-AUDIT residual (file-sqlite business-write serialization), the two RR-moderate exceptions. | — | **CLOSED — APPROVED WITH OBSERVATIONS** | OBS-F01-1 Info · OBS-F01-2 carried |
| TD-X01-UNIT-STATUS | **Status supersession (2026-08-21): BO-X-01 (Backend Tier) CLOSED — APPROVED WITH OBSERVATIONS** per the ITRGA BO-B-AUDIT determination predecessor line ("B-00 → B-07 + X-01 + F-00 (all APPROVED WITH OBSERVATIONS)") and the BO-F-01 header. The X-01 delivery is the eight-hop backend verification (patch `7f4c3459…`, chain position 29, docs-only); the deferred Terminal Tier remains sequenced after F-01 → F-06. This row ships now per the append-only convention (bookkeeping only — the approval is already on the record). | — | **CLOSED — APPROVED WITH OBSERVATIONS (recorded)** | Terminal Tier after F-units |
| TD-F02-UNIT | **BO-F-02 delivered (2026-08-21) — signal presentation: two families (Structural + Predictive), CA-RECON-2 §13.** F-02.1: the signal stream now carries an explicit family tab bar (STRUCTURAL | PREDICTIVE (ML), role=tablist, test-pinned separation) — the families are never merged; default is predictive (pre-F-02 behavior preserved). F-02.2: NEW `StructuralSignalStream` — derives discrete structure events from the server-computed SMC/ICT indicator series (BOS55/CHOCH55/FVG3/STRUCT55/SWINGS55) as a PURE TRANSCRIPTION of non-null series markers (unknown lines never invented; FVG top/bottom edges pair by shared timestamp; all-null/unavailable → zero events; capped at 24 newest-first). Each event: type/direction/time/symbol/timeframe/level with descriptive labels ("Break of structure upward" — never "price will rise"); the surface carries "DETERMINISTIC · DESCRIPTIVE — NOT A PREDICTION" framing + series provenance line (symbol/timeframe/series kind); timeframe selector (M1–D1); honest empty ("no structure events in the current window") and honest unavailable states. F-02.3: predictive family labeled "PREDICTIVE (ML)"; the deferred-empty state now states the deferral: "No predictive signals — the predictive track is deferred (no eligible model). Deterministic structural signals remain available…". Existing behavior preserved (state filters, verbatim signal_state, canonical CalibratedConfidenceBadge, rationale/eligibility, RESEARCH-ONLY · NON-ACTUATING). F-02.4: zero actuation vocabulary (test-pinned + capture scan); no fabricated entries (derivation tests). **Tests: 14 new (`f02_signal_families.test.tsx`) — transcription correctness (typed events/direction/level), FVG pairing + single-edge, no-fabrication (all-null/unavailable/unknown-line), sort+cap, SMC set pin, family tabs + default + label, structural switch, deferral-honest empty text, structural render/honest-empty/unavailable, no-actuation scan. Full frontend suite: 920 passed (906 floor + 14); tsc -b clean; production build green. Level-I capture (real browser, real backend): predictive deferred-empty captured; structural 24 EVENTS over the REAL corpus (BTCUSD H1 native series, seeded under the terminal's symbol key — capture fixture disclosed), first event "Fair value gap zone" 2026-07-31 23:00 UTC; honest empty states captured (seed-only EURUSD H1 and D1). Patch `f02.patch` (chain position 33, frontend-only, register rows in patch). Deviations D1–D3 disclosed. **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-F-02 §12 completion condition) |

**End**

---

## BO-F-03 appended rows (2026-08-21)

*Append-only convention honored (TD-REGISTER-APPEND-ONLY-CONVENTION): pure additions, historical rows untouched.*

| TD-F02-UNIT-STATUS | **Status supersession (2026-08-21): BO-F-02 CLOSED — ITRGA APPROVED WITH OBSERVATIONS** (ITRGA_DETERMINATION_F-02.md). CA-TRANSMIT-1 honored (10/10 hashes); patch `7f6a0931…` verified; 14/14 new + full suite green in custody (944/184 there vs 920/176 here — environment test-file-count difference); the CA-RECON-2 two-family separation verified (pure-transcription derivation, 24 real-data structural events, honest empties, zero-violation no-actuation scan). Observations: OBS-F02-1 (Info — the `BTCUSD` capture fixture re-ingest; a future unit could unify corpus/terminal symbol keys to avoid the workaround), OBS-F02-2 (Info, test-count environment difference). Carried: RR-moderate advisories; file-sqlite business-write serialization. | — | **CLOSED — APPROVED WITH OBSERVATIONS** | OBS-F02-1 (symbol-key unification candidate) · OBS-F02-2 Info |
| TD-F03-UNIT | **BO-F-03 delivered (2026-08-21) — intelligence presentation: all five B-04 families + governed generation.** F-03.1: five governed generation client functions (`createCorrelationReport`, `createRegimeReport`, `createScenarioReport`, `createPortfolioRiskReport`, `createSignalValidationReport`) — Bearer JWT, exact B-04 request schemas, structured 4xx mapping: the B-04 422 `{error_code, detail, insufficient_data}` becomes `IntelligenceGenerationError` (status/errorCode/insufficientData). Named `create*` deliberately (not `generate*`): the UI-006-P06 static pin forbids `generatesignal`/`generatescenario` tokens in frontend source — client-side generation would bypass the gated backend; the pin is UNCHANGED and still holds. F-03.2: all five families render — correlation (r + Fisher CI + n), regime (label + confidence band + n), scenario (name + hypothetical return + assumptions + n), portfolio-risk (max drawdown + realized vol + stress loss + n), signal-validation (Wilson metrics + outcome-data status); each card carries uncertainty where present, sample count, limitations, the server-appended `data-class` label (parsed from `notes`), lineage (report-hash prefix), and the research-only framing ("not a signal, not causation, not a prediction, not financial advice"). F-03.3: the governed generation surface — one Generate trigger per family; the window is derived from the FRESHEST PERSISTED CANDLE of the active series (30-day span; never fabricated — no series → triggers disabled with an honest no-window notice); on-request only; success persists + re-reads the family; structured insufficient-data renders as an honest notice ("Insufficient data: {code} — nothing fabricated"). F-03.4: zero actuation vocabulary (test + capture scan); server-values-only rendering. **Tests: 13 new (`f03_intelligence_generation.test.tsx`) — per-function endpoint/schema/auth contract, structured-422 → typed error, five-tab presence, scenario/portfolio-risk/correlation card fields (incl. data-class + framing), generation trigger → persisted id + re-read, insufficient-data notice, no-series disable, no-actuation scan, verbatim metric rendering. Full frontend suite: 933 passed (920 floor + 13); tsc -b clean; production build green. Level-I capture (real browser, real backend): generation surface with the resolved real window (BTCUSD H1); UI-driven SCENARIO (persisted `be653ac8…`, data-class: historical:real), PORTFOLIO-RISK (drawdown −6.20%, vol 0.35%, stress −1.01%, data-class real), CORRELATION (BTCUSD⇄ETHUSD r=0.877 CI[0.86,0.89] over the 30-day window, data-class real); SIGNAL-VALIDATION → honest structured 422 `SIGNAL_VALIDATION_EMPTY_SCOPE` notice; backend log shows 3× POST 201 + pipeline events + the 422. Capture fixture disclosed (same as F-02: real bars under the terminal's BTCUSD/ETHUSD keys). Patch `f03.patch` (chain position 34, frontend-only, register rows in patch). Deviations D1–D4 disclosed. **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-F-03 §12 completion condition) |

**End**

---

## BO-F-04 appended rows (2026-08-22)

*Append-only convention honored (TD-REGISTER-APPEND-ONLY-CONVENTION): pure additions, historical rows untouched.*

| TD-F03-UNIT-STATUS | **Status supersession (2026-08-22): BO-F-03 CLOSED — ITRGA APPROVED WITH OBSERVATIONS** (ITRGA_DETERMINATION_F-03.md). CA-TRANSMIT-1 honored (11/11 hashes); patch `cc7554df…` verified; 13/13 new tests; the five generation functions + typed insufficient-data handling verified in code; register-in-patch honored. **OBS-F03-1:** ITRGA's full suite ran 956/957 — one failure, `test_surf_p02_dock_alerts_deep_link_activates_the_alerts_tab`, a pre-existing SURF-P02 deep-link test unrelated to F-03's files, failing in isolation in ITRGA's custody; ITRGA attributes it to the baseline divergence (its clone rests on `60045b7`, 185 files/957 tests, vs the DA's declared `34f4c62` chain, 176/933 at delivery). The DA's chain-34 environment runs the same test GREEN (verified pre-F-04). **OBS-F03-2 (Operator-level):** re-baseline ITRGA's clone to the Operator-published `7e4d993` so full-suite counts are apples-to-apples; until then ITRGA verifies per-unit tests + affected surfaces. | — | **CLOSED — APPROVED WITH OBSERVATIONS** | OBS-F03-1 (custody env divergence) · OBS-F03-2 (re-baseline ITRGA clone — Operator-level) |
| OBS-B-AUDIT-1-STATUS | **Status supersession (2026-08-22): the residual materialized during the F-04 capture — and the B-AUDIT mechanism held exactly as designed.** Under the capture's concurrent dev load (11 parallel candle-series requests + WS traffic), one `auth.ws_ticket_issued` audit append could not be verified in 2 attempts and the durable marker could not persist either → the structured-log final record fired (`audit append failed AND failure marker could not persist … structured log is the final record`); the ws-ticket endpoint still returned **200** (no-500 invariant held); the loss was NON-SILENT. This is the file-sqlite business-write transaction-merging residual ITRGA recorded as OBS-B-AUDIT-1 — the future unit remains: serialize file-based SQLite business writes (and bring ws.py/live_service direct-factory sessions under the discipline) the way the `:memory:` harness already does. | Medium | **Open — residual observed in the wild; mechanism held; future unit still recommended** | Operator/ITRGA routing (future backend hardening order) |
| TD-F04-UNIT | **BO-F-04 delivered (2026-08-22) — alerts center completion: five-domain filtering + timestamp/lineage surfacing.** F-04.1: `alertDomain(alert)` — the documented DATA-ORIGIN-DERIVED mapping over persisted fields: `signal_id`/`advisory_signal` → signal; `model_artifact_id`/`model_artifact` → risk; `market_series`/`market_class` → market; `system_component` → system; unknown/null-fielded subjects → the documented neutral "research" bucket (never invented). A role=tablist filter bar (ALL + five domains) filters the fetched list client-side with an accurate `filtered/total` count; per-card domain chips; honest per-domain empty notice ("No alerts in the X domain.") — the global empty state stays distinct. F-04.2: every card now carries its **absolute-UTC** `Created:` timestamp (`formatAlertTimestamp` — never a relative age; invalid input falls back to the raw string) and a `Lineage:` line (source where present, honest "none" otherwise); the detail record gains a Domain row alongside the full existing field set. F-04.3: the read-state-only ack discipline is untouched (provider-owned M2 — state renders only after the API confirms; Acknowledge label, disabled-while-acknowledging, acknowledged-alerts-stay-visible all preserved; existing pins green). F-04.4: honest empty/error states preserved; no banned remediation vocabulary in any control (button-label scans, test + capture). **Tests: 13 new (`f04_alerts_domains.test.tsx`) — mapping per family + field-precedence + neutral fallback, filter tabs + per-domain filtering + counts + domain-empty, domain chips, absolute-UTC formatting + card timestamp/lineage rendering + "none" fallback, detail-domain row, ack discipline (unacked-only, disabled while acknowledging), no-actuation vocabulary. Full frontend suite: 946 passed (933 floor + 13); tsc -b clean; production build green. Level-I capture (real browser, real backend): all four concrete domains emitted through the REAL B-05 paths (stale candle / drift record / withheld signal / inference-health) and rendered with correct derived domains, absolute timestamps, and lineage sources; MARKET and RISK filters isolate their domains (1/4 each); RESEARCH renders the honest empty notice; ack flips one card from 4 unacked to 3 + one "Ack: yes" with the backend POST 200 in the log. The capture's concurrent load also fired the B-AUDIT defensive path once (see OBS-B-AUDIT-1-STATUS) — disclosed, non-blocking, frontend-unrelated. Patch `f04.patch` (chain position 35, frontend-only, register rows in patch). Deviations D1–D2 disclosed. **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-F-04 §12 completion condition) |

**End**

---

## BO-F-05 appended rows (2026-08-22)

*Append-only convention honored (TD-REGISTER-APPEND-ONLY-CONVENTION): pure additions, historical rows untouched.*

| TD-F04-UNIT-STATUS | **Status supersession (2026-08-22): BO-F-04 CLOSED — ITRGA APPROVED WITH OBSERVATIONS** (ITRGA_DETERMINATION_F-04.md). CA-TRANSMIT-1 honored (11/11 hashes); patch `920f8acf…` verified; the D3 churn verified as a genuine race fix — **ITRGA's clone now runs the full suite green (970/186, 0 failures) and the OBS-F03-1 substance is closed** (the "baseline divergence" was the pre-existing timing flake, correctly diagnosed and fixed by the DA); the re-baseline recommendation (OBS-F03-2) becomes optional. OBS-F04-1 (Info): the B-AUDIT defensive path fired once during the F-04 capture, non-silently, endpoint 200 — the residual materializing as designed; the future serialization unit stays recommended. | — | **CLOSED — APPROVED WITH OBSERVATIONS** | OBS-F03-1 closed by D3 · OBS-F03-2 optional · OBS-F04-1 carried |
| TD-F05-UNIT | **BO-F-05 delivered (2026-08-22) — lineage & evidence visualization (Reconciliation §17/§37 traceability).** F-05.1: lineage fields surfaced across the three surfaces — intelligence cards gained a per-report read-only Lineage toggle rendering the evidence panel (audit correlation id, report hash, created/created-by, and the persisted `source_artifact_ids`); the signal stream's expanded lineage section completed with the three previously-missing report ids (statistical/economic/generalization) + the **fabricated `"exp-001"` fallback replaced by the honest "not recorded"** (pre-F-05 fabrication removed — disclosed churn, stricter); alerts gained the audit-correlation line on every card + a detail-record row. The client read types were extended with the backend's persisted lineage fields (audit_correlation_id/source_artifact_ids/created_by, optional — "where present" per the BO; signal-validation carries `source_signal_ids`). F-05.2: `LineageEvidencePanel` reuses `ArtifactLineageTree` with **PERSISTED-ONLY nodes** — `buildPersistedLineageNodes` renders the artifact node (real hash/timestamp/status) plus real source nodes under the new neutral `source_artifact` tree role (the tree's type union extended for this — disclosed extension; the illustrative default chain with its placeholder hash is NEVER used for real artifacts). A 12-node source render cap with an honest disclosure note handles real per-bar lineage id lists (the F-05 capture caught a real correlation report carrying ~hundreds of source ids — the cap is the fix). F-05.3: scope discipline pinned — the presentation-only tab bar and counters carry zero lineage controls. F-05.4: absent fields render "provenance not recorded" everywhere (per-field and a banner when nothing at all is recorded; created_at counts as evidence, so partial reports show per-field absence only). **Tests: 10 new (`f05_lineage_evidence.test.tsx`) — persisted-only node construction (no fabricated chain roles), panel rendering with real hash/audit/sources, honest absence (per-field + banner conditions), the source cap (200 → 12 + disclosure), intelligence-card toggle round-trip (real fields + zero controls inside the panel), signal lineage completion (three report ids + audit + no `exp-001`), alert audit correlation (card + detail + absence), scope discipline. Full frontend suite: 956 passed (946 floor + 10); tsc -b clean; production build green. Level-I capture (real browser, real backend): lineage panel over the REAL persisted correlation report (hash `34070aaf…`, audit `b9e1d9ee…`, capped source list); the seeded signal's expanded lineage (real experiment id, truncated audit correlation, honest "not recorded" rows, no fabricated fallback); the alert detail record's audit-correlation row (`8404039e…`). Patch `f05.patch` (chain position 36, frontend-only, register rows in patch). Deviations D1–D4 disclosed. **Awaiting ITRGA determination.** | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-F-05 §12 completion condition) |

**End**

---

## BO-F-06 appended rows (2026-08-22)

*Append-only convention honored (TD-REGISTER-APPEND-ONLY-CONVENTION): pure additions, historical rows untouched.*

| TD-F05-UNIT-STATUS | **Status supersession (2026-08-22): BO-F-05 CLOSED — ITRGA APPROVED WITH OBSERVATIONS** (ITRGA_DETERMINATION_F-05.md). CA-TRANSMIT-1 honored (9/9 hashes); patch `cae375af…` verified; the D3 honesty fix verified in custody (the pre-F-05 fabricated `"exp-001"` fallback is gone; every absent field renders "not recorded"); the persisted-only lineage panel verified (`source_artifact` nodes, real hash only when persisted, 12-source cap + disclosure, the illustrative default chain never rendered for real artifacts); 10/10 new + full suite 980/187 green. OBS-F05-1 (Info): the 12-source render cap — an optional future "expand all / paginate lineage" enhancement, not a correctness gap. | — | **CLOSED — APPROVED WITH OBSERVATIONS** | OBS-F05-1 Info (optional lineage pagination) |
| TD-F06-UNIT | **BO-F-06 delivered (2026-08-22) — the FINAL frontend unit: accessibility, performance & navigation legibility.** F-06.1 (Operator binding): the cryptic glyph rail is gone — `workspaceRegistry` icon values are now 16 SEMANTIC KEYS rendered by a new inline-SVG `WorkspaceIcon` module (`src/workstation/navigation/icons.tsx`; stroke=currentColor → six-theme consistent by construction; no new dependency; unknown-key raw-text fallback); the collapsed rail (widened 48→64px) renders the SVG + a VISIBLE short label per item (`railShortLabel` keyed by workspace id, displayName fallback) — **16/16 items visibly labeled (incl. the alerts launcher)**; aria-label/title retained; icons aria-hidden; the dock uses the same icon renderer. F-06.2: the audit suite extended — per-theme contrast floors re-asserted (text ≥4.5:1, focus + accent ≥3:1 per theme incl. AAA high-contrast), rail focus-visible styling pinned, global reduced-motion freeze pinned, landmark/label structure pinned. No NEW WCAG violations surfaced; the one fixed gap was the rail's icon-only-with-hidden-label itself (the Operator's requirement). F-06.3: **MEASURED performance, all five targets HIT** (two runs, real browser + real backend): chart first render 9.5ms (≤1s); command-palette response 20.5ms (≤100ms); workspace switching 102.8ms (≤300ms); instrument selection 13.3ms (≤200ms); live-update propagation 50ms past the wall-minute boundary (≤500ms) — the metric is defined honestly against the B-00 wall-clock bind (the server emits at the boundary; the UI reflects the new bar within 50ms of it; the backend log shows 9 bars/symbol emitted during the observation). Raw numbers: `docs/evidence/f06/f06_performance_raw.json` + `f06_performance.log`; method disclosed in the report (rAF/interval polling, 1-frame resolution). **Tests: 13 new (`f06_legibility_accessibility.test.tsx`) — semantic-key pinning (no cryptic glyphs), SVG-render + fallback, visible-label presence on every rail item, aria-label retention, railShortLabel coverage, per-theme contrast (incl. focus), focus-visible + reduced-motion pins. Full frontend suite: 969 passed (956 floor + 13); tsc -b clean; production build green. Level-I capture: 16/16 SVGs + 16/16 visible labels (Operations/Live/Charts/Signals/Analytics/Intel/Investigate/Scenarios/Plans/Execution/Portfolio/Journal/Artifacts/Governance/Settings/Alerts); rail in light (white bg, blue stroke) and high-contrast (black bg, blue stroke) — token-driven theming verified. Patch `f06.patch` (chain position 37, frontend-only, register rows in patch). Deviations D1–D2 disclosed. **The frontend roadmap (F-00 → F-06) is now complete.** Awaiting ITRGA determination. | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-F-06 §12 completion condition) |

**End**

---

## BO-X-01-TERMINAL appended rows (2026-08-22)

*Append-only convention honored (TD-REGISTER-APPEND-ONLY-CONVENTION): pure additions, historical rows untouched.*

| TD-F06-UNIT-STATUS | **Status supersession (2026-08-22): BO-F-06 CLOSED — ITRGA APPROVED WITH OBSERVATIONS (ITRGA_DETERMINATION_F-06.md) — the frontend roadmap (F-00 → F-06) is COMPLETE.** CA-TRANSMIT-1 honored (patch `3ffc03c8…` verified, 7 files, zero backend); the navigation-legibility fix verified as the centerpiece (16 semantic SVG icons + visible labels — "the exact defect the Operator named is eliminated"); accessibility audit + per-theme assertions verified; 13/13 new + full suite 993/188 green in custody. **OBS-F06-1 (Medium, evidence transmission):** the F-06 performance raw evidence (`f06_performance.log`, `f06_performance_raw.json`, `f06_cloneside_vitest.log.txt`) and a distinct dock screenshot were declared in the manifest but absent from ITRGA custody (Operator relay gap — same class as OBS-F00-4), plus `f06_dock.png` was byte-identical to `f06_rail_highcontrast.png` (the DA's own disclosure: the dock capture landed on identical viewport pixels because the navigation was collapsed). **Corrective delivered with the X-01-TERMINAL transmission:** the three files re-transmitted byte-identical + a CORRECTED dock screenshot (navigation expanded; icons + labels per link captured). **OBS-F06-2 (Info):** the §33 "performance NOT PROVEN" finding is now resolved in substance (all five targets HIT with measured numbers; the raw JSON finalizes it). | — | **CLOSED — APPROVED WITH OBSERVATIONS; OBS-F06-1 corrective transmitted with X-01-TERMINAL** | ITRGA confirmation of the OBS-F06-1 closure |
| TD-X01-TERMINAL-UNIT | **BO-X-01-TERMINAL delivered (2026-08-22) — the terminal-tier verification: the ten-hop governed operator workflow executed through the RUNNING terminal, real browser, real dev stack, real data. No code changes (verification-only per BO §3/§6; the patch is docs-only: register rows + DOC13).** Per hop (Level-I evidence: capture log + screenshots + server-log cross-reference): (1) LOGIN — wave scene (16 candles/12 particles/glow), governance chips "GATE: CLOSED · RESEARCH-ONLY · NON-ACTUATING", motion frames differ / reduced-motion frames byte-identical, auth 200; (2) NAVIGATION — 16/16 SVG icons + 16/16 visible labels + aria-labels, theme switch persists across reload; (3) INSTRUMENT→CHART — BTC/USD selected, chart-symbol-badge, terminal chart stage, posture badge "live:simulated", overlay buttons present; (4) STRUCTURAL SIGNALS — 24 EVENTS over the REAL corpus (BTCUSD H1 native), "DETERMINISTIC · DESCRIPTIVE — NOT A PREDICTION" + provenance line; (5) PREDICTIVE — "PREDICTIVE (ML)" label + honest deferred-empty; (6) INTELLIGENCE — all five families UI-generated (scenario/portfolio-risk/correlation/regime persisted, data-class historical:real) + honest SIGNAL_VALIDATION_EMPTY_SCOPE 422 notice; (7) ALERTS — 3/3 with derived domains (risk/system/market), absolute-UTC created, lineage + audit lines, domain filter 1/3, ack read-state-only (3→2 buttons after API 200); (8) LINEAGE — persisted-only panel over the real correlation report (hash + audit correlation; 1442 source artifacts → 12-node cap disclosed; fabricated chain absent; zero controls); (9) ASSISTANT — grounded ask over the real correlation report (r=0.8766, audit correlation rendered), ORDER_INSTRUCTION_REFUSED, empty-grounding GROUNDING_REQUIRED from a clean panel; (10) GOVERNANCE/NON-ACTUATION — refined control scan: ZERO actuation-control violations (the inert "Execution Research"/"Trade Planning"/"Portfolio Research" surfaces correctly classified as research), posture badge live:simulated, audit trail active (last 50: SECURITY 20 / GOVERNANCE 30). Server-log cross-reference: 1143 request lines — 12 indicator-series, 3× each intelligence 201/422, 3 acks, 12 assistant 201s, 4 audit reads, 9 logins. **Gap list (honest):** (a) predictive deferral (no promoted model — the deferred track's resumption is the Operator's next decision, now unblocked); (b) the live feed is simulated (labeled live:simulated everywhere — real broker integration remains a future governance-gated unit); (c) carried residuals (OBS-B-AUDIT-1 file-sqlite serialization; RR-moderate advisories; OBS-F05-1 lineage pagination). **No defects found** (the two capture-script corrections — sticky-header click interception handled by disclosed DOM activation, and the hop-10 scan's false positive on the "Execution Research" nav label refined to phrase matching — were evidence-tooling fixes, not platform defects). Patch `x01t.patch` (chain position 38, docs-only: register + DOC13). Evidence: `docs/evidence/x01t/*`. **The programme is complete: the platform is operational with a full working UI — the Operator's gate for resuming predictive trials is met.** Awaiting ITRGA determination. | — | **Delivered — awaiting ITRGA determination** | ITRGA determination (BO-X-01-TERMINAL §11 completion condition) |

**End**
