──────────────────────────────────────────────────────────────────────────
BUILD ORDER — V2·BE-12E: RECONCILIATION + INCIDENT WORKFLOWS
(final band; carries-resolves law; the ladder CLOSES here)
POSTURE: builder-capable · execution-authority: NONE — reconciliation
and incident records are EVIDENCE machinery ONLY; every run/open/close
is an OPERATOR-INITIATED VERB exercised in coupon worlds and corridor
witnesses; NOTHING in this chapter runs on timer, scheduler, signal,
or self-activity of any kind (N4/N-O11 standing). Register posture
everywhere: ACTIVATION INSTRUMENT NOT IN FORCE; LIVE=REGISTERED_LOCKED;
funded account: NONE; LIVE real-network behavior: NOT PROVEN (≠ FALSE).

CITATIONS: REQ-V2-BE-12-001 (R-1.1; R-3.1 digest per-world; R-3.2/3.3;
R-3.4 step-up carriage; R-2; R-6.1 ordering law — SATISFIED: 12D
FIELDED) · DR-V2-BE-12-001 (DR-0 boundary recitals; DR-1 sub-ladder
12E + C-2 clean-runs-evidenced; DR-2 L1–L6; DR-3 HONESTY PAIR bound
to 12E; DR-4 surface floor + **DR-F2 CONVERSION LAW assigned to this
BO** + schemes v2_live_exec_reconciliation/v2_live_exec_incident;
DR-5 closeout line; DR-6 non-goals) · BO-V2-BE12A/12B/12C/12D
(predecessor chapters CLOSED: 12A FIELDED+WIRE-PROVED · 12B FIELDED ·
12C FIELDED · 12D FIELDED at head 20260909_0056 per
ITRGA-VERDICT-V2-0056-FIELDED-001) · REV-V2-BE12D-001 §9 (carry-list
landing in this BO: DR-F2 conversion law · honesty pair · counterfeit-
witness scan · compver rows with last build · LOW-1 · LOW-2) ·
LOW V2-BE12D-DEL-001 (mode-stamp; disposition PINNED §1.a.4) ·
LOW-2 SELECT-then-insert (disposition PINNED §1.a.5) · LAW-BORDER-01
(§1.f borders) · ERRATA E-0054-A11.4 + E-0055-A10.3 (drafting law
from birth: ITEMIZED drift; RENDERED CHECK pins; DDL print) ·
DIRECTION-POSTBE11-001 §1/§7 · N4/N-O11. Suite floor: **1,290**.
Alembic head: 20260909_0056 (FIELDED) → sub-head target 20260909_0057.
Pointer lane: ENUMERATIONS DRIFT — census/perm/compver figures below
are enumerated vs landed bytes; when in doubt, re-enumerate, never
recite.

──────────────────────────────────────────────────────────────────────────

1) THE ENGINEERING MISSION (12E — the final band)
   a. PREMISE LOCKS + THE TWO REGISTER ANNOTATIONS (carry-list items
      LOW-1, LOW-2 resolve HERE, by law, with ZERO 12D code moves):
      a.1 EVIDENCE-ONLY LAW: reconciliation report rows and incident
          rows are EVIDENCE. They actuate nothing, authorize nothing,
          trigger nothing. A `parity_break` never auto-acts (no
          incident auto-open, no notification fan-out, no retry);
          surfaces carry the facts; the operator decides. Pin: absence-
          with-pin scans for timer/scheduler/self-trigger tokens over
          both new packages + the API wedge.
      a.2 OPERATOR-INITIATED VERBS ONLY: run / open / close are POSTs
          with actor + operator carriage; incident OPEN and CLOSE are
          CONFIRM-RANK (SAL-4 + step-up; second FACTOR, never second
          actor — DR-2 carriage recited). Reconciliation RUN is SAL-4
          **without** step-up — BO-NAMED EXCEPTION with rationale: it
          writes only its own evidence ledger, actuates nothing, and
          must remain a routine operator act; actor_id/operator_id
          carriage stands regardless. Absence of permission ⇒ chassis
          403-family; absence of step-up on the confirm-rank pair ⇒
          the chassis's SEALED step-up refusal (enumerated at design
          commit; never a new untyped shape — SAME sealed chain as
          12A–12D, byte-recited).
      a.3 LANE NON-COMMUTATION, FINAL FORM: the practice lane NEVER
          consults reconciliation or incident state; LIVE-gate
          vocabulary stays L1–L6; PRACTICE_LOCK_ORDER stays five
          members (recited); a coupon runs recons + opens/closes an
          incident in-coupon-world and proves the practice lane's
          lawful pass and its degraded refusals BOTH carry practice
          vocabulary only. The lanes' refusals never commute — now
          with the full evidence plane standing.
      a.4 **REGISTER ANNOTATION LAW — kill-switch mode stamp (closes
          LOW-1 = V2-BE12D-DEL-001).** The review's docstring route
          is REJECTED with rationale: it would move compver-scoped
          bytes and break the `lxe-1.2.0` disk-re-proof law for every
          future card. ADOPTED INSTEAD (zero code move): on
          `v2_live_kill_switch`, `mode` is a HOUSE GOVERNANCE STAMP
          with fixed literal 'RESEARCH' — it is NOT a world-state
          field; the acting-world caption rides actor_id/operator_id/
          step_up_ref. Pinned by the suite coupon
          `test_killswitch_mode_stamp_fixed_constant`: arm-insert and
          EVERY valve transition leave `mode == 'RESEARCH'` — the
          constant is now LAW; a silent future drift of any kind
          fails the suite. From 12E onward, NEW evidence-ledger row
          types (reconciliation, incident) stamp `mode` = the REQUEST
          WORLD under house semantics (threaded from the chassis,
          never a literal) — coupon-witnessed; the stamp semantics
          forward are the corrected ones, and the 12D constant stands
          documented, not patched. **No edit to ANY 12D compver-scoped
          file is authorized by this annotation.**
      a.5 **REGISTER ANNOTATION LAW — singleton-writer posture (closes
          LOW-2, SELECT-then-insert family).** The SELECT-then-insert
          backstops and the valve-dance transitions assume the house
          SINGLETON-WRITER deployment posture; multi-writer hardening
          is a future DEPLOYMENT requirement (same register class as
          true two-operator segregation, DR-2 tail). 12E's new writers
          (reconcile insert; incident valve dance) are built under the
          same posture and carry this annotation in their module
          headers? NO — carried in the GOVERNANCE NOTE only (module
          docstrings of any prior-compver file stay byte-frozen; new
          12E modules may recite the posture line in their own
          docstrings at DA's option). No concurrency coupon is
          authorized or required.
   b. **THE DR-F2 CONVERSION LAW** (this BO writes THE law, per DR-4;
      it lands before any parity coupon compares live_exec facts
      against BE-9 projections):
      b.1 ONE LOCUS — `app/v2/live_exec/reconcile/money.py` is the
          SINGLE designated conversion module for the entire band.
          No other module, engine, coupon, or card converts money
          representations. Coupon-scanned: the canonicalization
          entry-point token appears ONLY in the locus and in call
          sites; Projection→canonical logic appears nowhere else.
      b.2 ONE DIRECTION — conversion runs projection-fact → canonical
          and live_exec-fact → canonical AT THE PARITY BOUNDARY ONLY.
          Nothing converts in the other direction; nothing converts
          "for display"; engines/coupons never hand-convert (no
          ad-hoc conversion in coupons — DR-F2 literal).
      b.3 THE LAW'S FOUR PINS (coupon-pinned):
          · interior representation: `decimal.Decimal` ONLY.
            **float is BANNED** anywhere in the live_exec money path
            (token scan: `float(` over both new packages + recon
            call sites; the scan is positive-pin, corpus enumerated).
          · rounding mode NAMED: **ROUND_HALF_EVEN**, applied exactly
            once at quantize-to-canonical, via the BE-9 projection
            body's OWN normalization convention — the locus DELEGATES
            (imports), never re-implements projection normalization;
            the delivery DR recites the imported convention clause
            verbatim under C-2. (If enumeration shows the projection
            convention names a different mode: the locus still
            delegates; the DR discloses the actual mode; the coupon
            pins the disclosed mode. The locus may never invent.)
          · canonical form: fixed-point string at exactly 2dp;
            no exponent; no leading '+'; leading zeros stripped
            except the single leading zero of |x|<1; sign only when
            negative. Both sides compared as canonical strings —
            equality IS parity at the money seam (D-B10-2DP mirror).
          · quantities that are NOT money (instrument quantities)
            are out of scope of the money locus and, if compared,
            ride their own standing projection discipline — named
            at design commit, not invented.
      b.4 The parity engine (§1.c) may import the locus ONLY.
   c. THE RECONCILIATION ENGINE (`reconcile/` package):
      c.1 READ-ONLY BOUNDARY: imports BE-9 projections (+ the
          sanctions/projection layer it already stands on) READ-ONLY;
          the import-boundary recital coupon re-proves the DR-0.1
          direction law extended over the 12E packages; nothing
          imports live_exec except the sanctioned adapter boundary
          (absence-with-pin scan over the new DB consumers).
      c.2 SCOPE-OF-PARITY (closed, enumerated at design commit):
          fill/position parity vs projection (DR-1 literal): projected
          fill/position facts vs the live_exec ledger facts
          (submissions → ack_fills → fills; modify ledger consistent).
          Every compared dimension enumerated; nothing ad-hoc.
      c.3 OUTCOME VOCABULARY — table CHECK closed:
          (`'clean'`, `'parity_break'`). Nothing else exists.
      c.4 **C-2 CLEAN-RUNS-EVIDENCED LAW (DR-1 binding):** EVERY run
          writes exactly ONE `v2_live_exec_reconciliation` row —
          clean runs included; a run writing zero rows is a defect;
          a run writing >1 is a defect. Both arms coupon-witnessed
          (empty-ledger world ⇒ exactly one row, outcome `clean`;
          seeded-drift world ⇒ exactly one row, `parity_break`,
          drift facts serialized in the row).
      c.5 ROW SHAPE: surrogate uuid pk; run_at; scope block (world,
          instrument set pinned, window); outcome (§c.3 CHECK);
          drift_facts (JSON, NULL iff clean); regime block
          (actor_id, data_class, mode=REQUEST WORLD per §a.4,
          operator_id, created_at); data_class CHECK single member
          ('evidence'); register-facing digest = sha256 over the
          canonical row serialization (R-3.1 per-world; coupon
          recomputes). Guard pair (UPDATE/DELETE triggers, house
          messages): `V2 live exec reconciliation is immutable;
          UPDATE/DELETE prohibited`. NO VALVE — a report NEVER
          transitions; correction = a new run, never an edit.
      c.6 SURFACE (DR-4 floor): POST `/v2/live-exec/reconcile/run`
          (SAL-4, §a.2 exception) — runs now, synchronously, returns
          the resulting row on the envelope; GET
          `/v2/live-exec/reconcile/latest` (SAL-2) — latest row or
          typed zero-row shape (never an error); GET
          `/v2/live-exec/reconcile/{id}` (SAL-2). No other routes.
   d. THE INCIDENT WORKFLOW (`incident/` package):
      d.1 RECORD SHAPE (DR-1 pinned, every field NOT NULL except as
          stated): surrogate uuid pk; opened_by (operator actor);
          severity — table CHECK CLOSED ('SEV-1','SEV-2','SEV-3',
          'SEV-4'); instruments_pinned (JSON array of instrument
          ids; empty array BANNED by app-level typed refusal —
          `incident_instruments_absent` — since an incident names
          what it touches; serialized deterministically);
          recovery_path (TEXT, non-empty); status — CHECK closed
          ('open','closed'); opened_at; closed_at (NULL iff open);
          closed_by (NULL iff open); regime block (actor_id,
          data_class CHECK single member 'evidence', mode=REQUEST
          WORLD per §a.4, operator_id, created_at); register-facing
          digest = sha256 over the canonical serialization incl.
          status lifecycle stamps (coupon recomputes digest on open
          AND on close).
      d.2 LIFECYCLE LAW (the valve dance, house pattern as proven by
          the kill-switch — drop UPDATE guard → ONE transition →
          recreate → verify-or-die): OPEN = INSERT 'open' (SAL-4 +
          step-up; severity vocabulary enforced at schema AND typed
          at the API edge). CLOSE = 'open' ⇒ valve ⇒ 'closed',
          stamping closed_at/closed_by (SAL-4 + step-up). **Close
          with no open/unknown incident ⇒ typed refusal
          `incident_not_open`** (non-firing); **close of an
          already-'closed' incident ⇒ typed refusal
          `incident_already_closed`** (non-firing). Every cell
          coupon-pinned; the inversion (direct-SQL UPDATE ⇒ guard
          refusal, valve ⇒ exactly once) witnessed over the real
          chain.
      d.3 SURFACE (DR-4 floor): POST `/v2/live-exec/incident/open`
          (SAL-4 + step-up); POST `/v2/live-exec/incident/{id}/close`
          (SAL-4 + step-up — THE single sanctioned close path; no
          other close exists, token-scanned); GET
          `/v2/live-exec/incident/{id}` (SAL-2); GET
          `/v2/live-exec/incidents` (SAL-2 — list shape, zero-row
          shape typed, never error). No DELETE route anywhere.
      d.4 NO SELF-ACTIVITY: the incident package never opens its own
          rows (absence-with-pin scan; a parity_break may be QUOTED
          in instruments/recovery text by an operator, never linked
          by the machine).
   e. THE HONESTY PAIR (DR-3, bound to 12E — BOTH halves ship here):
      e.1 Register-line clause: from this BO onward, ALL envelopes
          carry the clause exactly:
          `LIVE real-network behavior: NOT PROVEN (≠ FALSE)` —
          the 12D-era ordered `_REGISTER_LINE` is superseded BY
          CITATION with a grep-able supersession pointer (12D-era
          mechanism recited), and the 12E corpus carries the new
          exact-form pin:
          `BE-12E RECONCILIATION+INCIDENT | LIVE=REGISTERED_LOCKED |
          funded account: NONE | activation instrument: NOT IN FORCE
          | LIVE real-network behavior: NOT PROVEN (≠ FALSE)`
          — the floor law stands (startswith 'BE-12'; contains
          'LIVE=REGISTERED_LOCKED'; 'funded account: NONE'; the 12B
          structural-invariants pin (`BOUNDARY_STANCE ==
          "practice_wired"`) STANDS UNTOUCHED).
      e.2 **COUNTERFEIT-WITNESS SCAN** (PGF-022 lesson applied to
          claims): suite coupon asserting NO evidence artifact in the
          band family claims live-network proof. Corpus PINNED and
          POSITIVE-PINNED (every path asserted to exist — absence
          cannot pass vacuously): the band's evidence corpus the DA
          has shipped (12A–12E delivery DRs + witness transcripts as
          present), the activation template artifact text, and the
          register-line constants themselves. BANNED CLAIM TOKENS
          enumerated at design commit (e.g., live-network-proven
          claims in any case-form); the coupon body itself is
          EXCLUDED from the scanned corpus by recited law; allow-list
          for the honest phraseology (NOT PROVEN ≠ FALSE family)
          pinned.
      e.3 Template-literal closure (REV-V2-BE12D-001 §6.2 residual,
          optional item, TAKEN): coupon
          `test_template_artifact_hash_literal` pins the disclosed
          artifact hash `142f87723a63420f…` (full literal enumerated
          from final bytes at design commit) against the template —
          intra-band text drift is now formally closed.
   f. CLOSED VOCABULARIES + DEL-001-CLASS DEFENSE (LAW-BORDER-01,
      final extension):
      f.1 The 12E NEW-ID set is CLOSED and enumerated at design
          commit: `incident_not_open` · `incident_already_closed` ·
          `incident_instruments_absent` — DISJOINT from the practice
          lane (5), the ACT set (9), the 12D governor set (1 new),
          each other; F1 recital coupon closes the FULL active set
          LIVE(6) + PRACTICE(5) + ACTS(9) + GOVERNOR + 12E(3) with
          the ONLY overlap remaining exactly the two declared lock
          tokens (`killswitch_armed` per 12D law). CWS-register-
          recoverable per id.
      f.2 Every NEW writer gets API-LEVEL border coupons over the
          real alembic-applied chain with COUNTER-ARMS: incident
          cycle (perm absent ⇒ 403 family; step-up absent ⇒ sealed
          chassis refusal on BOTH confirm verbs; open 200 ⇒ close
          200 ⇒ re-close ⇒ `incident_already_closed`; close-of-
          unknown ⇒ `incident_not_open`; direct-SQL UPDATE on the
          incident row ⇒ guard refusal (the valve untouched); digest
          recompute on open + close over the real chain); reconcile
          (perm absent ⇒ 403; run 200 writes exactly one row —
          C-2 law over the real chain; latest reflects it; mode
          stamp == request world witnessed — §a.4 forward law). The
          reconcile engine NEVER touches the incident table (and
          vice-versa) — cross-table no-writer scans both directions.
   g. MIGRATION `20260909_0057` (AMEND-0057): +2 tables
      (`v2_live_exec_reconciliation`, `v2_live_exec_incident`) + two
      guard pairs (+4 triggers) + indexes enumerated at design commit
      and DISCLOSED (uuid surrogate pks; any functional indexes
      named) + closed CHECKs per §c.5/§d.1 (+5 permissions,
      enumerated fresh: `v2.live_exec.reconcile.run` SAL-4,
      `v2.live_exec.reconcile.read` SAL-2, `v2.live_exec.incident.open`
      SAL-4, `v2.live_exec.incident.close` SAL-4,
      `v2.live_exec.incident.read` SAL-2) +1 compver row INSERT-ONLY
      `live_exec_engine|lxe-1.3.0` over the EXPANDED lxe set (the 13
      12D-set files — **ALL BYTE-UNMOVED, disk re-proof law stands**
      — + the 12E package files enumerated at design commit:
      `reconcile/{__init__,money,engine}.py`,
      `incident/{__init__,engine}.py`, any additional module
      disclosed), DISCLOSED per C-2 from final bytes. Append-only
      registry law: `lxe-1.0.0`, `lxe-1.1.0`, `lxe-1.2.0` rows STAND
      AND STILL DISK-RE-PROVE (the carry item **"compver rows with
      last build"** closes here as: the registry's last row names the
      last build's order, `BO-V2-BE12E-001`, and the closeout verdict
      recites every row against its band). ONE upgrade line; census
      re-tattoo target **94/88/17** (base 90/83/16 + §g deltas,
      RE-ENUMERATED pre-commit); DOWNGRADE EXACT (restores 90/83/16;
      guard/valve dances silent; 1.0.0–1.2.0 preserved). CHECK-name
      pins in every card/probe carry the RENDERED form per
      E-0055-A10.3, enumerated from the delivery's migration coupon
      and pre-verified on the rehearsal render; DDL print rides the
      card from birth. Drift gate: ITEMIZED (exactly the 9 inherited
      V1.0-table tokens; ZERO 12E tokens).

2) COUPON RANGE (12E): **18–28 new**. Head-of-line after close:
   1,290 + Δ(18..28) = **1,308..1,318**. Supersession mechanics: the
   12D-era exact-form register pin is superseded BY CITATION (grep-
   able pointer, floor re-recited); the 12E corpus carries the new
   exact-form pin incl. the honesty clause (§e.1); the 12B structural
   pin stands untouched. Compver/perm/census deltas coupon-verified
   exact.

3) F1 RECITALS: every refusal id register-recoverable with CWS
   discipline; lane language separable (§a.3 coupon); closed
   vocabularies pinned at design commit (§f.1); binding re-tattoos
   clean F2 shadow first; the EVIDENCE-ONLY scans (§a.1) are positive-
   pin (corpus enumerated).

4) ACCEPTANCE (12E CLOSURE CLAUSES — the ladder's last rung):
   - Evidence-only law: no timer/scheduler/signal/self-activity token
     anywhere in the 12E surface (scans positive-pin); parity_break
     never side-effects; incident never self-opens.
   - Verb law: run/open/close operator-initiated; confirm-rank pair
     carries step-up on the SEALED chassis chain; the reconcile-run
     step-up exception stands per §a.2 rationale, coupon-witnessed.
   - DR-F2 conversion law: one locus, one direction, ROUND_HALF_EVEN
     delegated-not-reimplemented, canonical 2dp-string parity, float
     banned (scan), no ad-hoc coupon conversion — all coupon-pinned;
     C-2 recital of the imported convention in the DR verbatim.
   - Reconciliation: boundary read-only; outcome CHECK closed;
     clean-runs-evidenced exact (both arms); digest recompute over
     the real chain; NO valve; zero-row GET shape typed.
   - Incident: DR-1 record shape exact incl. instruments-pinned
     refusal; lifecycle every cell; close = THE single sanctioned
     path (scan); digest on both transitions; sealed step-up chain.
   - Honesty pair: clause on every envelope from this BO (exact-form
     pin + supersession pointer); counterfeit-witness scan shipped,
     corpus positive-pinned, banned-token set enumerated, self-
     exclusion recited; template-literal closure shipped (§e.3).
   - REGISTER ANNOTATIONS CLOSED: LOW-1/DEL-001 mode-stamp law +
     coupon, ZERO 12D byte moves (1.2.0 re-proof law preserved —
     coupon `test_killswitch_mode_stamp_fixed_constant` PASSES);
     LOW-2 annotation text landed; "compver rows with last build"
     closes per §g.
   - DEL-001-CLASS DEFENSE: §f.2 borders over the real corridor with
     counter-arms; no injection-only evidence for any writer.
   - Migration: one line; 94/88/17; lxe-1.3.0 insert-only + C-2 with
     **every prior row standing AND disk-re-proving**; downgrade
     exact; rendered CHECK pins + DDL print (both errata from birth);
     itemized drift.
   - Lane law: practice lane untouched by recon/incident state
     (§a.3); LOCK_ORDER byte-recital unchanged.
   - Suite green at 1,308..1,318; micro-compare green.
   - CLOSEOUT CONDITIONS (DR-5 + DR-3(i), the band's final gate —
     run at the 12E INT review and the closeout verdict): (i) the
     five locks L1–L6 enumerated with their fielded-lineage refusal
     witnesses across the 0053–0057 card record — ANY lock lacking a
     fielded-lineage witness is witnessed by a CLOSEOUT ADDENDUM
     card (read-only/400-pin pattern; operator-elective harness)
     BEFORE the DR-5 line issues; (ii) the DR-5 BAND-CLOSEOUT
     register line issues EXACTLY:
     `BE-12 CAPABILITY-COMPLETE · ACTIVATION INSTRUMENT: NOT IN
     EXISTENCE · funded account: NONE (activation prerequisite) ·
     LIVE mode: REGISTERED-LOCKED` **+ DR-3(i) clause appended
     exactly:** `· LIVE real-network behavior: NOT PROVEN (≠ FALSE)`
     — definitional note (prevents DR-5-vs-12D register-reading
     collision): "ACTIVATION INSTRUMENT" names the FORCE-BEARING
     RECORD (zero-row == not in existence); the fielded template
     artifact is the instrument's extant non-force form per 12D BO
     §b.2 law and does not contradict the phrase.
   - JURISDICTION NOTE (as carried): coupon-verified + corridor-
     witnessed acts are acceptance-grade; NO real-network activation
     act of any kind is performed or required — not by this band,
     not by the closeout; the instrument NEVER takes force; any
     future force-taking is its own register act under a future
     build order.

5) DELIVERY SHAPE: REM-001 source transcript (changed files only) +
   fail-first witness + full suite transcript + OBS-E-class witness
   for each NEW writer (reconcile.run, incident.open, incident.close)
   + failure-witness set + migration witness + C-2 disclosures
   (lxe-1.3.0 from final bytes; the imported rounding-convention
   clause verbatim). Register line on all envelopes closes at §e.1's
   exact form.

6) REGISTER MOVES: on issuance, campaign register appends AC12E:
   "BE-12E ISSUED | final band | DR-F2 locus+mode named | honesty
   pair ships | LOW-1/LOW-2 annotations closed | compver last-build
   row named | 0057 target 94/88/17 | suite floor 1,290 → head-of-
   line 1,308..1,318 | closeout gate pinned (DR-5 + DR-3(i))".
   Carry-forwards: **NONE REMAINING** — the register's carry-list
   closes with this BO; the sole postures outside the ladder remain
   the standing electives (real-wire practice witness = separate
   authorization; deployment hardening notes = future register acts).

──────────────────────────────────────────────────────────────────────────
ITRGA — issued 2026-09-09. This order authorizes no production/live-
network behavior; execution-authority remains NONE; reconciliation and
incident records are evidence machinery that never actuate; the
activation instrument NEVER takes force; the kill-switch's 12D bytes
stand frozen with their annotation law. On completion the INT review
runs under the 16-section house template with finding IDs
V2-BE12E-DEL-NNN; FIELDED then frees the DR-5 closeout verdict — the
ladder's last act.
──────────────────────────────────────────────────────────────────────────
