# DA ACKNOWLEDGEMENT — PC-FEU02-1 REVIEW (ACCEPTED WITH CORRECTIONS)

| Field | Value |
|---|---|
| Document ID | DA-ACK-REV-PCFEU02-001 |
| Author | DA · 2026-09-11 |
| Instrument processed | `AXIOM-V2-ITRGA-REV-PCFEU02-001` (custody md5 `ecaf77fb8074a012bf73ce8d58cc03b9`, cmp-verified) |
| Verdict absorbed | **ACCEPTED WITH CORRECTIONS** — filed bytes stand at `2089d28c…`; §6.2 discharged; E-1 pinned; **fail-first stage OPEN** |

## 1. Corrections absorbed as evidence duties (each will ride the pack)

| ACC | Duty absorbed | DA execution note |
|---|---|---|
| **ACC-1** | Expelled-site census = **SIX**, header measurement controls; E-4/E-7 evidence all six line-precisely before→after | **Re-measured this turn, line-precise on the as-built shell (`InstitutionalWorkspaceShell.tsx`):** L48 `SAL-2 (Internal)` container recital (comment header) · L142 command-registry toast body (`…Gate CLOSED. Research-only presentation.`) · L145 overlay.notify toast (`"Gate CLOSED"…`) · L299 gate chip literal · L303 `Research-only` chip · L306 `Presentation shell` chip · L308 `live:simulated` badge. NOTE FOR THE RECORD: the line-precise sweep finds the six RULED sites and additionally the two chip literals `Research-only`/`Presentation shell` named in contract C4 — the pack's expulsion census will carry ALL EIGHT rendered/recital sites with the six-of-record called out against the ruling and the two C4-named chips as contract-scope companions; nothing expelled silently, nothing under-counted. (Over-delivery on a census is disclosure, not scope creep: every site is inside the C4 rebuild the contract already orders.) |
| **ACC-2** | Conditional boundary files declare at E-7 per-file: moved/not-moved, trigger, additions-only rule | Absorbed; E-7 will carry four explicit rows (RouteAnnouncer.tsx/.test.tsx, tokens.css, theme.ts) |
| **ACC-3** | E-6 enumerates coupon disposition BY NAME: superseded-by-citation w/ negative pins vs byte-still heritage | Absorbed; the U01 pattern formalized as a named table |
| **ACC-4** | E-3 witnesses every KEPT header control (switcher, theme, settings, sign-out, palette trigger) producing real effect or truthful unavailability | Absorbed into the C6 script as explicit steps |
| **ACC-5** | Mode-badge = locked-config law (persist with unverified mark) distinct from health's stale⇒CHECKING | Confirmed as the contract's written intent; both laws will be separately couponed |

## 2. OBS absorbed

OBS-1 FE0-BF-5 registered · OBS-2 absorbed-reference set available at approval time (noted for the Operator's visual gate) · OBS-3 belt-and-suspenders clause retained.

## 3. Loop position

**Fail-first OPENS NOW** — reds against the as-built heritage shell for the C5 state matrix, the C9 boundary needles, and the ACC-5 twin laws. Then implementation, then `FEPACK-FEU02-001` custody-law packaged.
