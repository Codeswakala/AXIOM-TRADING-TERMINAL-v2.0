# ITRGA PROCESSED ACKNOWLEDGEMENT — PC-FEU02-1 REVIEW (ruling rider ACC-1-R1)

| Field | Value |
|---|---|
| Document ID | AXIOM-V2-ITRGA-ACKPROC-PCFEU02-001 |
| Date | 2026-09-11 |
| Instrument processed | `DA-ACK-REV-PCFEU02-001` — custody identity **md5 `57c14c695135af8511d62cf3c14b05ed`** (2,875 B) |
| Custody chain check | ACK's claimed pin for the review it processed — `ecaf77fb…` — measured **EXACT** against `AXIOM-V2-ITRGA-REV-PCFEU02-001` in corpus. The ack truthfully names what it absorbed |

## §1. VERDICT ON THE ACKNOWLEDGEMENT

**ACCEPTED AS ACKNOWLEDGED.** All five corrections absorbed without dispute and with concrete execution commitments (E-7 four-row declaration table, coupon disposition names, C6 witness steps, twin-law coupons). One evidentiary rider lands below; **no re-transmission required**; the fail-first stage remains **OPEN**.

## §2. CENSUS ADJUDICATION — the 7-lines / 8-sites question

The ACK enumerates **seven literal-bearing lines** while claiming an **eight-site** expulsion census (six of-record + two C4-named companions). Measured reading by this seat: the claim is **arithmetically admissible** under one reading — the contract header's "`Gate CLOSED` chip ×2 sites" denotes two *render sites* served by the single literal-bearing line listed at **L299**; thus: ruled six = `SAL-2` recital (L48) + toast bodies (L142, L145) + gate-chip literal (L299, ×2 render sites) + `live:simulated` badge (L308); companions = `Research-only` chip (L303) + `Presentation shell` chip (L306) → **8 sites, 7 lines.**

**Rider ACC-1-R1 (evidence duty, rides the pack with ACC-1…5):**
1. E-4/E-7's expulsion census must enumerate **(file:line · literal · site)** tuples, making the sites-≠-lines distinction explicit (L299 flagged as one line / two render sites).
2. Per-needle **expected raw-hit tallies** must accompany the C9 scan transcript: note that L142's toast body (`…Gate CLOSED. Research-only presentation.`) carries **two needle classes on one line**, so raw `Research-only` hits ≥ 2 inside the boundary pre-expulsion. Tallied expectation vs observed transcript must reconcile line-by-line; an unreconciled count is a finding, never a footnote.
3. The census of record is hereby restated: **8 expelled-literal sites — 6 against the ACC-1 ruling + 2 contract-scope companions** — expedited identically, line-precisely, before→after. Nothing expelled silently; nothing under-counted.

The DA's characterization of the over-delivery is **confirmed correct**: all eight sites sit inside the C4 rebuild the contract already orders — disclosure within scope, not scope creep.

## §3. LOOP STATE AT THIS RECORD

FE-U01: **CLOSED** · FE-U02: **IN BUILD — fail-first stage OPEN** (contract pinned @ `2089d28c…`; corrections + rider of record) · the corpus awaits red transcripts: C5 state-matrix pins, C9 boundary needles (with ACC-1-R1 tallies), ACC-5 twin-law pins — all red against the as-built heritage shell in `FEPACK-FEU02-001`.

---

*Recorded by this seat, 2026-09-11. The arithmetic was checked because the law requires it: narration is not evidence, and a census that cannot be reconciled is not a census.*
