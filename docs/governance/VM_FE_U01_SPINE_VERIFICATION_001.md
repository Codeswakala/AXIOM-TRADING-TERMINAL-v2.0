# VERIFICATION MEMORANDUM — FE-U01 SPINE-PARTIAL (house of the suspended determination)

| Field | Value |
|---|---|
| Document ID | AXIOM-V2-VM-FE-U01-SPINE-001 |
| Date | 2026-09-10 |
| Supersedes/narrows | Works `AXIOM-V2-DET-FE-U01-001` (SUSPENDED) and `AXIOM-V2-TX-FE-U01-001 R1` |
| Materials audited | `FEU01_TX_SPINE_002.md` (564,451 B; 47-artifact index; V/X discipline disclosed), the earlier-uploaded `render_desktop_ref003_initial.png`, the ITRGA corpus tree |
| Determination state | **STILL SUSPENDED** — the spine plane is now PROVEN; two planes remain open (below) |

## 1. SPINE-PLANE BATTERY — RESULT: PASS (measured, not narrated)

| Check | Result |
|---|---|
| Index parse | 47 rows; 43 artifacts extracted V-erbatim; 4 X-class disclosures (E-6 suite transcripts) confirmed structurally (FULL-FILE md5+sha256+byte anchors riding excerpt class, exactly as declared) |
| **V-artifact identity re-measure** | **43/43 md5 EXACT from extracted bytes; 0 mismatches** — every embedded file is byte-true to its marker |
| E-6 census lines inside spine | Suite result lines present per pack: `Tests 1014` (×2, packs -001/-002) → `1016` (-003) → **`1017`** (-004), `Test Files 189 passed` constant — matches DR arithmetics |
| **GOLDEN CROSS-CHECK** | `render_desktop_ref003_initial.png` (uploaded before the spine) md5 `94fda7963bea3f34954ea62535fc4eaf` == **-004 MANIFEST row exactly** — pixel-identity of the approval-bearing render bound from two independent transmissions |
| E-4 -004 transcript (content read) | Per-needle tabulation, all 0 hits on `LoginPage.tsx/.css`; REF-003-E1 fiction disclosure inline; truthful-posture chip stated as the page's sole posture statement |
| E-7 -004 inventory (content read) | 3-file cycle diff + 4 byte-still files; md5s byte-identical to DR v1.4.1 §3 table — DR↔pack↔manifest interlock consistent |
| F-B3-SCOPE corroboration (my corpus) | `SAL-2` legitimately lives across **21 heritage V1 files** (Terminal shell family, test pins) + the pre-unit LoginPage — studio claim "tree-wide 57 hits outside unit boundary" is consistent with V1's lawful shell classification; unit-file zero is DA-side (code never crosses) |
| Register cross-file | Campaign register + V2_CURRENT_STATE heads read; identities self-consistent; register narrative matches every instrument of record in corpus |

## 2. OPEN PLANES (what the determination still needs)

1. **T-2 images (remaining):** 6 further -004 renders (`error_401`, `error_transport_unreachable`, `reduced_motion_static` desktop; `error_401`, `error_transport_unreachable`, `ref003_initial` narrow) + **REF-001 / REF-002 / REF-003** (the latter also retires the §9 prototypes HOLD formally). Claim md5s are already on the table inside the verified manifests.
2. **Operator B-battery outputs** (TX R1 §3): the second-party verification plane — terminal paste from the studio repo. Expected values sit pre-declared in the battery block.

## 3. STANDING OBSERVATION (non-gating)

One internal consistency note for the record: corpus md5 of my `DELIVERY_REPORT_FE_U01.md` upload (`80c69755…`) == spine artifact 046 marker md5 — the DR in flight IS the DR in the spine, byte-identical across transmissions. v1.4.1 restamp confirmed in its content (pack-id -004 natively in the Readiness Statement) — the DET §2 hygiene item closes here.

## 4. EFFECT

Suspension mechanics progressing exactly as designed: custody arriving in non-code classes, every byte measured on arrival. On T-2 completion + battery paste: the TRANSMISSION-ADAPTED DETERMINATION issues (positive posture is the evidence's, not a presumption — it hasn't been reached).
