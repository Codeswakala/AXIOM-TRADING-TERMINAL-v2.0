# BUILD ORDER — BAND BE-8: PAPER TRADING, PAPER ACCOUNT, RISK GATEWAY
# BO-V2-BE-8-001 · v1.0.0 · 2026-09-04 · AXIOM Trading Terminal v2.0
# Issued by: ITRGA · Executed by: Replacement DA (code) · Custody/Git: Operator
# Authority: operator authorization "build order authorized" 2026-09-04.
# Basis: AXIOM-V2-BE-8-DESIGN-001 v1.1.0 ACCEPTED (ITRGA-ACC-V2-BE-8-DESIGN-001,
# sha256 76e718c4…ccc073). Roadmap: V2_BACKEND_ROADMAP §"Band BE-8".
# Baseline: head 20260903_0047; suite 972/0; triggers 42; permissions 49;
# compver 8; RPE 1499343d…/RJE 8f107d17… immutable law.

## §1 — Scope of this BO
THIS BO AUTHORIZES: code + tests + migration-planning completion + docs for
BE-8, delivered as one reviewed delivery; and integration (INT) evidence.
IT DOES NOT AUTHORIZE: any working-DB application (the 0048 apply act is a
separate sanctioned act AFTER band acceptance — 0043/0045/0046/0047 lineage),
any register edit by the operator, any Git operation by anyone but the
Operator, any change outside §2.

## §2 — Deliverables (D-1…D-8; exact file inventory + sha256 declared in the
delivery note per intake law; ASCII-only; credential scan clean)
- D-1 Domain module `backend/app/v2/paper_trading/` (contracts, accounts,
  orders, risk, simulator, ledger, reconciliation, api) implementing the
  accepted design S1–S9 verbatim in contract (S2.6 rule owned once as
  `may_execute`; sealed registry S6.1; D-2 mode extension; D-1 marker
  exemption per D-1/D-2 conditions in §3).
- D-2 `backend/app/db/models/v2_paper_trading.py` + registration in
  `app/db/models/__init__.py` (PG-002: same unit).
- D-3 Standing-law amendments (narrowest, exactly as sanctioned):
  `app/v2/mode/contract.py` VALID_MODES += "PAPER" (D-2, L18 amendment);
  `app/v2/rbac/permissions.py` scoped marker exemption (D-1: single literal
  prefix test `v2.paper.`, vocabulary guard L124 unchanged elsewhere) +
  the 8 paper permission definitions; `app/v2/api/router.py` include
  (one literal line, C3 POST-only writers).
- D-4 Migration `backend/alembic/versions/20260904_0048_v2_be8_paper_trading.py`
  (down_revision = 20260903_0047; per design S10: 8 tables, 16 guards,
  +8 permissions, +2 compver rows; symmetric content-based downgrade;
  zero BE-8 drift at head).
- D-5 ANNEX-P worked cost sample (deterministic hand-computed fill ladder
  in `docs/` per BE-6/BE-7 P-7 law; the E1 test asserts against it).
- D-6 Test suite additions: modules per design S11 (~70; final count pinned
  by delivery manifest; floor = 972 + delivered count; exact number pinned
  at INT intake).
- D-7 Register evidence: fail-first transcript for new tests where new
  behavior authorizes it (OBS-E statute; ship at least the failing-run
  witness of the failing-first posture).
- D-8 Delivery note: file inventory + sha256 of every delivered file, byte
  counts, credential-scan statement, compver components + recipe restated,
  test count, ruff state, OBS-E transcript ids, D-1/D-2 test-arm locations.

## §3 — Binding terminal contracts (T-1…T-18; INT must prove each)
- T-1 (D-1 law) marker exemption is EXACTLY `v2.paper.` prefix; guard keeps
  rejecting markers elsewhere; tests assert both arms incl. must-die
  `v2.research.order.*` and boundary `v2.paperwork.*`.
- T-2 (D-2 law) VALID_MODES = ("RESEARCH","SIMULATION","PAPER"); LIVE
  refused unchanged; paper writers require mode == "PAPER" (typed refusal;
  negative-mode tests).
- T-3 (N1) zero credential/secret access inside the paper domain (import
  scan executed and shipped).
- T-4 (N2) sealed single-entry execution registry (frozen literal), no
  adapter interface/ABC anywhere, typed PaperOrderIntent-only seam; the
  S6.4 attack-table tests all executed.
- T-5 (N3) every order row carries mode/actor/account/correlation/
  idempotency-key; risk decision + audit lineage by FK + event ledger.
- T-6 (N4) `fill_class` CHECK = single value 'paper_simulated'; raw INSERT
  of any other class refused (schema test); API responses carry the
  unconditional paper-simulated disclaimer.
- T-7 zero-UPDATE regime: 8 tables × UPDATE/DELETE guard pairs = 16
  triggers; verbatim guard messages; state derived from max event_index.
- T-8 Exactly-once risk evaluation via uq(intent_id); idempotency anchor
  uq(account_id, idempotency_key); fill anchor uq(intent_id, fill_index).
- T-9 State machine per accepted S2.1–S2.6: exhaustive transitions; typed
  refusals on absence; hold seam with ledger-derived consumption; the 4
  confirmation refusal classes; risk_block structurally terminal (C-1d
  both arms incl. forged-append refusal + vocabulary content assertion).
- T-10 Determinism: replay contract byte-identical ×3 (T-9-of-0047 law,
  repeated attestation counts); fill ladder equals ANNEX-P hand-computed
  sample; snapshot content re-verified before execution (mismatch = typed
  refusal).
- T-11 Money law: TEXT-decimal + Decimal internally; presentation-only
  rounding; USD-only; average-cost realized P&L; discrepancy-free.
- T-12 Reconciliation: genesis recompute + content comparison (PGF-012);
  discrepant = typed alarm, never auto-corrected.
- T-13 Census contracts at head 20260904_0048: triggers 58, permissions
  57 (+8), compver 10 (+2 components paper_execution_simulator=pxs-1.0.0,
  paper_risk_gateway=prg-1.0.0; rolling-hash recipe identical to 0047);
  RPE/RJE members/hash law unchanged (append-only).
- T-14 Drift gate: only the 9 inherited V1 tokens; zero BE-8 tokens; both
  formats; generational scoping of 0047-era drift tests at new head.
- T-15 Downgrade/no-touch: symmetric teardown; prior tables/rows/pins
  byte-untouched; cycle test passes.
- T-16 API census asserted exactly (route list pinned); envelope/errors/
  redaction/denial laws per BE-1; 401/403 negatives.
- T-17 Full suite at INT: floor = 972 + D-6 count, 0 failed, machine
  = operator box (dist-present law: CB-001 statute; the suite is run on
  the mandated terminal environment).
- T-18 PGF-021 screening: no filesystem/UI-state conditional behavior
  anywhere in delivered code; no env-var reads beyond the mode gate;
  import scan + source scan shipped as evidence.

## §4 — Prohibitions (unchanged laws, restated for the build)
No live/broker anything anywhere (no adapter abstraction, no credential
references, no network in the domain); no scheduler/tick source; no V1
paper-ledger writes (`execution_research`/test_simulated_paper_ledger
surface byte-frozen, pins re-hashed); no portfolio projection; no
multi-currency/FIFO-scope creep; no register edits in code; no silencing
of refusals; no comments claiming unimplemented proof.

## §5 — INT evidence contract (what the ITRGA will verify)
Delivery-intake: manifest hashes == files == no extras/difs; ruff clean;
D-5 sample recomputed; D-7 transcript present; design cross-map (each §3
T-item -> evidence location named in the delivery note).
Full-suite: T-17. Deep review: every T-contract line + full diff review;
migration review incl. DDL/CHECK/trigger literals vs design tables;
downgrade simulation; drift runs; determinism runs; isolation scans;
the D-1/D-2 Arms. (Corrections are issued on evidence; none are waved.)

## §6 — After INT acceptance (not this BO)
Band acceptance + CR (byte pins FD-set) -> the 20260904_0048 working-DB
application act (separate sanction): apply/verify instrument pair on the
proven 0047 chassis + PGF-020…024 statutes (+ execution-card law), anchors,
refusals, transcripts, register sync (DA-owned).

## §7 — Register line (DA-owned edit, exact text)
V2_CURRENT_STATE.md next version: BE-8 row status **BUILD ORDERED
(BO-V2-BE-8-001, 2026-09-04)**; baseline 20260903_0047; deliverables
D-1..D-8; contracts T-1..T-18; zero working-DB writes authorized.

**We don't guess. We prove.**
— BO-V2-BE-8-001 · v1.0.0 · 2026-09-04
