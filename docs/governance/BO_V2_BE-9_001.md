# BUILD ORDER — BAND BE-9: BROKER/EXCHANGE CONNECTIVITY AND ACCOUNT VISIBILITY
# BO-V2-BE-9-001 · v1.0.0 · 2026-09-05 · AXIOM Trading Terminal v2.0
# Issued by: ITRGA · Executed by: Replacement DA (code) · Custody/Git: Operator
# Authority: operator authorization "issue the build order" 2026-09-05.
# Basis: AXIOM-V2-BE-9-DESIGN-001 v1.2.1 ACCEPTED (ITRGA-ACC-V2-BE-9-DESIGN-001,
# sha256 7f61aa915f…bf). Roadmap: V2_BACKEND_ROADMAP §"Band BE-9".
# Provider substrate (resolved O-1, provisioned per DIR final edition): Exness
# demo (MT5, Standard) + investor (read-only) password; console-mounted MT5.
# Baseline: SEALED head 20260904_0048 (DB sha256 1d4005fa…61dd3e,
# ITRGA-V2-BE8-0048-CLO-SEAL-001); suite 1,026/0; triggers 58; permissions 57;
# compver 10; RPE 1499343d…/RJE 8f107d17… immutable law; PXS/PRG append-only.

## §1 — Scope of this BO
THIS BO AUTHORIZES: code + tests + migration-planning completion + docs for
BE-9, delivered as one reviewed delivery; and integration (INT) evidence.
IT DOES NOT AUTHORIZE: any working-DB application (the 0049 apply act is a
separate sanctioned act AFTER band acceptance — 0047/0048 lineage), any
register edit by the operator, any Git operation by anyone but the Operator,
any network contact with the provider (the practice endpoint contact is a
separately-authorized E1 evidence act at INT, not a compose-time act), any
change outside §2.

## §2 — Deliverables (D-1…D-8; exact file inventory + sha256 declared in the
delivery note per intake law; ASCII-only; credential scan clean)
- D-1 Domain module `backend/app/v2/broker_read/` implementing the accepted
  design verbatim in contract: `contract.py` (closed six-verb
  `BrokerReadContract` Protocol + frozen page dataclasses with provenance),
  `providers/exness_mt5.py` (the ONLY provider-leg file; MetaTrader5 package
  mapping per S1.2; time-window paging A-8 24h/1h; terminal-courtesy
  sequential law; fetch-budget A-7 200/model/run; typed taxonomy S1.3 incl.
  `broker.terminal.unavailable`), `sync.py` (all-or-nothing single-
  transaction landing; S4.1 canon result_digest), `reconcile.py`
  (authority = broker; writes only discrepancies + lineage), `vault.py`
  (SOLE resolution module per S2), `api.py` (routes per S9), `health.py`
  (three-facts law per S7). Sealed single-entry registry
  `{"exness_mt5_demo": ...}` per S1.4.
- D-2 `backend/app/db/models/v2_broker_read.py` + registration in
  `app/db/models/__init__.py` (PG-002: same unit): the 9 tables per design
  S3 (six projections + sync_run + reconcile_run + discrepancy), provenance
  pins NOT NULL, `data_class` CHECK 'simulated', environment CHECK
  'practice', anchors/unique keys verbatim from the design tables.
- D-3 Standing-law amendments (narrowest, exactly as sanctioned):
  `app/v2/rbac/permissions.py` scoped marker exemption DECISION-1 (literal
  prefix `v2.broker.`, vocabulary guard unchanged elsewhere) + the 7
  permission definitions with SAL ladder; `app/v2/api/router.py` include
  (one literal line; C3 POST-only writers).
- D-4 Migration `backend/alembic/versions/20260905_0049_v2_be9_broker_read.py`
  (down_revision = 20260904_0048; 9 tables, 18 guards (+16→76 census law
  per design recount: 18 trigger rows of 9 guard-pairs), +7 permissions,
  +1 compver row broker_read_engine=bre-1.0.0; symmetric content-based
  downgrade; zero BE-9 drift at head).
- D-5 TEST-SUBSTRATE provider + fixtures: fixture-backed `MetaTrader5`-shaped
  provider for the suite (zero network; socket guard stands), recorded
  practice-environment payload fixtures (BE-3 P1 fixture-foundation law),
  and the test vault (in-memory passphrase per S2.4 canary law). The REAL
  binding literals (server hostname + account number + environment string)
  are delivered by the Operator at a pre-INT pinning act DIRECT to the DA
  (operator-held until then; recorded in the delivery note as a provisioning
  record with no secrets — the three identifiers are pinned facts, not
  credentials; the investor password is NEVER part of any delivery).
- D-6 Test suite additions per design S11 (budget ~72 per adjusted A-6:
  ~60 base + ≈8 C-1/C-2/observation + ≈4 E-ENV-1 terminal-state; final
  count pinned by delivery manifest; floor = 1,026 + delivered count; the
  exact number pinned at INT intake).
- D-7 Register evidence: fail-first transcript for new tests where new
  behavior authorizes it (OBS-E statute; ship at least the failing-run
  witness of the failing-first posture).
- D-8 Delivery note: file inventory + sha256 of every delivered file, byte
  counts, credential-scan statement, compver component + recipe restated,
  test count, ruff state, OBS-E transcript ids, the D-5 binding-literal
  provisioning record, and the N-1…N-4 review findings closed (§3 T-20).

## §3 — Binding terminal contracts (T-1…T-20; INT must prove each)
- T-1 (DECISION-1 law) marker exemption is EXACTLY `v2.broker.` literal
  prefix; guard keeps rejecting markers elsewhere; both arms + must-die
  boundary `v2.brokerage.*`.
- T-2 (N1 provider arm) scope probe at first unlock asserts READ-ONLY login
  via the terminal trade-permission flag (NO mutation attempt — the probe
  reads facts only); master-login detection = typed refusal + audit
  (`broker.auth.misprovisioned`-class), credential refused.
- T-3 (N1 type arm) Protocol census: exactly the six read verbs as members;
  any seventh member fails; no import of the V1 `BrokerPort` anywhere.
- T-4 (N1 route arm) API census asserted exactly: 4 POST + 8 GET = 12
  routes, zero PUT/PATCH/DELETE; no route path/body carries an order verb;
  code-path scan: no import chain from `app/v2/broker_read/` to any
  module matching the execution/order/submit token predicate, with `paper`
  added to the banned set for this domain.
- T-5 (N2) provider hostname exists ONLY inside the sealed registry binding;
  zero frontend references; CORS unchanged; frontend builds unchanged.
- T-6 (N3) disjoint `v2_broker_*` vs `v2_paper_*`: no cross-FK, boundary
  tests from BOTH sides (each domain's scan names the other's tokens);
  `provider_id` NOT NULL on every projection row; `data_class` CHECK
  single value 'simulated' (raw INSERT of any other class refused, schema
  test); account `environment` CHECK 'practice'.
- T-7 (N4 vault cryptography) AES-256-GCM + Argon2id KDF + DPAPI second
  wrap; passphrase NEVER stored/logged/serialized; `ResolvedBroker
  Credential` excluded from repr/str; request-scoped only; sole-resolution
  module with import-boundary test; TTL 8h (BO-pinned O-2 default;
  operator may set at INT); lock/unlock/rotate audited; rotate = two-act
  confirmation (BE-8 pattern).
- T-8 (N4 blindness suite) grep-class audit in-suite (domain + API +
  fixtures; sole-resolution import rule; credential field-name absence in
  response models); error-path canary; PAYLOAD-ECHO canary (canary bytes
  exist ONLY in the lawful projection row, never logs/envelopes/audits/
  health); generic denial vocabulary (no vault/KDF/path terms anywhere);
  the vault file path lives outside the repo and never appears in code,
  logs, or envelopes.
- T-9 Projections append-only: 9 tables × UPDATE/DELETE guard pairs = 18
  triggers, verbatim guard messages; "current" = derivation (greatest
  record_seq / latest complete sync_run); table anchors per design rows.
- T-10 Idempotency & replay: fill anchor uq(provider_id,
  broker_account_ext_id, transaction_ext_id) with `broker.fill.reused`
  audited skips; snapshot generations by sync_run_id; `result_digest`
  under the S4.1 canon; ×3 replay attestation against the fixture
  provider with equal digests.
- T-11 S4.1 canon implemented verbatim + its three pinned tests
  (shuffle-invariance, single-value sensitivity, volatile-invariance);
  allow-list exclusion (unknown fields excluded by construction).
- T-12 Reconcile lineage: every reconciliation lands exactly one
  `v2_broker_reconcile_run` row — both-side digests under the ONE canon,
  scope, counts, `discrepancy_count`, `outcome` CHECK ('clean',
  'discrepant') consistent with the count (CHECK-companion via trigger
  or test-armed application invariant — mechanism pinned at INT).
- T-13 Discrepancy machine: 7 closed CHECK classes; detected → triaged →
  owned → resolved | dismissed_with_reason (reason mandatory, typed
  refusal without); generations only (zero UPDATE); ownership at `owned`;
  aging derived; every transition audited.
- T-14 Health three-facts law: terminal / reachability / freshness each
  independent; `terminal_up_unreachable` and `reachable_stale` typed
  states; GREEN requires all three; classifier-visible facts only (no
  payload, no credential, no vault path); read-only-login assertion state
  surfacing from T-2; unowned-discrepancy count by threshold.
- T-15 Sync law: operator-initiated POST only (no scheduler — V2-TD-24
  family); single-transaction landing; partial fetch ⇒ `partial_refused`
  + zero rows + typed refusal; fetch-budget ceiling ⇒
  `broker.fetch_budget.exceeded` (zero rows); terminal unavailable ⇒
  `broker.terminal.unavailable` (zero rows); history windows per A-8
  with 1h overlap deduped on ticket ids.
- T-16 Money/currency law: TEXT-decimal + Decimal internally; comparisons
  exact (no tolerance v1); currency explicit per row; single-currency
  scope; Standard-demo balance units = USD cents-of-dollar zero skew
  (provision pin compiance test — a cent-denominated environment is
  REFUSED at the binding assertion, test-armed).
- T-17 Census contracts at head 20260905_0049: triggers 76, permissions
  64 (+7), compver 11 (+1 component broker_read_engine=bre-1.0.0 over
  {adapter,sync,reconcile}.py; rolling-hash recipe identical to
  0047/0048); RPE/RJE members/hash law unchanged and verified;
  PXS/PRG members/hash law unchanged and verified.
- T-18 Drift gate: only the 9 inherited V1 tokens; zero BE-9 tokens; both
  formats; generational scoping of 0048-era drift tests at new head.
- T-19 Downgrade/no-touch: symmetric teardown; prior tables/rows/pins
  byte-untouched; cycle test passes; full suite at INT: floor = 1,026 +
  D-6 count, 0 failed, operator box (CB-001 statute).
- T-20 Findings closure (ACC §3): N-1 cosmetic change-note title fixed;
  N-2 pages-vs-windows vocabulary unified (pick "page" as the canonical
  fetch unit, window = time span, declare both at header of adapter
  docs); N-3 the S4 REST-era 429/token-bucket line must NOT appear in
  code/docs (terminal-courtesy law only); N-4 first-sync ORIGIN rule
  pinned (history windows begin at the account-creation basis or a
  BO-declared origin floor, whichever the DA declares with rationale at
  INT; origin fact recorded per first sync row).

## §4 — Prohibitions (unchanged laws, restated for the build)
No order submission/amendment/cancellation anywhere (no verb, no shape,
no import of a mutation-capable protocol); NO live broker account of any
tier (binding assertion refuses non-practice environment); NO third-party
relays/hosted bridges for the provider leg; NO master password in the
vault (the Operator attests investor-only at the D-5 provisioning record);
no scheduler; no network in the suite (socket guard; E1 practice contact
only as the separately-authorized evidence act); no multi-broker; no
tolerance bands; no paper↔broker joins; no register edits in code; no
silencing of refusals; no comments claiming unimplemented proof; the
operator-vault directory must never be created under the repo tree.

## §5 — INT evidence contract (what the ITRGA will verify)
Delivery-intake: manifest hashes == files == no extras/difs; ruff clean;
ASCII-only; the D-5 provisioning record (binding literals present,
no secrets); N-1…N-4 closures present; design cross-map (each §3 T-item
→ evidence location named in the delivery note).
Full-suite: T-19. Deep review: every T-contract line + full diff review;
migration review incl. DDL/CHECK/trigger literals vs design tables;
downgrade simulation; drift runs; ×3 replay runs; canon test battery;
isolation scans (T-2/T-3/T-4/T-5/T-6/T-8 arms). (Corrections are issued
on evidence; none are waved.)

## §6 — After INT acceptance (not this BO)
Band acceptance + CR (byte pins FD-set) → the E1 practice-endpoint contact
act (separately-authorized, witnessed-transcript pattern of 0047/0048,
under plain-wifi per E-ENV-1) → the 20260905_0049 working-DB application
act (separate sanction): apply/verify instrument pair on the proven 0048
console chassis (identity gate = sealed 0048 sha `1d4005fa…61dd3e` +
`current == 20260904_0048`; witnessed-pre-landing adjudication already
compiled into the chassis' chain-shape gate; E-0046-DUP; PGF-020…024;
execution-card law; the ISS-004/005/006 defect lessons compiled):
anchors, refusals, transcripts, register sync (DA-owned).

## §7 — Register line (DA-owned edit, exact text)
V2_CURRENT_STATE.md next version: BE-9 row status **BUILD ORDERED
(BO-V2-BE-9-001, 2026-09-05)**; baseline sealed 20260904_0048
(sha 1d4005fa…); design v1.2.1 ACCEPTED; deliverables D-1..D-8;
contracts T-1..T-20; provider Exness/MT5-investor (demo);
zero working-DB writes authorized; zero network authorized
(E1 = separate act).

**We don't guess. We prove.**
— BO-V2-BE-9-001 · v1.0.0 · 2026-09-05
