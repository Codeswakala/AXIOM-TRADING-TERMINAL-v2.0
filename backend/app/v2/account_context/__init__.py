"""AXIOM V2 BE-10 — signal-against-account intelligence (BO-V2-BE-10-001).

Read-only analytics join: BE-9 broker projections x BE-5 signal registry.
Zero execution verbs; zero broker writes; zero broker contact (this band
never imports the provider leg, the vault, or any network surface — the
banned-import law L3 is census-tested). The band SPEAKS analysis in
state nouns; it never prescribes trades.
"""
